Only in ../../windrbd-1.1-branch/windrbd/converted-sources: .git
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: .gitignore
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-Added-files-from-1.1.6-release.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-Corking-Uncorking-backing-device-request-in-bitmap-I.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-No-timeout-waiting-for-becoming-standalone-in-try_di.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-Only-outdate-on-disconnect-when-there-is-currently-a.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-drbd-Give-up-local-wenr-references-if-necessary.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-drbd-headers-IOCTL_WINDRBD_ROOT_ARE_THERE_NL_PACKETS.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-drbd_request_endio-avoid-unnecessary-BSOD-s.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: 0001-transport_tcp-Treat-EINTR-and-ERESTARTSYS-as-errors-.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources: busy-resync-suspend-io.patch
diff -r -u converted-sources/drbd/Makefile ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/Makefile
--- converted-sources/drbd/Makefile	2024-09-03 16:13:51.273309732 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/Makefile	2024-03-08 13:36:11.619781302 +0000
@@ -49,49 +49,10 @@
 
 # Name of the pfx file (without extension)
 KEY = linbit-2019
-
-ifeq ($(BUILD_ENV),jt-win7-32bit)
-export EWDK_BASE := c:\\Ewdk1703
-EWDK_KIT := $(EWDK_BASE)\\Program Files\\Windows Kits\\10
-EWDK_VERSION := 10.0.15063.0
-EWDK_BIN := $(EWDK_KIT)\\bin\\x86
-
-# Name of the pfx file (without extension)
-KEY = linbit-ha
-PASSWD = ""
-
-WINDRBD_SOURCE = ..
-DRBD_UTILS_SOURCE = Y:\\drbd-utils-windows-32bit
-
-CONFIG_32BIT = 1
-endif
-
-# (does not work)
-# https://www.microsoft.com/en-us/download/details.aspx?id=11800
-# also does not work (can't start cl):
-# git clone https://github.com/9176324/WinDDK.git
-# But it works after doing:
-# cd /cygdrive/c/WinDDK
-# find . -iname \*.exe | xargs chmod a+x
-# find . -iname \*.dll | xargs chmod a+x
-
-# But does not work at all .. C compiler does not understand
-# gcc extension at all use jt-win7-32bit environment instead
-ifeq ($(BUILD_ENV),jt-win7-32bit-server2003)
-export EWDK_BASE := C:\\WinDDK
-EWDK_VERSION := 7600.16385.1
-EWDK_KIT := $(EWDK_BASE)\\$(EWDK_VERSION)
-EWDK_BIN := $(EWDK_KIT)\\bin\\x86
-
-# Name of the pfx file (without extension)
-KEY = linbit-ha
 PASSWD = ""
 
 WINDRBD_SOURCE = ..
 DRBD_UTILS_SOURCE = Y:\\drbd-utils-windows
-
-CONFIG_32BIT = 1
-DDK = 1
 endif
 
 # Linux build host running MSVC compiler using wine :)
@@ -163,8 +124,6 @@
 
 WINDRBD_SOURCE = ..
 DRBD_UTILS_SOURCE = Y:\\drbd-utils-windows
-
-CONFIG_32BIT = 1
 endif
 
 ifeq ($(BUILD_ENV),jt-server2016)
@@ -230,67 +189,37 @@
 # This is relative to the inno-setup directory
 DRIVER_DIR ?= converted-sources\\drbd
 
-ifdef CONFIG_32BIT
-CYGWIN_BINARIES = cygwin-binaries-x86
-else
-CYGWIN_BINARIES = cygwin-binaries
-endif
-
-ifdef DDK
-EWDK_INC := $(EWDK_KIT)\\inc
-EWDK_LIB := $(EWDK_KIT)\\lib\\wnet\\i386
-else
 EWDK_INC := $(EWDK_KIT)\\Include
 EWDK_LIB := $(EWDK_KIT)\\Lib
-endif
-
 VC ?= /cygdrive/c/Windows/System32/cmd.exe /c ms-cl-$(BUILD_ENV).cmd
 MSBUILD ?= /cygdrive/c/Windows/System32/cmd.exe /c run-msbuild.cmd
-CERTMGR := "$(EWDK_BIN)/certmgr.exe"
 
 # /a      for debugging output
 
 
 # CL.exe takes (AND RETURNS) forward slashes; that makes navigating an error output easy.
-ifdef DDK
-C_DEFINES += -D inline=__inline
-WIN_INCLUDE_DIRS += "-I$(EWDK_INC)"\\ddk
-WIN_INCLUDE_DIRS += "-I$(EWDK_INC)"\\crt
-else
 WIN_INCLUDE_DIRS += "-I$(EWDK_INC)\\$(EWDK_VERSION)\\shared"
 WIN_INCLUDE_DIRS += "-I$(EWDK_INC)\\$(EWDK_VERSION)\\km"
 WIN_INCLUDE_DIRS += "-I$(EWDK_INC)\\$(EWDK_VERSION)\\km\\crt"
 WIN_INCLUDE_DIRS += "-I$(EWDK_INC)\\$(EWDK_VERSION)\\um"
 
 WIN_DEF_INCLUDES += /FI"$(EWDK_INC)/$(EWDK_VERSION)\\shared\\warning.h"
-endif
 # include that file
 
-WIN_CFLAGS += /c /nologo /W4 
-ifndef DDK
+WIN_CFLAGS += /c /W4 
 WIN_CFLAGS += /Zi # /Zi gives us a PDB file; without that we'd get debug information in the .obj file instead
-endif
 WIN_CFLAGS += /WX # warnings == errors
-ifndef DDK
 WIN_CFLAGS += /Wv:18 # disable hides previous local declaration
-endif
 WIN_CFLAGS += /O2 # optimize for speed
 WIN_CFLAGS += /Oi # enable intrinsic functions
 WIN_CFLAGS += /Oy- # enable frame pointer omission
-
-ifdef CONFIG_32BIT
-C_DEFINES += -D _X86_=1 -D i386=1 -D _M_IX86=1 # 32bit
-# C_DEFINES += -D DECLSPEC_IMPORT="__declspec(dllimport)" -D _WIN32_WINNT=0x0601 -D WINVER=0x0601 -D WINNT=1 -D NTDDI_VERSION=0x06010000 -D KMDF_VERSION_MAJOR=1 -D KMDF_VERSION_MINOR=15 
-C_DEFINES += -D _WIN32_WINNT=0x0502 -D WINVER=0x0502 -D WINNT=1 -D NTDDI_VERSION=0x05020000
-else
+# C_DEFINES += -D _X86_=1 -D i386=1 # 32bit
 C_DEFINES += -D _WIN64 -D _AMD64_ -D AMD64 -D _M_AMD64 # 64bit
 C_DEFINES += -D STD_CALL -D DEPRECATE_DDK_FUNCTIONS=1 -D MSC_NOOPT -D _WIN32_WINNT=0x0601 -D WINVER=0x0601 -D WINNT=1 -D NTDDI_VERSION=0x06010000 -D KMDF_VERSION_MAJOR=1 -D KMDF_VERSION_MINOR=15 
 # This makes Windows use NonPagedPoolNx instead of NonPagedPool for
 # memory allocations. This is needed to pass the code integrity tests
 # for hardware lab kit (HLK) needed for SecureBoot.
 C_DEFINES += -DPOOL_NX_OPTIN=1
-endif
-
 # C_DEFINES += -D DBG=1
 ifdef R
 C_DEFINES += -D RELEASE=1
@@ -302,32 +231,17 @@
 C_DEFINES += -D BIO_REF_DEBUG=1
 endif
 
-ifdef CONFIG_32BIT
-# /Gy makes declspec(dllimport) work incorrectly (need _foo@4 decoration for
-# 32 bit linking) need to specify /Gz (default to stdcall) for making linking
-# working. /Gs1048576 disables calls to chkstk()
-WIN_CFLAGS += /GF /Gm- /Zp8 /Gz /Gs1048576
-else
 WIN_CFLAGS += /GF /Gm- /Zp8 /GS /Gy 
-endif
 # /fp:precise  # precise - "precise" floating-point model; results are predictable
-WIN_CFLAGS += /Zc:wchar_t- /Zc:forScope /GR- 
-# Output preprocessor output. Disable this to compile 'normally'
-# WIN_CFLAGS += /P
+WIN_CFLAGS += /Zc:wchar_t- /Zc:forScope /Zc:inline /GR- 
 # On x64, there's only one calling convention. https://msdn.microsoft.com/en-us/library/zthk2dkh.aspx
 # WIN_CFLAGS += /Gz # __stdcall calling convention  -- is default
 # WIN_CFLAGS += /Gr # __fastcall calling convention -- is what we want for linux compat
 WIN_CFLAGS += /wd4748 /wd4603 /wd4627 /wd4986 /wd4987 /wd4996 
-ifndef DDK
 WIN_CFLAGS += /analyze- /errorReport:queue /kernel -cbstring /d1import_no_registry /d2AllowCompatibleILVersions /d2Zi+ 
-endif
 C_DEFINES += -D __KERNEL__=1
 C_DEFINES += -D __BYTE_ORDER=1 -D __LITTLE_ENDIAN=1 -D __LITTLE_ENDIAN_BITFIELD
 
-# see ./shared/sdkddkver.h for a list.
-# this is Windows XP
-# C_DEFINES += -D NTDDI_VERSION=0x05010000
-
 # WIN_CFLAGS += /Ze # Microsoft extensions - eg. nameless struct/union. Not enabled because
 #   The /Ze option is deprecated because its behavior is on by default. We recommend you use the /Zc (Conformance) compiler options to control specific language extension features.
 # but there's no setting for that.
@@ -345,8 +259,7 @@
 WIN_CFLAGS += /showIncludes 
 endif
 
-# TODO: remove this it is duplicate
-# C_DEFINES += -D _WIN64=1
+C_DEFINES += -D _WIN64=1
 C_DEFINES += -D COMPAT_HAVE_BOOL_TYPE=1 # for "typedef bool" in drbd/linux/lru_cache.h
 # C_DEFINES += -D _AMD64_=1
 
@@ -359,48 +272,23 @@
 
 WIN_LDFLAGS += /link # must be first!
 # Was:
-WIN_LDFLAGS += /DRIVER:WDM /SUBSYSTEM:NATIVE /NODEFAULTLIB /ENTRY:DriverEntry
+# WIN_LDFLAGS += /DRIVER:WDM /NODEFAULTLIB /ENTRY:DriverEntry
 # However this prevents the driver from being unloaded. We try
 # this now:
+WIN_LDFLAGS += /DRIVER /SUBSYSTEM:WINDOWS /NODEFAULTLIB /ENTRY:DriverEntry
 # Commented out. Else cannot find __imp_KeGetCurrentIrql symbol on linking
 # WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)/win7/km/x64/"
-# WIN_LDFLAGS += /DRIVER /SUBSYSTEM:WINDOWS /NODEFAULTLIB /ENTRY:DriverEntry
-
-ifdef DDK
-WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)"
-else
-ifdef CONFIG_32BIT
-WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)/win7/km/x86/"
-WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)/$(EWDK_VERSION)/km/x86/"
-else
-WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)/win7/km/x64/"
 WIN_LDFLAGS += "/LIBPATH:$(EWDK_LIB)/$(EWDK_VERSION)/km/x64/"
-endif
-endif
-
 WIN_LDFLAGS += /DEBUG:FULL
-
 WIN_LINK_INPUT += wdm.lib
 WIN_LINK_INPUT += ntoskrnl.lib
-ifndef CONFIG_32BIT
 WIN_LINK_INPUT += bufferoverflowfastfailk.lib # for __security_check_cookie
 WIN_LINK_INPUT += netio.lib
-WIN_LINK_INPUT += aux_klib.lib
-else
-WIN_LINK_INPUT += bufferoverflowk.lib # for __security_check_cookie
-endif
-ifdef CONFIG_32BIT
 WIN_LINK_INPUT += wdmsec.lib
-endif
 WIN_LINK_INPUT += ksecdd.lib
 # TODO: used?
 # WIN_LINK_INPUT += "$(EWDK_LIB)/win8/km/x64/aux_klib.lib"
 WIN_LINK_INPUT += hal.lib
-WIN_LINK_INPUT += aux_klib.lib
-
-# TODO: only for 32 bit?
-WIN_LINK_INPUT += iointex.lib
-WIN_LINK_INPUT += procgrp.lib
 # WIN_LINK_INPUT += setupapi.lib
 
 DRBD_FILES = drbd_buildtag.c drbd_bitmap.c drbd_proc.c
@@ -433,8 +321,6 @@
 	mv -f "_$@" "$@"
 # currently does not work with wine:
 	$(WINE) "$(EWDK_BIN)/inf2cat.exe" /driver:. /os:XP_X86,Vista_X86,Vista_X64,7_X86,7_X64,8_X86,8_X64,6_3_X86,6_3_X64,10_X86,10_X64
-# No driver signing on 32 bit platforms ...
-ifndef CONFIG_32BIT
 ifdef R
 # Linbit DigiCert certificate (trusted by Microsoft)
 # For Windows Server 2008 use SHA1 as secure hash
@@ -451,7 +337,7 @@
 	$(WINE) "$(EWDK_BIN)/signtool.exe" sign /fd SHA256 /f ../../crypto/$(KEY).pfx /p $(PASSWD) /v "$@"
 	$(WINE) "$(EWDK_BIN)/signtool.exe" sign /fd SHA256 /f ../../crypto/$(KEY).pfx /p $(PASSWD) /v windrbd.cat
 endif
-endif
+
 
 resource.res: resource.rc
 	$(WINE) "$(EWDK_BIN)/rc.exe" $<
@@ -469,12 +355,7 @@
 
 %.obj: %.c
 	rm -f "$(call fPDB,$<)"
-ifdef DDK
-	$(VC) $(WIN_CFLAGS) $< /c /Fo"$@" /Fd"$(call fPDB,$<)"
-else
 	$(VC) $(WIN_CFLAGS) $(WIN_DEF_INCLUDES) "$<" /c /Fo:"$@" /Fd:"$(call fPDB,$<)"
-endif
-
 # /Fi:"$(patsubst %.c,%.E,$<)" ...  cl : Command line warning D9007 : '/Fi:' requires '/P'; option ignored
 	echo "-- $@ done --"
 
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: _windrbd.ilk
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: disk-timeout.patch
diff -r -u converted-sources/drbd/drbd-headers/drbd_strings.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_strings.h
--- converted-sources/drbd/drbd-headers/drbd_strings.h	2024-09-03 16:24:00.063228433 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_strings.h	2024-08-05 17:29:15.373682308 +0000
@@ -21,5 +21,4 @@
 extern const char *drbd_set_st_err_str(enum drbd_state_rv);
 extern const char *drbd_packet_name(enum drbd_packet);
 
-
 #endif  /* __DRBD_STRINGS_H */
diff -r -u converted-sources/drbd/drbd-headers/drbd_strings.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_strings.h.tmp.bak
--- converted-sources/drbd/drbd-headers/drbd_strings.h.tmp.bak	2024-09-03 16:24:00.059228395 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_strings.h.tmp.bak	2024-08-05 17:29:15.369682271 +0000
@@ -21,5 +21,4 @@
 extern const char *drbd_set_st_err_str(enum drbd_state_rv);
 extern const char *drbd_packet_name(enum drbd_packet);
 
-
 #endif  /* __DRBD_STRINGS_H */
diff -r -u converted-sources/drbd/drbd-headers/drbd_transport.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h
--- converted-sources/drbd/drbd-headers/drbd_transport.h	2024-09-03 16:24:01.579243122 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h	2024-08-05 17:29:16.517692784 +0000
@@ -28,25 +28,25 @@
 #define GFP_TRY	(__GFP_HIGHMEM | __GFP_NOWARN | __GFP_RECLAIM)
 
 
-#define tr_printk(level, transport, fmt, args...)  {                    \
+#define tr_printk(level, transport, fmt, ...)  {                        \
     KIRQL _tr_printk_rcu_flags;                                         \
     _tr_printk_rcu_flags = rcu_read_lock();                             \
     printk(level "drbd %s %s:%s: " fmt,                                 \
            (transport)->log_prefix,                                     \
            (transport)->class->name,                                    \
            rcu_dereference((transport)->net_conf)->name,                \
-	   ## args);                                                    \
+           __VA_ARGS__);                                                \
     rcu_read_unlock(_tr_printk_rcu_flags);                              \
     }
 
-#define tr_err(transport, fmt, args...) \
-	tr_printk(KERN_ERR, transport, fmt, ## args)
-#define tr_warn(transport, fmt, args...) \
-	tr_printk(KERN_WARNING, transport, fmt, ## args)
-#define tr_notice(transport, fmt, args...) \
-	tr_printk(KERN_NOTICE, transport, fmt, ## args)
-#define tr_info(transport, fmt, args...) \
-	tr_printk(KERN_INFO, transport, fmt, ## args)
+#define tr_err(transport, fmt, ...) \
+	tr_printk(KERN_ERR, transport, fmt, ## __VA_ARGS__)
+#define tr_warn(transport, fmt, ...) \
+	tr_printk(KERN_WARNING, transport, fmt, ## __VA_ARGS__)
+#define tr_notice(transport, fmt, ...) \
+	tr_printk(KERN_NOTICE, transport, fmt, ## __VA_ARGS__)
+#define tr_info(transport, fmt, ...) \
+	tr_printk(KERN_INFO, transport, fmt, ## __VA_ARGS__)
 
 #define TR_ASSERT(x, exp)							\
 	do {									\
diff -r -u converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.bak
--- converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.bak	2024-09-03 16:24:00.887236418 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.bak	2024-08-05 17:29:16.085688828 +0000
@@ -27,24 +27,26 @@
  */
 #define GFP_TRY	(__GFP_HIGHMEM | __GFP_NOWARN | __GFP_RECLAIM)
 
-#define tr_printk(level, transport, fmt, args...)  {		\
-	rcu_read_lock();					\
-	printk(level "drbd %s %s:%s: " fmt,			\
-	       (transport)->log_prefix,				\
-	       (transport)->class->name,			\
-	       rcu_dereference((transport)->net_conf)->name,	\
-	       ## args);					\
-	rcu_read_unlock();					\
-	}
-
-#define tr_err(transport, fmt, args...) \
-	tr_printk(KERN_ERR, transport, fmt, ## args)
-#define tr_warn(transport, fmt, args...) \
-	tr_printk(KERN_WARNING, transport, fmt, ## args)
-#define tr_notice(transport, fmt, args...) \
-	tr_printk(KERN_NOTICE, transport, fmt, ## args)
-#define tr_info(transport, fmt, args...) \
-	tr_printk(KERN_INFO, transport, fmt, ## args)
+
+#define tr_printk(level, transport, fmt, ...)  {                        \
+    KIRQL _tr_printk_rcu_flags;                                         \
+    _tr_printk_rcu_flags = rcu_read_lock();                             \
+    printk(level "drbd %s %s:%s: " fmt,                                 \
+           (transport)->log_prefix,                                     \
+           (transport)->class->name,                                    \
+           rcu_dereference((transport)->net_conf)->name,                \
+           __VA_ARGS__);                                                \
+    rcu_read_unlock(_tr_printk_rcu_flags);                              \
+    }
+
+#define tr_err(transport, fmt, ...) \
+	tr_printk(KERN_ERR, transport, fmt, ## __VA_ARGS__)
+#define tr_warn(transport, fmt, ...) \
+	tr_printk(KERN_WARNING, transport, fmt, ## __VA_ARGS__)
+#define tr_notice(transport, fmt, ...) \
+	tr_printk(KERN_NOTICE, transport, fmt, ## __VA_ARGS__)
+#define tr_info(transport, fmt, ...) \
+	tr_printk(KERN_INFO, transport, fmt, ## __VA_ARGS__)
 
 #define TR_ASSERT(x, exp)							\
 	do {									\
@@ -216,7 +218,6 @@
 	struct list_head list;
 };
 
-
 /* An "abstract base class" for transport implementations. I.e. it
    should be embedded into a transport specific representation of a
    listening "socket" */
diff -r -u converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.orig
--- converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.orig	2024-09-03 16:24:01.483242193 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/drbd_transport.h.tmp.orig	2024-08-05 17:29:16.413691832 +0000
@@ -27,24 +27,26 @@
  */
 #define GFP_TRY	(__GFP_HIGHMEM | __GFP_NOWARN | __GFP_RECLAIM)
 
-#define tr_printk(level, transport, fmt, args...)  {		\
-	rcu_read_lock();					\
-	printk(level "drbd %s %s:%s: " fmt,			\
-	       (transport)->log_prefix,				\
-	       (transport)->class->name,			\
-	       rcu_dereference((transport)->net_conf)->name,	\
-	       ## args);					\
-	rcu_read_unlock();					\
-	}
-
-#define tr_err(transport, fmt, args...) \
-	tr_printk(KERN_ERR, transport, fmt, ## args)
-#define tr_warn(transport, fmt, args...) \
-	tr_printk(KERN_WARNING, transport, fmt, ## args)
-#define tr_notice(transport, fmt, args...) \
-	tr_printk(KERN_NOTICE, transport, fmt, ## args)
-#define tr_info(transport, fmt, args...) \
-	tr_printk(KERN_INFO, transport, fmt, ## args)
+
+#define tr_printk(level, transport, fmt, ...)  {                        \
+    KIRQL _tr_printk_rcu_flags;                                         \
+    _tr_printk_rcu_flags = rcu_read_lock();                             \
+    printk(level "drbd %s %s:%s: " fmt,                                 \
+           (transport)->log_prefix,                                     \
+           (transport)->class->name,                                    \
+           rcu_dereference((transport)->net_conf)->name,                \
+           __VA_ARGS__);                                                \
+    rcu_read_unlock(_tr_printk_rcu_flags);                              \
+    }
+
+#define tr_err(transport, fmt, ...) \
+	tr_printk(KERN_ERR, transport, fmt, ## __VA_ARGS__)
+#define tr_warn(transport, fmt, ...) \
+	tr_printk(KERN_WARNING, transport, fmt, ## __VA_ARGS__)
+#define tr_notice(transport, fmt, ...) \
+	tr_printk(KERN_NOTICE, transport, fmt, ## __VA_ARGS__)
+#define tr_info(transport, fmt, ...) \
+	tr_printk(KERN_INFO, transport, fmt, ## __VA_ARGS__)
 
 #define TR_ASSERT(x, exp)							\
 	do {									\
@@ -195,7 +197,7 @@
 	void (*stats)(struct drbd_transport *, struct drbd_transport_stats *stats);
 	void (*net_conf_change)(struct drbd_transport *, struct net_conf *new_net_conf);
 	void (*set_rcvtimeo)(struct drbd_transport *, enum drbd_stream, LONG_PTR timeout);
-	LONG_PTR (*get_rcvtimeo)(struct drbd_transport *, enum drbd_stream);
+	long (*get_rcvtimeo)(struct drbd_transport *, enum drbd_stream);
 	int (*send_page)(struct drbd_transport *, enum drbd_stream, struct page *,
 			 int offset, size_t size, unsigned msg_flags);
 	int (*send_zc_bio)(struct drbd_transport *, struct bio *bio);
@@ -216,7 +218,6 @@
 	struct list_head list;
 };
 
-
 /* An "abstract base class" for transport implementations. I.e. it
    should be embedded into a transport specific representation of a
    listening "socket" */
diff -r -u converted-sources/drbd/drbd-headers/linux/genl_magic_func.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h
--- converted-sources/drbd/drbd-headers/linux/genl_magic_func.h	2024-09-03 16:24:11.315337454 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h	2024-08-05 17:29:24.949770016 +0000
@@ -46,7 +46,7 @@
 
 #ifndef __KERNEL__
 #ifndef pr_info
-#define pr_info(args...)	fprintf(stderr, args);
+#define pr_info(...)	fprintf(stderr, __VA_ARGS__);
 #endif
 /* genl_magic_func.h generates *_from_attrs() helper functions
  * that now allocate/free the nested attribute tables.
@@ -182,7 +182,7 @@
 	return __ ## s_name ## _from_attrs(s, NULL, info, true);	\
 }					__attribute__((unused))		\
 
-#define __assign(attr_nr, attr_flag, name, nla_type, type, assignment...)	\
+#define __assign(attr_nr, attr_flag, name, nla_type, type, ...)	\
 		nla = ntb[attr_nr];						\
 		if (nla) {						\
 			if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
@@ -190,7 +190,7 @@
 				err = -EEXIST;				\
 				goto out;				\
 			}						\
-			assignment;					\
+			__VA_ARGS__;					\
 		} else if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
 			/* attribute missing from payload, */		\
 			/* which was expected */			\
@@ -352,7 +352,6 @@
 	return s_name ## _to_skb(skb, s, 1);				\
 }
 
-
 #undef __field
 #define __field(attr_nr, attr_flag, name, nla_type, type, __get, __put,	\
 		__is_signed)						\
@@ -375,7 +374,6 @@
 
 #include GENL_MAGIC_INCLUDE_FILE
 
-
 /* Functions for initializing structs to default values.  */
 
 #undef __field
diff -r -u converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.bak
--- converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.bak	2024-09-03 16:24:11.127335632 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.bak	2024-08-05 17:29:24.821768843 +0000
@@ -46,7 +46,7 @@
 
 #ifndef __KERNEL__
 #ifndef pr_info
-#define pr_info(args...)	fprintf(stderr, args);
+#define pr_info(...)	fprintf(stderr, __VA_ARGS__);
 #endif
 /* genl_magic_func.h generates *_from_attrs() helper functions
  * that now allocate/free the nested attribute tables.
@@ -182,7 +182,7 @@
 	return __ ## s_name ## _from_attrs(s, NULL, info, true);	\
 }					__attribute__((unused))		\
 
-#define __assign(attr_nr, attr_flag, name, nla_type, type, assignment...)	\
+#define __assign(attr_nr, attr_flag, name, nla_type, type, ...)	\
 		nla = ntb[attr_nr];						\
 		if (nla) {						\
 			if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
@@ -190,7 +190,7 @@
 				err = -EEXIST;				\
 				goto out;				\
 			}						\
-			assignment;					\
+			__VA_ARGS__;					\
 		} else if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
 			/* attribute missing from payload, */		\
 			/* which was expected */			\
@@ -352,7 +352,6 @@
 	return s_name ## _to_skb(skb, s, 1);				\
 }
 
-
 #undef __field
 #define __field(attr_nr, attr_flag, name, nla_type, type, __get, __put,	\
 		__is_signed)						\
@@ -375,7 +374,6 @@
 
 #include GENL_MAGIC_INCLUDE_FILE
 
-
 /* Functions for initializing structs to default values.  */
 
 #undef __field
diff -r -u converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.orig
--- converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.orig	2024-09-03 16:24:11.131335671 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_func.h.tmp.orig	2024-08-05 17:29:24.825768880 +0000
@@ -46,7 +46,7 @@
 
 #ifndef __KERNEL__
 #ifndef pr_info
-#define pr_info(args...)	fprintf(stderr, args);
+#define pr_info(...)	fprintf(stderr, __VA_ARGS__);
 #endif
 /* genl_magic_func.h generates *_from_attrs() helper functions
  * that now allocate/free the nested attribute tables.
@@ -182,7 +182,7 @@
 	return __ ## s_name ## _from_attrs(s, NULL, info, true);	\
 }					__attribute__((unused))		\
 
-#define __assign(attr_nr, attr_flag, name, nla_type, type, assignment...)	\
+#define __assign(attr_nr, attr_flag, name, nla_type, type, ...)	\
 		nla = ntb[attr_nr];						\
 		if (nla) {						\
 			if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
@@ -190,7 +190,7 @@
 				err = -EEXIST;				\
 				goto out;				\
 			}						\
-			assignment;					\
+			__VA_ARGS__;					\
 		} else if (exclude_invariants && !!((attr_flag) & DRBD_F_INVARIANT)) {		\
 			/* attribute missing from payload, */		\
 			/* which was expected */			\
@@ -352,7 +352,6 @@
 	return s_name ## _to_skb(skb, s, 1);				\
 }
 
-
 #undef __field
 #define __field(attr_nr, attr_flag, name, nla_type, type, __get, __put,	\
 		__is_signed)						\
@@ -375,7 +374,6 @@
 
 #include GENL_MAGIC_INCLUDE_FILE
 
-
 /* Functions for initializing structs to default values.  */
 
 #undef __field
diff -r -u converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h
--- converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h	2024-09-03 16:24:07.775303156 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h	2024-08-05 17:29:21.825741399 +0000
@@ -123,7 +123,7 @@
 #define __str_field_def(attr_nr, attr_flag, name, maxlen) \
 	__str_field(attr_nr, attr_flag, name, maxlen)
 
-#define GENL_op_init(args...)	args
+#define GENL_op_init(...)	__VA_ARGS__
 #define GENL_doit(handler)		\
 	.doit = handler,		\
 	.flags = GENL_ADMIN_PERM,
diff -r -u converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h.tmp.bak
--- converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h.tmp.bak	2024-09-03 16:24:07.771303117 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd-headers/linux/genl_magic_struct.h.tmp.bak	2024-08-05 17:29:21.821741362 +0000
@@ -123,7 +123,7 @@
 #define __str_field_def(attr_nr, attr_flag, name, maxlen) \
 	__str_field(attr_nr, attr_flag, name, maxlen)
 
-#define GENL_op_init(args...)	args
+#define GENL_op_init(...)	__VA_ARGS__
 #define GENL_doit(handler)		\
 	.doit = handler,		\
 	.flags = GENL_ADMIN_PERM,
diff -r -u converted-sources/drbd/drbd_actlog.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c
--- converted-sources/drbd/drbd_actlog.c	2024-09-03 16:23:37.939014050 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c	2024-08-05 17:29:00.229543669 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 2003-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2003-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
  /* Enable all warnings throws lots of those warnings: */
@@ -63,15 +62,9 @@
 void wait_until_done_or_force_detached(struct drbd_device *device, struct drbd_backing_dev *bdev,
 				       unsigned int *done)
 {
! manual: disable DRBD disk timeout (disk timeout is handled by WinDRBD)
-	KIRQL rcu_flags;
 	LONG_PTR dt;
 
-	rcu_flags = rcu_read_lock();
-	dt = rcu_dereference(bdev->disk_conf)->disk_timeout;
-	rcu_read_unlock(rcu_flags);
-	dt = dt * HZ / 10;
-	if (dt == 0)
-		dt = MAX_SCHEDULE_TIMEOUT;
+	dt = MAX_SCHEDULE_TIMEOUT;
 
 	wait_event_timeout(dt, device->misc_wait,
 			   *done || test_bit(FORCE_DETACH, &device->flags),
@@ -532,7 +525,6 @@
 	KIRQL rcu_flags;
 	bool locked = false;
 
-
 	if (drbd_md_dax_active(device->ldev)) {
 		drbd_dax_al_begin_io_commit(device);
 		return;
@@ -916,7 +908,6 @@
 	[SET_OUT_OF_SYNC] = "drbd_set_out_of_sync"
 };
 
-
 /* ATTENTION. The AL's extents are 4MB each, while the extents in the
  * resync LRU-cache are 128MB each.
  * The caller of this function has to hold an get_ldev() reference.
@@ -1081,7 +1072,6 @@
 		drbd_peer_device_post_work(peer_device, RS_LAZY_BM_WRITE);
 }
 
-
 static int update_sync_bits(struct drbd_peer_device *peer_device,
 		ULONG_PTR sbnr, ULONG_PTR ebnr,
 		enum update_sync_bits_mode mode)
@@ -1427,6 +1417,14 @@
 	return 0;
 }
 
! remove (DRBD 9.1 supports stepaside: TODO ask Phil if this is true
+void resync_stalled_timer_fn(struct timer_list *t)
+{
+        struct drbd_peer_device *peer_device = from_timer(peer_device, t, resync_stalled_timer, struct drbd_peer_device);
+
+	windrbd_suspend_application_io(peer_device->device->this_bdev,
+		"Sync stalled, suspending application I/O.\n");
+}
+
 /**
  * drbd_try_rs_begin_io() - Gets an extent in the resync LRU cache, does not sleep
  *
@@ -1539,6 +1537,17 @@
 	set_bit(BME_LOCKED, &bm_ext->flags);
 proceed:
 	peer_device->resync_wenr = LC_FREE;
! remove (DRBD 9.1 supports stepaside: TODO ask Phil if this is true
+
+	if (peer_device->rs_last_bm_extent == NULL)	/* first extent */
+		peer_device->rs_last_bm_extent = bm_ext;
+
+	if (peer_device->rs_last_bm_extent != bm_ext) {
+		del_timer(&peer_device->resync_stalled_timer);
+
+		windrbd_resume_application_io(peer_device->device->this_bdev,
+			"Sync progress, resuming application I/O.\n");
+	}
+
 	spin_unlock_irqrestore(&device->al_lock, spin_lock_irq_flags);
 	return 0;
 
@@ -1559,6 +1568,18 @@
 		} else
 			peer_device->resync_wenr = enr;
 	}
! remove (DRBD 9.1 supports stepaside: TODO ask Phil if this is true
+	/* If suspended do nothing. Wait until sync finishes,
+	 * makes progress or is aborted.
+	 */
+	if (!windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		if (!timer_pending(&peer_device->resync_stalled_timer) &&
+		   (peer_device->repl_state[NOW] == L_SYNC_SOURCE ||
+		    peer_device->repl_state[NOW] == L_SYNC_TARGET))
+			mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+
+		peer_device->rs_last_bm_extent = bm_ext;
+	}
+
 	spin_unlock_irqrestore(&device->al_lock, spin_lock_irq_flags);
 	return -EAGAIN;
 }
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_actlog.c.rej
diff -r -u converted-sources/drbd/drbd_actlog.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c.tmp.bak
--- converted-sources/drbd/drbd_actlog.c.tmp.bak	2024-09-03 16:23:35.366989124 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c.tmp.bak	2024-08-05 17:28:59.705538873 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 2003-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2003-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/slab.h>
@@ -63,12 +62,7 @@
 {
 	LONG_PTR dt;
 
-	rcu_read_lock();
-	dt = rcu_dereference(bdev->disk_conf)->disk_timeout;
-	rcu_read_unlock();
-	dt = dt * HZ / 10;
-	if (dt == 0)
-		dt = MAX_SCHEDULE_TIMEOUT;
+	dt = MAX_SCHEDULE_TIMEOUT;
 
 	dt = wait_event_timeout(device->misc_wait,
 			*done || test_bit(FORCE_DETACH, &device->flags), dt);
@@ -519,7 +513,6 @@
 {
 	bool locked = false;
 
-
 	if (drbd_md_dax_active(device->ldev)) {
 		drbd_dax_al_begin_io_commit(device);
 		return;
@@ -899,7 +892,6 @@
 	[SET_OUT_OF_SYNC] = "drbd_set_out_of_sync"
 };
 
-
 /* ATTENTION. The AL's extents are 4MB each, while the extents in the
  * resync LRU-cache are 128MB each.
  * The caller of this function has to hold an get_ldev() reference.
@@ -1064,7 +1056,6 @@
 		drbd_peer_device_post_work(peer_device, RS_LAZY_BM_WRITE);
 }
 
-
 static int update_sync_bits(struct drbd_peer_device *peer_device,
 		ULONG_PTR sbnr, ULONG_PTR ebnr,
 		enum update_sync_bits_mode mode)
@@ -1405,6 +1396,14 @@
 	return 0;
 }
 
+void resync_stalled_timer_fn(struct timer_list *t)
+{
+        struct drbd_peer_device *peer_device = from_timer(peer_device, t, resync_stalled_timer, struct drbd_peer_device);
+
+	windrbd_suspend_application_io(peer_device->device->this_bdev,
+		"Sync stalled, suspending application I/O.\n");
+}
+
 /**
  * drbd_try_rs_begin_io() - Gets an extent in the resync LRU cache, does not sleep
  *
@@ -1516,6 +1515,17 @@
 	set_bit(BME_LOCKED, &bm_ext->flags);
 proceed:
 	peer_device->resync_wenr = LC_FREE;
+
+	if (peer_device->rs_last_bm_extent == NULL)	/* first extent */
+		peer_device->rs_last_bm_extent = bm_ext;
+
+	if (peer_device->rs_last_bm_extent != bm_ext) {
+		del_timer(&peer_device->resync_stalled_timer);
+
+		windrbd_resume_application_io(peer_device->device->this_bdev,
+			"Sync progress, resuming application I/O.\n");
+	}
+
 	spin_unlock_irq(&device->al_lock);
 	return 0;
 
@@ -1536,6 +1546,18 @@
 		} else
 			peer_device->resync_wenr = enr;
 	}
+	/* If suspended do nothing. Wait until sync finishes,
+	 * makes progress or is aborted.
+	 */
+	if (!windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		if (!timer_pending(&peer_device->resync_stalled_timer) &&
+		   (peer_device->repl_state[NOW] == L_SYNC_SOURCE ||
+		    peer_device->repl_state[NOW] == L_SYNC_TARGET))
+			mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+
+		peer_device->rs_last_bm_extent = bm_ext;
+	}
+
 	spin_unlock_irq(&device->al_lock);
 	return -EAGAIN;
 }
diff -r -u converted-sources/drbd/drbd_actlog.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c.tmp.orig
--- converted-sources/drbd/drbd_actlog.c.tmp.orig	2024-09-03 16:23:37.911013778 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_actlog.c.tmp.orig	2024-08-05 17:29:00.057542095 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 2003-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2003-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/slab.h>
@@ -60,15 +59,9 @@
 void wait_until_done_or_force_detached(struct drbd_device *device, struct drbd_backing_dev *bdev,
 				       unsigned int *done)
 {
-	KIRQL rcu_flags;
 	LONG_PTR dt;
 
-	rcu_flags = rcu_read_lock();
-	dt = rcu_dereference(bdev->disk_conf)->disk_timeout;
-	rcu_read_unlock(rcu_flags);
-	dt = dt * HZ / 10;
-	if (dt == 0)
-		dt = MAX_SCHEDULE_TIMEOUT;
+	dt = MAX_SCHEDULE_TIMEOUT;
 
 	wait_event_timeout(dt, device->misc_wait,
 			   *done || test_bit(FORCE_DETACH, &device->flags),
@@ -339,7 +332,7 @@
 # error FIXME
 #endif
 
-static ULONG_PTR al_extent_to_bm_bit(unsigned int al_enr)
+static unsigned long al_extent_to_bm_bit(unsigned int al_enr)
 {
 	return (ULONG_PTR)al_enr << (AL_EXTENT_SHIFT - BM_BLOCK_SHIFT);
 }
@@ -529,7 +522,6 @@
 	KIRQL rcu_flags;
 	bool locked = false;
 
-
 	if (drbd_md_dax_active(device->ldev)) {
 		drbd_dax_al_begin_io_commit(device);
 		return;
@@ -913,7 +905,6 @@
 	[SET_OUT_OF_SYNC] = "drbd_set_out_of_sync"
 };
 
-
 /* ATTENTION. The AL's extents are 4MB each, while the extents in the
  * resync LRU-cache are 128MB each.
  * The caller of this function has to hold an get_ldev() reference.
@@ -1078,7 +1069,6 @@
 		drbd_peer_device_post_work(peer_device, RS_LAZY_BM_WRITE);
 }
 
-
 static int update_sync_bits(struct drbd_peer_device *peer_device,
 		ULONG_PTR sbnr, ULONG_PTR ebnr,
 		enum update_sync_bits_mode mode)
@@ -1424,6 +1414,14 @@
 	return 0;
 }
 
+void resync_stalled_timer_fn(struct timer_list *t)
+{
+        struct drbd_peer_device *peer_device = from_timer(peer_device, t, resync_stalled_timer, struct drbd_peer_device);
+
+	windrbd_suspend_application_io(peer_device->device->this_bdev,
+		"Sync stalled, suspending application I/O.\n");
+}
+
 /**
  * drbd_try_rs_begin_io() - Gets an extent in the resync LRU cache, does not sleep
  *
@@ -1536,6 +1534,17 @@
 	set_bit(BME_LOCKED, &bm_ext->flags);
 proceed:
 	peer_device->resync_wenr = LC_FREE;
+
+	if (peer_device->rs_last_bm_extent == NULL)	/* first extent */
+		peer_device->rs_last_bm_extent = bm_ext;
+
+	if (peer_device->rs_last_bm_extent != bm_ext) {
+		del_timer(&peer_device->resync_stalled_timer);
+
+		windrbd_resume_application_io(peer_device->device->this_bdev,
+			"Sync progress, resuming application I/O.\n");
+	}
+
 	spin_unlock_irqrestore(&device->al_lock, spin_lock_irq_flags);
 	return 0;
 
@@ -1556,6 +1565,18 @@
 		} else
 			peer_device->resync_wenr = enr;
 	}
+	/* If suspended do nothing. Wait until sync finishes,
+	 * makes progress or is aborted.
+	 */
+	if (!windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		if (!timer_pending(&peer_device->resync_stalled_timer) &&
+		   (peer_device->repl_state[NOW] == L_SYNC_SOURCE ||
+		    peer_device->repl_state[NOW] == L_SYNC_TARGET))
+			mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+
+		peer_device->rs_last_bm_extent = bm_ext;
+	}
+
 	spin_unlock_irqrestore(&device->al_lock, spin_lock_irq_flags);
 	return -EAGAIN;
 }
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_actlog.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_actlog.pdb
diff -r -u converted-sources/drbd/drbd_bitmap.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c
--- converted-sources/drbd/drbd_bitmap.c	2024-09-03 16:24:40.803623121 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c	2024-08-05 17:29:29.369810511 +0000
@@ -44,7 +44,6 @@
  * function name      bm_... => internal to implementation, "private".
  */
 
-
 /*
  * LIMITATIONS:
  * We want to support >= peta byte of backend storage, while for now still using
@@ -327,7 +326,6 @@
  * to be able to report device specific.
  */
 
-
 static void bm_free_pages(struct page **pages, ULONG_PTR number)
 {
 	ULONG_PTR i;
@@ -1071,7 +1069,6 @@
 	bm_op(peer_device->device, peer_device->bitmap_index, start, end, BM_OP_EXTRACT, (__le32 *)buffer);
 }
 
-
 static void drbd_bm_aio_ctx_destroy(struct kref *kref)
 {
 	struct drbd_bm_aio_ctx *ctx = container_of(kref, struct drbd_bm_aio_ctx, kref);
@@ -1308,13 +1305,6 @@
 
 	/* let the layers below us try_ to merge these bios... */
 
! remove (WinDRBD should get a daemon for disk scheduling) plus this causes trouble somewhere in the field
-	/* This will cause WinDRBD to wait until windrbd_bdev_uncork()
-	 * is called which then will try to merge all I/O requests
-	 * into larger ones.
-	 */
-
-	windrbd_bdev_cork(device->ldev->md_bdev);
-
 	if (flags & BM_AIO_READ) {
 		for (i = start_page; i <= end_page; i++) {
 			atomic_inc(&ctx->in_flight);
@@ -1362,7 +1352,6 @@
 			cond_resched();
 		}
 	}
! remove (WinDRBD should get a daemon for disk scheduling) plus this causes trouble somewhere in the field
-	windrbd_bdev_uncork(device->ldev->md_bdev);
 
 	/*
 	 * We initialize ctx->in_flight to one to make sure drbd_bm_endio
@@ -1456,7 +1445,6 @@
 		push_al_bitmap_hint(device, page_nr);
 }
 
-
 /**
  * drbd_bm_write() - Write the whole bitmap to its on disk location.
  * @device:	DRBD device.
diff -r -u converted-sources/drbd/drbd_bitmap.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c.tmp.bak
--- converted-sources/drbd/drbd_bitmap.c.tmp.bak	2024-09-03 16:24:40.287618122 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c.tmp.bak	2024-08-05 17:29:28.841805673 +0000
@@ -41,7 +41,6 @@
  * function name      bm_... => internal to implementation, "private".
  */
 
-
 /*
  * LIMITATIONS:
  * We want to support >= peta byte of backend storage, while for now still using
@@ -324,7 +323,6 @@
  * to be able to report device specific.
  */
 
-
 static void bm_free_pages(struct page **pages, ULONG_PTR number)
 {
 	ULONG_PTR i;
@@ -1067,7 +1065,6 @@
 	bm_op(peer_device->device, peer_device->bitmap_index, start, end, BM_OP_EXTRACT, (__le32 *)buffer);
 }
 
-
 static void drbd_bm_aio_ctx_destroy(struct kref *kref)
 {
 	struct drbd_bm_aio_ctx *ctx = container_of(kref, struct drbd_bm_aio_ctx, kref);
@@ -1435,7 +1432,6 @@
 		push_al_bitmap_hint(device, page_nr);
 }
 
-
 /**
  * drbd_bm_write() - Write the whole bitmap to its on disk location.
  * @device:	DRBD device.
diff -r -u converted-sources/drbd/drbd_bitmap.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c.tmp.orig
--- converted-sources/drbd/drbd_bitmap.c.tmp.orig	2024-09-03 16:24:40.747622579 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_bitmap.c.tmp.orig	2024-08-05 17:29:29.293809814 +0000
@@ -12,9 +12,6 @@
 
 #define pr_fmt(fmt) ":" fmt
 
- /* Enable all warnings throws lots of those warnings: */
-#pragma warning(disable: 4061 4062 4255 4388 4668 4820 5032 4711 5045)
-
 #include <linux/bitops.h>
 #include <linux/vmalloc.h>
 #include <linux/string.h>
@@ -44,7 +41,6 @@
  * function name      bm_... => internal to implementation, "private".
  */
 
-
 /*
  * LIMITATIONS:
  * We want to support >= peta byte of backend storage, while for now still using
@@ -247,7 +243,7 @@
 	set_page_private(page, idx);
 }
 
-static ULONG_PTR bm_page_to_idx(struct page *page)
+static unsigned long bm_page_to_idx(struct page *page)
 {
 	return page_private(page) & BM_PAGE_IDX_MASK;
 }
@@ -327,7 +323,6 @@
  * to be able to report device specific.
  */
 
-
 static void bm_free_pages(struct page **pages, ULONG_PTR number)
 {
 	ULONG_PTR i;
@@ -352,7 +347,7 @@
 {
 	struct page **old_pages = b->bm_pages;
 	struct page **new_pages, *page;
-	ULONG_PTR i, bytes;
+	unsigned int i, bytes;
 	ULONG_PTR have = b->bm_number_of_pages;
 
 	BUG_ON(have == 0 && old_pages != NULL);
@@ -434,14 +429,14 @@
 	kfree(bitmap);
 }
 
-static inline ULONG_PTR interleaved_word32(struct drbd_bitmap *bitmap,
+static inline unsigned long interleaved_word32(struct drbd_bitmap *bitmap,
 					       unsigned int bitmap_index,
 					       ULONG_PTR bit)
 {
 	return (bit >> 5) * bitmap->bm_max_peers + bitmap_index;
 }
 
-static inline ULONG_PTR word32_to_page(ULONG_PTR word)
+static inline unsigned long word32_to_page(ULONG_PTR word)
 {
 	return word >> (PAGE_SHIFT - 2);
 }
@@ -451,7 +446,7 @@
 	return word & ((1 << (PAGE_SHIFT - 2)) - 1);
 }
 
-static inline ULONG_PTR last_bit_on_page(struct drbd_bitmap *bitmap,
+static inline unsigned long last_bit_on_page(struct drbd_bitmap *bitmap,
 					     unsigned int bitmap_index,
 					     ULONG_PTR bit)
 {
@@ -460,7 +455,7 @@
 	return (bit | 31) + ((word32_in_page(-(word + 1)) / bitmap->bm_max_peers) << 5);
 }
 
-static inline ULONG_PTR bit_to_page_interleaved(struct drbd_bitmap *bitmap,
+static inline unsigned long bit_to_page_interleaved(struct drbd_bitmap *bitmap,
 						    unsigned int bitmap_index,
 						    ULONG_PTR bit)
 {
@@ -767,7 +762,7 @@
 
 #ifdef BITMAP_DEBUG
 #define bm_op(device, bitmap_index, start, end, op, buffer) \
-	{ ULONG_PTR ret; \
+	{ unsigned long ret; \
 	   drbd_info(device, "%s: bm_op(..., %u, %lu, %lu, %u, %p)\n", \
 		     __func__, bitmap_index, start, end, op, buffer); \
 	   ret = bm_op(device, bitmap_index, start, end, op, buffer); \
@@ -775,7 +770,7 @@
 	   ret; }
 
 #define __bm_op(device, bitmap_index, start, end, op, buffer) \
-	{ ULONG_PTR ret; \
+	{ unsigned long ret; \
 	   drbd_info(device, "%s: __bm_op(..., %u, %lu, %lu, %u, %p)\n", \
 		     __func__, bitmap_index, start, end, op, buffer); \
 	   ret = __bm_op(device, bitmap_index, start, end, op, buffer); \
@@ -785,7 +780,7 @@
 
 #ifdef BITMAP_DEBUG
 #define ___bm_op(device, bitmap_index, start, end, op, buffer) \
-	{ ULONG_PTR ret; \
+	{ unsigned long ret; \
 	   drbd_info(device, "%s: ___bm_op(..., %u, %lu, %lu, %u, %p)\n", \
 		     __func__, bitmap_index, start, end, op, buffer); \
 	   ret = ____bm_op(device, bitmap_index, start, end, op, buffer); \
@@ -1071,7 +1066,6 @@
 	bm_op(peer_device->device, peer_device->bitmap_index, start, end, BM_OP_EXTRACT, (__le32 *)buffer);
 }
 
-
 static void drbd_bm_aio_ctx_destroy(struct kref *kref)
 {
 	struct drbd_bm_aio_ctx *ctx = container_of(kref, struct drbd_bm_aio_ctx, kref);
@@ -1122,7 +1116,7 @@
 	bio_put(bio);
 
! review (WinDRBD sometimes keeps pages longer)
 	if (page)
-		put_page(page);
+		mempool_free(page, &drbd_md_io_page_pool);
 
 	if (atomic_dec_and_test(&ctx->in_flight)) {
 		ctx->done = 1;
@@ -1191,6 +1185,10 @@
 	bm_set_page_unchanged(b->bm_pages[page_nr]);
 
 	if (ctx->flags & BM_AIO_COPY_PAGES) {
+/*
+		page = mempool_alloc(&drbd_md_io_page_pool,
+				GFP_NOIO | __GFP_HIGHMEM);
+*/
 		page = alloc_page(GFP_NOIO | __GFP_HIGHMEM);
 		expect(device, page);
 
@@ -1448,7 +1446,6 @@
 		push_al_bitmap_hint(device, page_nr);
 }
 
-
 /**
  * drbd_bm_write() - Write the whole bitmap to its on disk location.
  * @device:	DRBD device.
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_bitmap.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_bitmap.pdb
diff -r -u converted-sources/drbd/drbd_buildtag.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_buildtag.c
--- converted-sources/drbd/drbd_buildtag.c	2024-09-03 16:24:43.983653924 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_buildtag.c	2024-08-06 15:00:09.879760129 +0000
@@ -1 +1 @@
-const char *drbd_buildtag(void){return "windrbd-1.1.8-81-g8882d292";}
+const char *drbd_buildtag(void){return "windrbd-1.1.19";}
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_buildtag.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_buildtag.pdb
diff -r -u converted-sources/drbd/drbd_dax_pmem.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_dax_pmem.c
--- converted-sources/drbd/drbd_dax_pmem.c	2024-09-03 16:23:39.091025213 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_dax_pmem.c	2024-08-05 17:29:01.633556518 +0000
@@ -6,7 +6,6 @@
 
    Copyright (C) 2017, LINBIT HA-Solutions GmbH.
 
-
  */
 
 /*
@@ -121,7 +120,6 @@
 	arch_wb_cache_pmem(slot, sizeof(*slot));
 }
 
-
 void drbd_dax_al_begin_io_commit(struct drbd_device *device)
 {
 	KIRQL spin_lock_irq_flags;
diff -r -u converted-sources/drbd/drbd_dax_pmem.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_dax_pmem.c.tmp.bak
--- converted-sources/drbd/drbd_dax_pmem.c.tmp.bak	2024-09-03 16:23:38.847022849 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_dax_pmem.c.tmp.bak	2024-08-05 17:29:01.313553590 +0000
@@ -6,7 +6,6 @@
 
    Copyright (C) 2017, LINBIT HA-Solutions GmbH.
 
-
  */
 
 /*
@@ -121,7 +120,6 @@
 	arch_wb_cache_pmem(slot, sizeof(*slot));
 }
 
-
 void drbd_dax_al_begin_io_commit(struct drbd_device *device)
 {
 	struct lc_element *e;
diff -r -u converted-sources/drbd/drbd_debugfs.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c
--- converted-sources/drbd/drbd_debugfs.c	2024-09-03 16:24:40.043615759 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c	2024-08-05 17:29:28.585803327 +0000
@@ -978,7 +978,6 @@
 	}
 }
 
-
 static int device_act_log_histogram_show(struct seq_file *m, void *ignored)
 {
 	struct drbd_device *device = m->private;
diff -r -u converted-sources/drbd/drbd_debugfs.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c.tmp.bak
--- converted-sources/drbd/drbd_debugfs.c.tmp.bak	2024-09-03 16:24:39.595611420 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c.tmp.bak	2024-08-05 17:29:28.025798197 +0000
@@ -953,7 +953,6 @@
 	}
 }
 
-
 static int device_act_log_histogram_show(struct seq_file *m, void *ignored)
 {
 	struct drbd_device *device = m->private;
diff -r -u converted-sources/drbd/drbd_debugfs.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c.tmp.orig
--- converted-sources/drbd/drbd_debugfs.c.tmp.orig	2024-09-03 16:24:39.847613861 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_debugfs.c.tmp.orig	2024-08-05 17:29:28.345801129 +0000
@@ -977,7 +977,6 @@
 	}
 }
 
-
 static int device_act_log_histogram_show(struct seq_file *m, void *ignored)
 {
 	struct drbd_device *device = m->private;
diff -r -u converted-sources/drbd/drbd_int.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h
--- converted-sources/drbd/drbd_int.h	2024-09-03 16:23:53.551165335 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h	2024-08-05 17:29:08.229616896 +0000
@@ -8,7 +8,6 @@
   Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
   Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
 */
 
 /* Enable all warnings throws lots of those warnings: */
@@ -341,7 +340,6 @@
 	 *      how long did it take the lower level device to complete this request
 	 */
 
-
 	/* once it hits 0, we may complete the master_bio */
 	atomic_t completion_ref;
 	/* once it hits 0, we may destroy this drbd_request object */
@@ -757,6 +755,7 @@
 	CONN_HANDSHAKE_DISCONNECT,
 	CONN_HANDSHAKE_RETRY,
 	CONN_HANDSHAKE_READY,
! upstream (do not start fencing twice)
+	FENCING_HANDLER_RUNNING,
 };
 
 /* flag bits per resource */
@@ -834,7 +833,6 @@
 	int additional_size;  /* additional space to be added to next packet's size */
 };
 
-
 struct drbd_resource {
 	KIRQL wrcu_flags;
 	char *name;
@@ -1107,6 +1105,9 @@
 	struct list_head twopc_parent_list;
 	struct rcu_head rcu;
 
! upstream (do not start fencing twice)
+	wait_queue_head_t fencing_handler_wait;
+	int retval_from_fencing_khelper;
+
 	struct drbd_transport transport; /* The transport needs to be the last member. The acutal
 					    implementation might have more members than the
 					    abstract one. */
@@ -1177,6 +1178,10 @@
 	ULONG_PTR rs_failed;
 	/* Syncer's start time [unit jiffies] */
 	ULONG_PTR rs_start;
! remove (DRBD 9.1 supports stepaside: TODO ask Phil if this is true)
+	/* Bitmap extent that failed to be locked for 5 seconds. */
+	struct bm_extent *rs_last_bm_extent;
+	/* Timer for sync stall */
+	struct timer_list resync_stalled_timer;
 	/* cumulated time in PausedSyncX state [unit jiffies] */
 	ULONG_PTR rs_paused;
 	/* skipped because csum was equal [unit BM_BLOCK_SIZE] */
@@ -1433,7 +1438,6 @@
 	return (struct drbd_device *)idr_find(&drbd_devices, minor);
 }
 
-
 static inline struct drbd_peer_device *
 conn_peer_device(struct drbd_connection *connection, int volume_number)
 {
@@ -1699,7 +1703,6 @@
 
 #define BM_BLOCKS_PER_BM_EXT_MASK  (BM_BITS_PER_EXT - 1)
 
-
 /* in one sector of the bitmap, we have this many activity_log extents. */
 #define AL_EXT_PER_BM_SECT  (1 << (BM_EXT_SHIFT - AL_EXTENT_SHIFT))
 
@@ -2102,6 +2105,7 @@
 extern void drbd_al_shrink(struct drbd_device *device);
 extern bool drbd_sector_has_priority(struct drbd_peer_device *, sector_t);
 extern int drbd_al_initialize(struct drbd_device *, void *);
! remove (DRBD 9.1 supports stepaside: TODO ask Phil if this is true)
+extern void resync_stalled_timer_fn(struct timer_list *t);
 
 /* drbd_nl.c */
 
diff -r -u converted-sources/drbd/drbd_int.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h.tmp.bak
--- converted-sources/drbd/drbd_int.h.tmp.bak	2024-09-03 16:23:52.467154831 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h.tmp.bak	2024-08-05 17:29:07.621611330 +0000
@@ -8,7 +8,6 @@
   Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
   Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
 */
 
 #ifndef _DRBD_INT_H
@@ -339,7 +338,6 @@
 	 *      how long did it take the lower level device to complete this request
 	 */
 
-
 	/* once it hits 0, we may complete the master_bio */
 	atomic_t completion_ref;
 	/* once it hits 0, we may destroy this drbd_request object */
@@ -755,6 +753,7 @@
 	CONN_HANDSHAKE_DISCONNECT,
 	CONN_HANDSHAKE_RETRY,
 	CONN_HANDSHAKE_READY,
+	FENCING_HANDLER_RUNNING,
 };
 
 /* flag bits per resource */
@@ -832,7 +831,6 @@
 	int additional_size;  /* additional space to be added to next packet's size */
 };
 
-
 struct drbd_resource {
 	KIRQL wrcu_flags;
 	char *name;
@@ -1105,6 +1103,9 @@
 	struct list_head twopc_parent_list;
 	struct rcu_head rcu;
 
+	wait_queue_head_t fencing_handler_wait;
+	int retval_from_fencing_khelper;
+
 	struct drbd_transport transport; /* The transport needs to be the last member. The acutal
 					    implementation might have more members than the
 					    abstract one. */
@@ -1175,6 +1176,10 @@
 	ULONG_PTR rs_failed;
 	/* Syncer's start time [unit jiffies] */
 	ULONG_PTR rs_start;
+	/* Bitmap extent that failed to be locked for 5 seconds. */
+	struct bm_extent *rs_last_bm_extent;
+	/* Timer for sync stall */
+	struct timer_list resync_stalled_timer;
 	/* cumulated time in PausedSyncX state [unit jiffies] */
 	ULONG_PTR rs_paused;
 	/* skipped because csum was equal [unit BM_BLOCK_SIZE] */
@@ -1429,7 +1434,6 @@
 	return (struct drbd_device *)idr_find(&drbd_devices, minor);
 }
 
-
 static inline struct drbd_peer_device *
 conn_peer_device(struct drbd_connection *connection, int volume_number)
 {
@@ -1695,7 +1699,6 @@
 
 #define BM_BLOCKS_PER_BM_EXT_MASK  (BM_BITS_PER_EXT - 1)
 
-
 /* in one sector of the bitmap, we have this many activity_log extents. */
 #define AL_EXT_PER_BM_SECT  (1 << (BM_EXT_SHIFT - AL_EXTENT_SHIFT))
 
@@ -2099,6 +2102,7 @@
 extern void drbd_al_shrink(struct drbd_device *device);
 extern bool drbd_sector_has_priority(struct drbd_peer_device *, sector_t);
 extern int drbd_al_initialize(struct drbd_device *, void *);
+extern void resync_stalled_timer_fn(struct timer_list *t);
 
 /* drbd_nl.c */
 
diff -r -u converted-sources/drbd/drbd_int.h.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h.tmp.orig
--- converted-sources/drbd/drbd_int.h.tmp.orig	2024-09-03 16:23:53.375163630 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_int.h.tmp.orig	2024-08-05 17:29:08.169616347 +0000
@@ -8,9 +8,11 @@
   Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
   Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
 */
 
+/* Enable all warnings throws lots of those warnings: */
+#pragma warning(disable: 4061 4062 4255 4388 4668 4820 5032 4711 5045)
+
 #ifndef _DRBD_INT_H
 #define _DRBD_INT_H
 
@@ -338,7 +340,6 @@
 	 *      how long did it take the lower level device to complete this request
 	 */
 
-
 	/* once it hits 0, we may complete the master_bio */
 	atomic_t completion_ref;
 	/* once it hits 0, we may destroy this drbd_request object */
@@ -754,6 +755,7 @@
 	CONN_HANDSHAKE_DISCONNECT,
 	CONN_HANDSHAKE_RETRY,
 	CONN_HANDSHAKE_READY,
+	FENCING_HANDLER_RUNNING,
 };
 
 /* flag bits per resource */
@@ -831,7 +833,6 @@
 	int additional_size;  /* additional space to be added to next packet's size */
 };
 
-
 struct drbd_resource {
 	KIRQL wrcu_flags;
 	char *name;
@@ -1104,6 +1105,9 @@
 	struct list_head twopc_parent_list;
 	struct rcu_head rcu;
 
+	wait_queue_head_t fencing_handler_wait;
+	int retval_from_fencing_khelper;
+
 	struct drbd_transport transport; /* The transport needs to be the last member. The acutal
 					    implementation might have more members than the
 					    abstract one. */
@@ -1174,6 +1178,10 @@
 	ULONG_PTR rs_failed;
 	/* Syncer's start time [unit jiffies] */
 	ULONG_PTR rs_start;
+	/* Bitmap extent that failed to be locked for 5 seconds. */
+	struct bm_extent *rs_last_bm_extent;
+	/* Timer for sync stall */
+	struct timer_list resync_stalled_timer;
 	/* cumulated time in PausedSyncX state [unit jiffies] */
 	ULONG_PTR rs_paused;
 	/* skipped because csum was equal [unit BM_BLOCK_SIZE] */
@@ -1430,7 +1438,6 @@
 	return (struct drbd_device *)idr_find(&drbd_devices, minor);
 }
 
-
 static inline struct drbd_peer_device *
 conn_peer_device(struct drbd_connection *connection, int volume_number)
 {
@@ -1696,7 +1703,6 @@
 
 #define BM_BLOCKS_PER_BM_EXT_MASK  (BM_BITS_PER_EXT - 1)
 
-
 /* in one sector of the bitmap, we have this many activity_log extents. */
 #define AL_EXT_PER_BM_SECT  (1 << (BM_EXT_SHIFT - AL_EXTENT_SHIFT))
 
@@ -2099,6 +2105,7 @@
 extern void drbd_al_shrink(struct drbd_device *device);
 extern bool drbd_sector_has_priority(struct drbd_peer_device *, sector_t);
 extern int drbd_al_initialize(struct drbd_device *, void *);
+extern void resync_stalled_timer_fn(struct timer_list *t);
 
 /* drbd_nl.c */
 
@@ -2309,7 +2316,7 @@
 		return (drbd_get_capacity(bdev->backing_bdev) & ~7ULL) - 8;
 
 	/* external, some index; this is the old fixed size layout */
! manual: we need that else problems with large disks
-	return (128 << 20 >> 9) * bdev->md.meta_dev_idx;
+	return (128ULL << 20 >> 9) * bdev->md.meta_dev_idx;
 }
 
 void drbd_queue_work(struct drbd_work_queue *, struct drbd_work *);
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_interval.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_interval.pdb
diff -r -u converted-sources/drbd/drbd_kref_debug.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_kref_debug.c
--- converted-sources/drbd/drbd_kref_debug.c	2024-09-03 16:23:54.407173630 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_kref_debug.c	2024-08-05 17:29:09.197625758 +0000
@@ -1,7 +1,6 @@
 #include <drbd_kref_debug.h>
 #include "drbd_int.h"
 
-
 static void get_resource_name(const struct kref_debug_info *debug_info, char *name)
 {
 	struct drbd_resource *resource = container_of(debug_info, struct drbd_resource, kref_debug);
@@ -73,8 +72,6 @@
 	}
 };
 
-
-
 struct kref_debug_class kref_class_device = {
 	"device",
 	get_device_name,
@@ -90,4 +87,3 @@
 	}
 };
 
-
diff -r -u converted-sources/drbd/drbd_kref_debug.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_kref_debug.c.tmp.bak
--- converted-sources/drbd/drbd_kref_debug.c.tmp.bak	2024-09-03 16:23:54.395173514 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_kref_debug.c.tmp.bak	2024-08-05 17:29:09.185625648 +0000
@@ -1,7 +1,6 @@
 #include <drbd_kref_debug.h>
 #include "drbd_int.h"
 
-
 static void get_resource_name(const struct kref_debug_info *debug_info, char *name)
 {
 	struct drbd_resource *resource = container_of(debug_info, struct drbd_resource, kref_debug);
@@ -72,8 +71,6 @@
 	}
 };
 
-
-
 struct kref_debug_class kref_class_device = {
 	"device",
 	get_device_name,
@@ -89,4 +86,3 @@
 	}
 };
 
-
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_kref_debug.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_kref_debug.pdb
diff -r -u converted-sources/drbd/drbd_main.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c
--- converted-sources/drbd/drbd_main.c	2024-09-03 16:23:49.423125335 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c	2024-08-05 17:29:04.373581598 +0000
@@ -10,7 +10,6 @@
    Thanks to Carter Burden, Bart Grantham and Gennadiy Nerubayev
    from Logicworks, Inc. for making SDP replication support possible.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -131,7 +130,6 @@
 unsigned int drbd_protocol_version_min = PRO_VERSION_MIN;
 module_param_named(protocol_version_min, drbd_protocol_version_min, drbd_protocol_version, 0644);
 
-
 /* in 2.6.x, our device mapping and config info contains our virtual gendisks
  * as member "struct gendisk *vdisk;"
  */
@@ -231,7 +229,6 @@
 	return connection;
 }
 
-
 struct drbd_peer_device *__drbd_next_peer_device_ref(u64 *visited,
 						     struct drbd_peer_device *peer_device,
 						     struct drbd_device *device)
@@ -514,7 +511,6 @@
 	change_cstate(connection, C_PROTOCOL_ERROR, CS_HARD);
 }
 
-
 /**
  * _tl_walk() - Walks the transfer log, and applies an action to all requests
  * @connection:	DRBD connection to operate on.
@@ -711,7 +707,6 @@
 	return true;
 }
 
-
 void _drbd_thread_stop(struct drbd_thread *thi, int restart, int wait)
 {
 	KIRQL flags;
@@ -1112,7 +1107,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	mutex_lock(&connection->mutex[stream]);
 	flush_send_buffer(connection, stream);
 
@@ -2896,7 +2890,6 @@
 	drbd_set_defaults(device);
 }
 
-
 static void drbd_destroy_mempools(void)
 {
 	bioset_exit(&drbd_io_bio_set);
@@ -3115,7 +3108,6 @@
 		bool expected;
 		ktime_get_accounting_assign(ktime_t start_kt, req->start_kt);
 
-
 		expected =
 			expect(device, atomic_read(&req->completion_ref) == 0) &&
 			expect(device, req->local_rq_state & RQ_POSTPONED) &&
@@ -3168,7 +3160,6 @@
 	queue_work(retry.wq, &retry.worker);
 }
 
-
 void drbd_cleanup(void)
 {
 	/* first remove proc,
@@ -3587,6 +3578,8 @@
 	connection->resource = resource;
 	connection->after_reconciliation.lost_node_id = -1;
 
! upstream fencing
+	init_waitqueue_head(&connection->fencing_handler_wait);
+
 	INIT_LIST_HEAD(&connection->transport.paths);
 	connection->transport.log_prefix = resource->name;
 	if (tc->init(&connection->transport))
@@ -3674,6 +3667,7 @@
 	}
 
 	timer_setup(&peer_device->start_resync_timer, start_resync_timer_fn, 0);
! remove stepaside
+	timer_setup(&peer_device->resync_stalled_timer, resync_stalled_timer_fn, 0);
 
 	INIT_LIST_HEAD(&peer_device->resync_work.list);
 	peer_device->resync_work.cb  = w_resync_timer;
@@ -3786,6 +3780,12 @@
 	timer_setup(&device->request_timer, request_timer_fn, 0);
 
 	init_waitqueue_head(&device->misc_wait);
! review what is this??
+	init_waitqueue_head(&device->al_wait);
+	init_waitqueue_head(&device->seq_wait);
+
+	init_rwsem(&device->uuid_sem);
+
+	disk = blk_alloc_disk(NUMA_NO_NODE);
         device->this_bdev = block_device;
 	if (block_device->bd_disk != NULL) {
 		printk("Warning: block_device already has a disk, freeing it.\n");
@@ -3795,12 +3795,6 @@
 	}
         device->this_bdev->bd_disk = disk;
 
! review what is this??
-	init_waitqueue_head(&device->al_wait);
-	init_waitqueue_head(&device->seq_wait);
-
-	init_rwsem(&device->uuid_sem);
-
-	disk = blk_alloc_disk(NUMA_NO_NODE);
 	if (!disk)
 		goto out_no_disk;
 
@@ -4052,6 +4046,13 @@
 	list_del_rcu(&connection->connections);
 	spin_unlock_irqrestore(&resource->req_lock, spin_lock_irq_flags);
 
! upstream fencing
+		/* something invalid as process exit code */
+	connection->retval_from_fencing_khelper = 42 << 8;
+	if (test_and_clear_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("Terminating threads waiting for fencing handler to exit (they will exit with 42)\n");
+		wake_up(&connection->fencing_handler_wait);
+	}
+
 	list_for_each_entry(struct drbd_peer_device, peer_device, &work_list, peer_devices)
 		drbd_debugfs_peer_device_cleanup(peer_device);
 	drbd_debugfs_connection_cleanup(connection);
@@ -4510,7 +4511,6 @@
 	return ~not_weak;
 }
 
-
 static bool __new_current_uuid_prepare(struct drbd_device *device, bool forced) 
 {
 	KIRQL spin_lock_irq_flags;
@@ -5491,8 +5491,7 @@
 		timeout = net_conf->ko_count ? net_conf->timeout * HZ / 10 * net_conf->ko_count :
 					       MAX_SCHEDULE_TIMEOUT;
 	} else {
! manual do disk timeout in DRBD (until it is fixed there) 
-		struct disk_conf *disk_conf = rcu_dereference(device->ldev->disk_conf);
-		timeout = disk_conf->disk_timeout * HZ / 10;
+		timeout = MAX_SCHEDULE_TIMEOUT;
 	}
 	rcu_read_unlock(rcu_flags);
 
@@ -5698,7 +5697,6 @@
 }
 #endif
 
-
 module_init(drbd_init)
 module_exit(drbd_cleanup)
 
diff -r -u converted-sources/drbd/drbd_main.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c.tmp.bak
--- converted-sources/drbd/drbd_main.c.tmp.bak	2024-09-03 16:23:40.827042037 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c.tmp.bak	2024-08-05 17:29:03.661575080 +0000
@@ -10,7 +10,6 @@
    Thanks to Carter Burden, Bart Grantham and Gennadiy Nerubayev
    from Logicworks, Inc. for making SDP replication support possible.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -128,7 +127,6 @@
 unsigned int drbd_protocol_version_min = PRO_VERSION_MIN;
 module_param_named(protocol_version_min, drbd_protocol_version_min, drbd_protocol_version, 0644);
 
-
 /* in 2.6.x, our device mapping and config info contains our virtual gendisks
  * as member "struct gendisk *vdisk;"
  */
@@ -225,7 +223,6 @@
 	return connection;
 }
 
-
 struct drbd_peer_device *__drbd_next_peer_device_ref(u64 *visited,
 						     struct drbd_peer_device *peer_device,
 						     struct drbd_device *device)
@@ -506,7 +503,6 @@
 	change_cstate(connection, C_PROTOCOL_ERROR, CS_HARD);
 }
 
-
 /**
  * _tl_walk() - Walks the transfer log, and applies an action to all requests
  * @connection:	DRBD connection to operate on.
@@ -708,7 +704,6 @@
 	return true;
 }
 
-
 void _drbd_thread_stop(struct drbd_thread *thi, int restart, int wait)
 {
 	ULONG_PTR flags;
@@ -1104,7 +1099,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	mutex_lock(&connection->mutex[stream]);
 	flush_send_buffer(connection, stream);
 
@@ -2856,7 +2850,6 @@
 	drbd_set_defaults(device);
 }
 
-
 static void drbd_destroy_mempools(void)
 {
 	bioset_exit(&drbd_io_bio_set);
@@ -3071,7 +3064,6 @@
 		bool expected;
 		ktime_get_accounting_assign(ktime_t start_kt, req->start_kt);
 
-
 		expected =
 			expect(device, atomic_read(&req->completion_ref) == 0) &&
 			expect(device, req->local_rq_state & RQ_POSTPONED) &&
@@ -3124,7 +3116,6 @@
 	queue_work(retry.wq, &retry.worker);
 }
 
-
 static void drbd_cleanup(void)
 {
 	/* first remove proc,
@@ -3539,6 +3530,8 @@
 	connection->resource = resource;
 	connection->after_reconciliation.lost_node_id = -1;
 
+	init_waitqueue_head(&connection->fencing_handler_wait);
+
 	INIT_LIST_HEAD(&connection->transport.paths);
 	connection->transport.log_prefix = resource->name;
 	if (tc->init(&connection->transport))
@@ -3626,6 +3619,7 @@
 	}
 
 	timer_setup(&peer_device->start_resync_timer, start_resync_timer_fn, 0);
+	timer_setup(&peer_device->resync_stalled_timer, resync_stalled_timer_fn, 0);
 
 	INIT_LIST_HEAD(&peer_device->resync_work.list);
 	peer_device->resync_work.cb  = w_resync_timer;
@@ -3962,6 +3956,13 @@
 	list_del_rcu(&connection->connections);
 	spin_unlock_irq(&resource->req_lock);
 
+		/* something invalid as process exit code */
+	connection->retval_from_fencing_khelper = 42 << 8;
+	if (test_and_clear_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("Terminating threads waiting for fencing handler to exit (they will exit with 42)\n");
+		wake_up(&connection->fencing_handler_wait);
+	}
+
 	list_for_each_entry(struct drbd_peer_device, peer_device, &work_list, peer_devices)
 		drbd_debugfs_peer_device_cleanup(peer_device);
 	drbd_debugfs_connection_cleanup(connection);
@@ -4422,7 +4423,6 @@
 	return ~not_weak;
 }
 
-
 static bool __new_current_uuid_prepare(struct drbd_device *device, bool forced) 
 {
 	u64 got_new_bitmap_uuid, val, old_current_uuid;
@@ -5383,8 +5383,7 @@
 		timeout = net_conf->ko_count ? net_conf->timeout * HZ / 10 * net_conf->ko_count :
 					       MAX_SCHEDULE_TIMEOUT;
 	} else {
-		struct disk_conf *disk_conf = rcu_dereference(device->ldev->disk_conf);
-		timeout = disk_conf->disk_timeout * HZ / 10;
+		timeout = MAX_SCHEDULE_TIMEOUT;
 	}
 	rcu_read_unlock();
 
@@ -5589,7 +5588,6 @@
 }
 #endif
 
-
 void drbd_cleanup_by_win_shutdown(PVOLUME_EXTENSION VolumeExtension)
 {
     int i;
diff -r -u converted-sources/drbd/drbd_main.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c.tmp.orig
--- converted-sources/drbd/drbd_main.c.tmp.orig	2024-09-03 16:23:49.379124909 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_main.c.tmp.orig	2024-08-05 17:29:04.325581158 +0000
@@ -10,7 +10,6 @@
    Thanks to Carter Burden, Bart Grantham and Gennadiy Nerubayev
    from Logicworks, Inc. for making SDP replication support possible.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -131,7 +130,6 @@
 unsigned int drbd_protocol_version_min = PRO_VERSION_MIN;
 module_param_named(protocol_version_min, drbd_protocol_version_min, drbd_protocol_version, 0644);
 
-
 /* in 2.6.x, our device mapping and config info contains our virtual gendisks
  * as member "struct gendisk *vdisk;"
  */
@@ -231,7 +229,6 @@
 	return connection;
 }
 
-
 struct drbd_peer_device *__drbd_next_peer_device_ref(u64 *visited,
 						     struct drbd_peer_device *peer_device,
 						     struct drbd_device *device)
@@ -514,7 +511,6 @@
 	change_cstate(connection, C_PROTOCOL_ERROR, CS_HARD);
 }
 
-
 /**
  * _tl_walk() - Walks the transfer log, and applies an action to all requests
  * @connection:	DRBD connection to operate on.
@@ -711,7 +707,6 @@
 	return true;
 }
 
-
 void _drbd_thread_stop(struct drbd_thread *thi, int restart, int wait)
 {
 	KIRQL flags;
@@ -1112,7 +1107,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	mutex_lock(&connection->mutex[stream]);
 	flush_send_buffer(connection, stream);
 
@@ -2896,7 +2890,6 @@
 	drbd_set_defaults(device);
 }
 
-
 static void drbd_destroy_mempools(void)
 {
 	bioset_exit(&drbd_io_bio_set);
@@ -3115,7 +3108,6 @@
 		bool expected;
 		ktime_get_accounting_assign(ktime_t start_kt, req->start_kt);
 
-
 		expected =
 			expect(device, atomic_read(&req->completion_ref) == 0) &&
 			expect(device, req->local_rq_state & RQ_POSTPONED) &&
@@ -3168,7 +3160,6 @@
 	queue_work(retry.wq, &retry.worker);
 }
 
-
 void drbd_cleanup(void)
 {
 	/* first remove proc,
@@ -3587,6 +3578,8 @@
 	connection->resource = resource;
 	connection->after_reconciliation.lost_node_id = -1;
 
+	init_waitqueue_head(&connection->fencing_handler_wait);
+
 	INIT_LIST_HEAD(&connection->transport.paths);
 	connection->transport.log_prefix = resource->name;
 	if (tc->init(&connection->transport))
@@ -3674,6 +3667,7 @@
 	}
 
 	timer_setup(&peer_device->start_resync_timer, start_resync_timer_fn, 0);
+	timer_setup(&peer_device->resync_stalled_timer, resync_stalled_timer_fn, 0);
 
 	INIT_LIST_HEAD(&peer_device->resync_work.list);
 	peer_device->resync_work.cb  = w_resync_timer;
@@ -3786,6 +3780,12 @@
 	timer_setup(&device->request_timer, request_timer_fn, 0);
 
 	init_waitqueue_head(&device->misc_wait);
+	init_waitqueue_head(&device->al_wait);
+	init_waitqueue_head(&device->seq_wait);
+
+	init_rwsem(&device->uuid_sem);
+
+	disk = blk_alloc_disk(NUMA_NO_NODE);
         device->this_bdev = block_device;
 	if (block_device->bd_disk != NULL) {
 		printk("Warning: block_device already has a disk, freeing it.\n");
@@ -3795,12 +3795,6 @@
 	}
         device->this_bdev->bd_disk = disk;
 
-	init_waitqueue_head(&device->al_wait);
-	init_waitqueue_head(&device->seq_wait);
-
-	init_rwsem(&device->uuid_sem);
-
-	disk = blk_alloc_disk(NUMA_NO_NODE);
 	if (!disk)
 		goto out_no_disk;
 
@@ -4051,6 +4045,13 @@
 	list_del_rcu(&connection->connections);
 	spin_unlock_irqrestore(&resource->req_lock, spin_lock_irq_flags);
 
+		/* something invalid as process exit code */
+	connection->retval_from_fencing_khelper = 42 << 8;
+	if (test_and_clear_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("Terminating threads waiting for fencing handler to exit (they will exit with 42)\n");
+		wake_up(&connection->fencing_handler_wait);
+	}
+
 	list_for_each_entry(struct drbd_peer_device, peer_device, &work_list, peer_devices)
 		drbd_debugfs_peer_device_cleanup(peer_device);
 	drbd_debugfs_connection_cleanup(connection);
@@ -4509,7 +4510,6 @@
 	return ~not_weak;
 }
 
-
 static bool __new_current_uuid_prepare(struct drbd_device *device, bool forced) 
 {
 	KIRQL spin_lock_irq_flags;
@@ -5490,8 +5490,7 @@
 		timeout = net_conf->ko_count ? net_conf->timeout * HZ / 10 * net_conf->ko_count :
 					       MAX_SCHEDULE_TIMEOUT;
 	} else {
-		struct disk_conf *disk_conf = rcu_dereference(device->ldev->disk_conf);
-		timeout = disk_conf->disk_timeout * HZ / 10;
+		timeout = MAX_SCHEDULE_TIMEOUT;
 	}
 	rcu_read_unlock(rcu_flags);
 
@@ -5697,7 +5696,6 @@
 }
 #endif
 
-
 module_init(drbd_init)
 module_exit(drbd_cleanup)
 
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_main.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_main.pdb
diff -r -u converted-sources/drbd/drbd_nl.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c
--- converted-sources/drbd/drbd_nl.c	2024-09-03 16:23:56.459193513 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c	2024-08-05 17:29:11.361645571 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -632,14 +631,14 @@
 }
 
 /* Macro refers to local variables peer_device, device and connection! */
-#define magic_printk(level, fmt, args...)				\
+#define magic_printk(level, fmt, ...)				\
 	do {								\
 		if (peer_device)					\
-			drbd_printk(level, peer_device, fmt, args);	\
+			drbd_printk(level, peer_device, fmt, __VA_ARGS__);	\
 		else if (device)					\
-			drbd_printk(level, device, fmt, args);		\
+			drbd_printk(level, device, fmt, __VA_ARGS__);		\
 		else							\
-			drbd_printk(level, connection, fmt, args);	\
+			drbd_printk(level, connection, fmt, __VA_ARGS__);	\
 	} while (0)
 
 static int drbd_khelper(struct drbd_device *device, struct drbd_connection *connection, char *cmd)
@@ -869,9 +868,25 @@
 	if (fencing_policy == FP_DONT_CARE)
 		return true;
 
! upstream fencing
-	r = drbd_maybe_khelper(NULL, connection, "fence-peer");
-	if (r == DRBD_UMH_DISABLED)
+	if (test_and_set_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("fence-peer handler already running, waiting for it to exit and using its return value.\n");
+
+		wait_event(connection->fencing_handler_wait, !test_bit(FENCING_HANDLER_RUNNING, &connection->flags));
+		r = connection->retval_from_fencing_khelper;
+
+		printk("Ok, the one running fence-peer handler returned %d, using this for further actions.\n", (r>>8)&0xff);
+	} else {
+		printk("About to start fence-peer handler ...\n");
+
+		r = drbd_maybe_khelper(NULL, connection, "fence-peer");
+		connection->retval_from_fencing_khelper = r;
+	}
+	if (r == DRBD_UMH_DISABLED) {
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		return true;
+	}
 
 	begin_state_change(resource, &irq_flags, CS_VERBOSE);
 	switch ((r>>8) & 0xff) {
@@ -910,6 +925,9 @@
 	default:
 		/* The script is broken ... */
 		drbd_err(connection, "fence-peer helper broken, returned %d\n", (r>>8)&0xff);
! upstream fencing
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		abort_state_change(resource, &irq_flags);
 		return false; /* Eventually leave IO frozen */
 	}
@@ -917,6 +935,9 @@
 	drbd_info(connection, "fence-peer helper returned %d (%s)\n",
 		  (r>>8) & 0xff, ex_to_string);
 
! upstream fencing
+	clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+	wake_up(&connection->fencing_handler_wait);
+
 	if (connection->cstate[NOW] >= C_CONNECTED ||
 	    initial_states_pending(connection)) {
 		/* connection re-established; do not fence */
@@ -1082,7 +1103,6 @@
 	return up_to_date > initial_up_to_date;
 }
 
-
 enum drbd_state_rv
 drbd_set_role(struct drbd_resource *resource, enum drbd_role role, bool force, struct sk_buff *reply_skb)
 {
@@ -2288,7 +2308,6 @@
 		drbd_info(device, "Suspended AL updates\n");
 }
 
-
 static bool should_set_defaults(struct genl_info *info)
 {
 	unsigned flags = ((struct drbd_genlmsghdr*)info->userhdr)->flags;
@@ -2504,6 +2523,8 @@
 		if (peer_device->repl_state[NOW] >= L_ESTABLISHED)
 			drbd_send_sync_param(peer_device);
 	}
! manual disk timeout
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, new_disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, new_disk_conf->disk_timeout * HZ / 10);
 
 	synchronize_rcu();
 	kfree(old_disk_conf);
@@ -3539,6 +3560,9 @@
 
 	device->device_conf.intentional_diskless = false; /* just in case... */
 
! manual disk timeout
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+
 	mod_timer(&device->request_timer, jiffies + HZ);
 
 	if (resource->role[NOW] == R_PRIMARY
@@ -4752,7 +4776,6 @@
 					      struct sk_buff *reply_skb)
 {
 	KIRQL spin_lock_irq_flags;
-	long remaining_time;
 	struct drbd_resource *resource = connection->resource;
 	enum drbd_conn_state cstate;
 	enum drbd_state_rv rv;
@@ -4800,10 +4823,9 @@
 	}
 
 	if (rv >= SS_SUCCESS)
! review: what is this?
-		wait_event_interruptible_timeout(remaining_time,
-						 resource->state_wait,
-						 connection->cstate[NOW] == C_STANDALONE,
-						 HZ);
+		wait_event(resource->state_wait,
+			   connection->cstate[NOW] == C_STANDALONE);
+
 	if (err_str) {
 		drbd_msg_put_info(reply_skb, err_str);
 		kfree(err_str);
@@ -5011,7 +5033,6 @@
 		}
 	}
 
-
 	local_max_size = drbd_local_max_size(device);
 	if (rs.resize_size && local_max_size < (sector_t)rs.resize_size) {
 		drbd_err(device, "requested %llu sectors, backend seems only able to support %llu\n",
@@ -5383,7 +5404,6 @@
 	return retcode;
 }
 
-
 int drbd_adm_invalidate_peer(struct sk_buff *skb, struct genl_info *info)
 {
 	struct drbd_config_context adm_ctx;
diff -r -u converted-sources/drbd/drbd_nl.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c.tmp.bak
--- converted-sources/drbd/drbd_nl.c.tmp.bak	2024-09-03 16:23:55.879187893 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c.tmp.bak	2024-08-05 17:29:10.777640224 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -594,14 +593,14 @@
 }
 
 /* Macro refers to local variables peer_device, device and connection! */
-#define magic_printk(level, fmt, args...)				\
+#define magic_printk(level, fmt, ...)				\
 	do {								\
 		if (peer_device)					\
-			drbd_printk(level, peer_device, fmt, args);	\
+			drbd_printk(level, peer_device, fmt, __VA_ARGS__);	\
 		else if (device)					\
-			drbd_printk(level, device, fmt, args);		\
+			drbd_printk(level, device, fmt, __VA_ARGS__);		\
 		else							\
-			drbd_printk(level, connection, fmt, args);	\
+			drbd_printk(level, connection, fmt, __VA_ARGS__);	\
 	} while (0)
 
 static int drbd_khelper(struct drbd_device *device, struct drbd_connection *connection, char *cmd)
@@ -825,12 +824,28 @@
 	if (fencing_policy == FP_DONT_CARE)
 		return true;
 
-	r = drbd_maybe_khelper(NULL, connection, "fence-peer");
-	if (r == DRBD_UMH_DISABLED)
! upstream fencing
+	if (test_and_set_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("fence-peer handler already running, waiting for it to exit and using its return value.\n");
+
+		wait_event(connection->fencing_handler_wait, !test_bit(FENCING_HANDLER_RUNNING, &connection->flags));
+		r = connection->retval_from_fencing_khelper;
+
+		printk("Ok, the one running fence-peer handler returned %d, using this for further actions.\n", (r>>8)&0xff);
+	} else {
+		printk("About to start fence-peer handler ...\n");
+
+		r = drbd_maybe_khelper(NULL, connection, "fence-peer");
+		connection->retval_from_fencing_khelper = r;
+	}
+	if (r == DRBD_UMH_DISABLED) {
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		return true;
+	}
 
 	begin_state_change(resource, &irq_flags, CS_VERBOSE);
-	switch (r & 0xff) {
! upstream fencing
+	switch ((r>>8) & 0xff) {
 	case P_INCONSISTENT: /* peer is inconsistent */
 		ex_to_string = "peer is inconsistent or worse";
 		__downgrade_peer_disk_states(connection, D_INCONSISTENT);
@@ -866,6 +881,9 @@
 	default:
 		/* The script is broken ... */
 		drbd_err(connection, "fence-peer helper broken, returned %d\n", (r>>8)&0xff);
! upstream fencing
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		abort_state_change(resource, &irq_flags);
 		return false; /* Eventually leave IO frozen */
 	}
@@ -873,6 +891,9 @@
 	drbd_info(connection, "fence-peer helper returned %d (%s)\n",
 		  (r>>8) & 0xff, ex_to_string);
 
! upstream fencing
+	clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+	wake_up(&connection->fencing_handler_wait);
+
 	if (connection->cstate[NOW] >= C_CONNECTED ||
 	    initial_states_pending(connection)) {
 		/* connection re-established; do not fence */
@@ -1033,7 +1054,6 @@
 	return up_to_date > initial_up_to_date;
 }
 
-
 enum drbd_state_rv
 drbd_set_role(struct drbd_resource *resource, enum drbd_role role, bool force, struct sk_buff *reply_skb)
 {
@@ -2203,7 +2223,6 @@
 		drbd_info(device, "Suspended AL updates\n");
 }
 
-
 static bool should_set_defaults(struct genl_info *info)
 {
 	unsigned flags = ((struct drbd_genlmsghdr*)info->userhdr)->flags;
@@ -2419,6 +2438,8 @@
 		if (peer_device->repl_state[NOW] >= L_ESTABLISHED)
 			drbd_send_sync_param(peer_device);
 	}
! manual disk timeout
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, new_disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, new_disk_conf->disk_timeout * HZ / 10);
 
 	synchronize_rcu();
 	kfree(old_disk_conf);
@@ -3452,6 +3473,9 @@
 
 	device->device_conf.intentional_diskless = false; /* just in case... */
 
! manual disk timeout
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+
 	mod_timer(&device->request_timer, jiffies + HZ);
 
 	if (resource->role[NOW] == R_PRIMARY
@@ -4699,9 +4723,9 @@
 	}
 
 	if (rv >= SS_SUCCESS)
-		wait_event_interruptible_timeout(resource->state_wait,
-						 connection->cstate[NOW] == C_STANDALONE,
-						 HZ);
+		wait_event(resource->state_wait,
+			   connection->cstate[NOW] == C_STANDALONE);
+
 	if (err_str) {
 		drbd_msg_put_info(reply_skb, err_str);
 		kfree(err_str);
@@ -4908,7 +4932,6 @@
 		}
 	}
 
-
 	local_max_size = drbd_local_max_size(device);
 	if (rs.resize_size && local_max_size < (sector_t)rs.resize_size) {
 		drbd_err(device, "requested %llu sectors, backend seems only able to support %llu\n",
@@ -5279,7 +5302,6 @@
 	return retcode;
 }
 
-
 int drbd_adm_invalidate_peer(struct sk_buff *skb, struct genl_info *info)
 {
 	struct drbd_config_context adm_ctx;
diff -r -u converted-sources/drbd/drbd_nl.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c.tmp.orig
--- converted-sources/drbd/drbd_nl.c.tmp.orig	2024-09-03 16:23:56.415193087 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_nl.c.tmp.orig	2024-08-05 17:29:11.321645205 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #define pr_fmt(fmt) ":" fmt
@@ -632,14 +631,14 @@
 }
 
 /* Macro refers to local variables peer_device, device and connection! */
-#define magic_printk(level, fmt, args...)				\
+#define magic_printk(level, fmt, ...)				\
 	do {								\
 		if (peer_device)					\
-			drbd_printk(level, peer_device, fmt, args);	\
+			drbd_printk(level, peer_device, fmt, __VA_ARGS__);	\
 		else if (device)					\
-			drbd_printk(level, device, fmt, args);		\
+			drbd_printk(level, device, fmt, __VA_ARGS__);		\
 		else							\
-			drbd_printk(level, connection, fmt, args);	\
+			drbd_printk(level, connection, fmt, __VA_ARGS__);	\
 	} while (0)
 
 static int drbd_khelper(struct drbd_device *device, struct drbd_connection *connection, char *cmd)
@@ -869,9 +868,25 @@
 	if (fencing_policy == FP_DONT_CARE)
 		return true;
 
-	r = drbd_maybe_khelper(NULL, connection, "fence-peer");
-	if (r == DRBD_UMH_DISABLED)
+	if (test_and_set_bit(FENCING_HANDLER_RUNNING, &connection->flags)) {
+		printk("fence-peer handler already running, waiting for it to exit and using its return value.\n");
+
+		wait_event(connection->fencing_handler_wait, !test_bit(FENCING_HANDLER_RUNNING, &connection->flags));
+		r = connection->retval_from_fencing_khelper;
+
+		printk("Ok, the one running fence-peer handler returned %d, using this for further actions.\n", (r>>8)&0xff);
+	} else {
+		printk("About to start fence-peer handler ...\n");
+
+		r = drbd_maybe_khelper(NULL, connection, "fence-peer");
+		connection->retval_from_fencing_khelper = r;
+	}
+	if (r == DRBD_UMH_DISABLED) {
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		return true;
+	}
 
 	begin_state_change(resource, &irq_flags, CS_VERBOSE);
 	switch ((r>>8) & 0xff) {
@@ -910,6 +925,9 @@
 	default:
 		/* The script is broken ... */
 		drbd_err(connection, "fence-peer helper broken, returned %d\n", (r>>8)&0xff);
+		clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+		wake_up(&connection->fencing_handler_wait);
+
 		abort_state_change(resource, &irq_flags);
 		return false; /* Eventually leave IO frozen */
 	}
@@ -917,6 +935,9 @@
 	drbd_info(connection, "fence-peer helper returned %d (%s)\n",
 		  (r>>8) & 0xff, ex_to_string);
 
+	clear_bit(FENCING_HANDLER_RUNNING, &connection->flags);
+	wake_up(&connection->fencing_handler_wait);
+
 	if (connection->cstate[NOW] >= C_CONNECTED ||
 	    initial_states_pending(connection)) {
 		/* connection re-established; do not fence */
@@ -1082,7 +1103,6 @@
 	return up_to_date > initial_up_to_date;
 }
 
-
 enum drbd_state_rv
 drbd_set_role(struct drbd_resource *resource, enum drbd_role role, bool force, struct sk_buff *reply_skb)
 {
@@ -2294,7 +2314,6 @@
 		drbd_info(device, "Suspended AL updates\n");
 }
 
-
 static bool should_set_defaults(struct genl_info *info)
 {
 	unsigned flags = ((struct drbd_genlmsghdr*)info->userhdr)->flags;
@@ -2510,6 +2529,8 @@
 		if (peer_device->repl_state[NOW] >= L_ESTABLISHED)
 			drbd_send_sync_param(peer_device);
 	}
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, new_disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, new_disk_conf->disk_timeout * HZ / 10);
 
 	synchronize_rcu();
 	kfree(old_disk_conf);
@@ -3545,6 +3566,9 @@
 
 	device->device_conf.intentional_diskless = false; /* just in case... */
 
+	windrbd_set_disk_timeout(device->ldev->backing_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+	windrbd_set_disk_timeout(device->ldev->md_bdev, device->ldev->disk_conf->disk_timeout * HZ / 10);
+
 	mod_timer(&device->request_timer, jiffies + HZ);
 
 	if (resource->role[NOW] == R_PRIMARY
@@ -4758,7 +4782,6 @@
 					      struct sk_buff *reply_skb)
 {
 	KIRQL spin_lock_irq_flags;
-	long remaining_time;
 	struct drbd_resource *resource = connection->resource;
 	enum drbd_conn_state cstate;
 	enum drbd_state_rv rv;
@@ -4806,10 +4829,9 @@
 	}
 
 	if (rv >= SS_SUCCESS)
-		wait_event_interruptible_timeout(remaining_time,
-						 resource->state_wait,
-						 connection->cstate[NOW] == C_STANDALONE,
-						 HZ);
+		wait_event(resource->state_wait,
+			   connection->cstate[NOW] == C_STANDALONE);
+
 	if (err_str) {
 		drbd_msg_put_info(reply_skb, err_str);
 		kfree(err_str);
@@ -5017,7 +5039,6 @@
 		}
 	}
 
-
 	local_max_size = drbd_local_max_size(device);
 	if (rs.resize_size && local_max_size < (sector_t)rs.resize_size) {
 		drbd_err(device, "requested %llu sectors, backend seems only able to support %llu\n",
@@ -5389,7 +5410,6 @@
 	return retcode;
 }
 
-
 int drbd_adm_invalidate_peer(struct sk_buff *skb, struct genl_info *info)
 {
 	struct drbd_config_context adm_ctx;
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_nl.c.tmp.rej
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_nl.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_nl.pdb
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_nla.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_nla.pdb
diff -r -u converted-sources/drbd/drbd_proc.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_proc.c
--- converted-sources/drbd/drbd_proc.c	2024-09-03 16:23:59.379221806 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_proc.c	2024-08-05 17:29:14.633675531 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
diff -r -u converted-sources/drbd/drbd_proc.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_proc.c.tmp.bak
--- converted-sources/drbd/drbd_proc.c.tmp.bak	2024-09-03 16:23:59.375221768 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_proc.c.tmp.bak	2024-08-05 17:29:14.629675495 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_proc.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_proc.pdb
diff -r -u converted-sources/drbd/drbd_receiver.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c
--- converted-sources/drbd/drbd_receiver.c	2024-09-03 16:23:35.162987148 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c	2024-08-05 17:28:59.453536567 +0000
@@ -10,7 +10,6 @@
 
  */
 
-
 #include <linux/module.h>
 
 #include <linux/uaccess.h>
@@ -3550,7 +3549,6 @@
 	/* no longer valid, about to call drbd_recv again for the digest... */
 	p = pi->data = NULL;
 
-
 	if (peer_device->repl_state[NOW] == L_AHEAD) {
 		if (pi->cmd == P_DATA_REQUEST) {
 			/* P_DATA_REQUEST originates from a Primary,
@@ -3955,7 +3953,6 @@
 		  (unsigned long long)flags);
 }
 
-
 static void drbd_uuid_dump_peer(struct drbd_peer_device *peer_device, u64 bits, u64 flags)
 {
 	const int node_id = peer_device->device->resource->res_opts.node_id;
@@ -5916,7 +5913,6 @@
 	int not_allocated = -1;
 	KIRQL flags = KeGetCurrentIrql();
 
-
 	peer_device = conn_peer_device(connection, pi->vnr);
 	if (!peer_device)
 		return config_unknown_volume(connection, pi);
@@ -6017,7 +6013,6 @@
 	return err;
 }
 
-
 /* If a primary looses connection to a SYNC_SOURCE node from us, then we
  * need to abort that resync. Why?
  *
@@ -6041,8 +6036,8 @@
 		if ((repl_state == L_SYNC_TARGET || repl_state == L_PAUSED_SYNC_T) &&
 		    NODE_MASK(peer_device->node_id) & weak_nodes) {
 			rcu_read_unlock(rcu_flags);
-	KIRQL flags = KeGetCurrentIrql();
 			goto abort;
+	KIRQL flags = KeGetCurrentIrql();
 		}
 	}
 	rcu_read_unlock(rcu_flags);
@@ -6583,7 +6578,6 @@
 	return CSC_MATCH;
 }
 
-
 enum alt_rv {
 	ALT_LOCKED,
 	ALT_MATCH,
@@ -6678,7 +6672,6 @@
 	return cmd == P_TWOPC_PREP_RSZ || cmd == P_TWOPC_PREPARE;
 }
 
-
 enum determine_dev_size
 drbd_commit_size_change(struct drbd_device *device, struct resize_parms *rs, u64 nodes_to_reach)
 {
@@ -7448,21 +7441,6 @@
 		transport->ops->set_rcvtimeo(transport, DATA_STREAM, MAX_SCHEDULE_TIMEOUT);
 	}
 
! upstream: that fixed some error in the field (can not thaw IO with an only Consistent peer)
-	if (new_repl_state == L_ESTABLISHED && peer_disk_state == D_CONSISTENT &&
-	    drbd_suspended(device) && peer_device->repl_state[NOW] < L_ESTABLISHED &&
-	    test_and_clear_bit(NEW_CUR_UUID, &device->flags)) {
-		/* Do not allow RESEND for a rebooted peer. We can only allow this
-		   for temporary network outages! */
-		drbd_err(peer_device, "Aborting Connect, can not thaw IO with an only Consistent peer\n");
-		tl_walk(connection, CONNECTION_LOST_WHILE_PENDING);
-		drbd_uuid_new_current(device, false);
-		begin_state_change(resource, &spin_lock_irq_flags, CS_HARD);
-		__change_cstate(connection, C_PROTOCOL_ERROR);
-		__change_io_susp_user(resource, false);
-		end_state_change(resource, &spin_lock_irq_flags);
-		return -EIO;
-	}
-
 	clear_bit(RS_SOURCE_MISSED_END, &peer_device->flags);
 	clear_bit(RS_PEER_MISSED_END, &peer_device->flags);
 
@@ -8340,6 +8318,7 @@
 	del_timer_sync(&peer_device->resync_timer);
 	resync_timer_fn(&peer_device->resync_timer);
 	del_timer_sync(&peer_device->start_resync_timer);
! remove stepaside
+	del_timer_sync(&peer_device->resync_stalled_timer);
 }
 
 static void drain_resync_activity(struct drbd_connection *connection)
@@ -9441,7 +9420,6 @@
 	return bitmap_bits;
 }
 
-
 static bool is_sync_source(struct drbd_peer_device *peer_device)
 {
 	return is_sync_source_state(peer_device, NOW) ||
@@ -9641,7 +9619,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	rcu_flags = rcu_read_lock();
 	nc = rcu_dereference(transport->net_conf);
 	t = ping_timeout ? nc->ping_timeo : nc->ping_int;
diff -r -u converted-sources/drbd/drbd_receiver.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c.tmp.bak
--- converted-sources/drbd/drbd_receiver.c.tmp.bak	2024-09-03 16:23:05.534699987 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c.tmp.bak	2024-08-05 17:28:58.873531259 +0000
@@ -10,7 +10,6 @@
 
  */
 
-
 #include <linux/module.h>
 
 #include <linux/uaccess.h>
@@ -3464,7 +3463,6 @@
 	/* no longer valid, about to call drbd_recv again for the digest... */
 	p = pi->data = NULL;
 
-
 	if (peer_device->repl_state[NOW] == L_AHEAD) {
 		if (pi->cmd == P_DATA_REQUEST) {
 			/* P_DATA_REQUEST originates from a Primary,
@@ -3864,7 +3862,6 @@
 		  (unsigned long long)flags);
 }
 
-
 static void drbd_uuid_dump_peer(struct drbd_peer_device *peer_device, u64 bits, u64 flags)
 {
 	const int node_id = peer_device->device->resource->res_opts.node_id;
@@ -5812,7 +5809,6 @@
 	struct drbd_device *device;
 	int not_allocated = -1;
 
-
 	peer_device = conn_peer_device(connection, pi->vnr);
 	if (!peer_device)
 		return config_unknown_volume(connection, pi);
@@ -5913,7 +5909,6 @@
 	return err;
 }
 
-
 /* If a primary looses connection to a SYNC_SOURCE node from us, then we
  * need to abort that resync. Why?
  *
@@ -6471,7 +6466,6 @@
 	return CSC_MATCH;
 }
 
-
 enum alt_rv {
 	ALT_LOCKED,
 	ALT_MATCH,
@@ -6563,7 +6557,6 @@
 	return cmd == P_TWOPC_PREP_RSZ || cmd == P_TWOPC_PREPARE;
 }
 
-
 enum determine_dev_size
 drbd_commit_size_change(struct drbd_device *device, struct resize_parms *rs, u64 nodes_to_reach)
 {
@@ -7304,21 +7297,6 @@
 		transport->ops->set_rcvtimeo(transport, DATA_STREAM, MAX_SCHEDULE_TIMEOUT);
 	}
 
-	if (new_repl_state == L_ESTABLISHED && peer_disk_state == D_CONSISTENT &&
-	    drbd_suspended(device) && peer_device->repl_state[NOW] < L_ESTABLISHED &&
-	    test_and_clear_bit(NEW_CUR_UUID, &device->flags)) {
-		/* Do not allow RESEND for a rebooted peer. We can only allow this
-		   for temporary network outages! */
-		drbd_err(peer_device, "Aborting Connect, can not thaw IO with an only Consistent peer\n");
-		tl_walk(connection, CONNECTION_LOST_WHILE_PENDING);
-		drbd_uuid_new_current(device, false);
-		begin_state_change(resource, &irq_flags, CS_HARD);
-		__change_cstate(connection, C_PROTOCOL_ERROR);
-		__change_io_susp_user(resource, false);
-		end_state_change(resource, &irq_flags);
-		return -EIO;
-	}
-
 	clear_bit(RS_SOURCE_MISSED_END, &peer_device->flags);
 	clear_bit(RS_PEER_MISSED_END, &peer_device->flags);
 
@@ -8188,6 +8166,7 @@
 	del_timer_sync(&peer_device->resync_timer);
 	resync_timer_fn(&peer_device->resync_timer);
 	del_timer_sync(&peer_device->start_resync_timer);
+	del_timer_sync(&peer_device->resync_stalled_timer);
 }
 
 static void drain_resync_activity(struct drbd_connection *connection)
@@ -9256,7 +9235,6 @@
 	return bitmap_bits;
 }
 
-
 static bool is_sync_source(struct drbd_peer_device *peer_device)
 {
 	return is_sync_source_state(peer_device, NOW) ||
@@ -9450,7 +9428,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	rcu_read_lock();
 	nc = rcu_dereference(transport->net_conf);
 	t = ping_timeout ? nc->ping_timeo : nc->ping_int;
diff -r -u converted-sources/drbd/drbd_receiver.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c.tmp.orig
--- converted-sources/drbd/drbd_receiver.c.tmp.orig	2024-09-03 16:23:35.146986993 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_receiver.c.tmp.orig	2024-08-05 17:28:59.437536421 +0000
@@ -10,7 +10,6 @@
 
  */
 
-
 #include <linux/module.h>
 
 #include <linux/uaccess.h>
@@ -3550,7 +3549,6 @@
 	/* no longer valid, about to call drbd_recv again for the digest... */
 	p = pi->data = NULL;
 
-
 	if (peer_device->repl_state[NOW] == L_AHEAD) {
 		if (pi->cmd == P_DATA_REQUEST) {
 			/* P_DATA_REQUEST originates from a Primary,
@@ -3955,7 +3953,6 @@
 		  (unsigned long long)flags);
 }
 
-
 static void drbd_uuid_dump_peer(struct drbd_peer_device *peer_device, u64 bits, u64 flags)
 {
 	const int node_id = peer_device->device->resource->res_opts.node_id;
@@ -5916,7 +5913,6 @@
 	int not_allocated = -1;
 	KIRQL flags = KeGetCurrentIrql();
 
-
 	peer_device = conn_peer_device(connection, pi->vnr);
 	if (!peer_device)
 		return config_unknown_volume(connection, pi);
@@ -6017,7 +6013,6 @@
 	return err;
 }
 
-
 /* If a primary looses connection to a SYNC_SOURCE node from us, then we
  * need to abort that resync. Why?
  *
@@ -6041,8 +6036,8 @@
 		if ((repl_state == L_SYNC_TARGET || repl_state == L_PAUSED_SYNC_T) &&
 		    NODE_MASK(peer_device->node_id) & weak_nodes) {
 			rcu_read_unlock(rcu_flags);
-	KIRQL flags = KeGetCurrentIrql();
 			goto abort;
+	KIRQL flags = KeGetCurrentIrql();
 		}
 	}
 	rcu_read_unlock(rcu_flags);
@@ -6583,7 +6578,6 @@
 	return CSC_MATCH;
 }
 
-
 enum alt_rv {
 	ALT_LOCKED,
 	ALT_MATCH,
@@ -6678,7 +6672,6 @@
 	return cmd == P_TWOPC_PREP_RSZ || cmd == P_TWOPC_PREPARE;
 }
 
-
 enum determine_dev_size
 drbd_commit_size_change(struct drbd_device *device, struct resize_parms *rs, u64 nodes_to_reach)
 {
@@ -7448,21 +7441,6 @@
 		transport->ops->set_rcvtimeo(transport, DATA_STREAM, MAX_SCHEDULE_TIMEOUT);
 	}
 
-	if (new_repl_state == L_ESTABLISHED && peer_disk_state == D_CONSISTENT &&
-	    drbd_suspended(device) && peer_device->repl_state[NOW] < L_ESTABLISHED &&
-	    test_and_clear_bit(NEW_CUR_UUID, &device->flags)) {
-		/* Do not allow RESEND for a rebooted peer. We can only allow this
-		   for temporary network outages! */
-		drbd_err(peer_device, "Aborting Connect, can not thaw IO with an only Consistent peer\n");
-		tl_walk(connection, CONNECTION_LOST_WHILE_PENDING);
-		drbd_uuid_new_current(device, false);
-		begin_state_change(resource, &spin_lock_irq_flags, CS_HARD);
-		__change_cstate(connection, C_PROTOCOL_ERROR);
-		__change_io_susp_user(resource, false);
-		end_state_change(resource, &spin_lock_irq_flags);
-		return -EIO;
-	}
-
 	clear_bit(RS_SOURCE_MISSED_END, &peer_device->flags);
 	clear_bit(RS_PEER_MISSED_END, &peer_device->flags);
 
@@ -8340,6 +8318,7 @@
 	del_timer_sync(&peer_device->resync_timer);
 	resync_timer_fn(&peer_device->resync_timer);
 	del_timer_sync(&peer_device->start_resync_timer);
+	del_timer_sync(&peer_device->resync_stalled_timer);
 }
 
 static void drain_resync_activity(struct drbd_connection *connection)
@@ -9441,7 +9420,6 @@
 	return bitmap_bits;
 }
 
-
 static bool is_sync_source(struct drbd_peer_device *peer_device)
 {
 	return is_sync_source_state(peer_device, NOW) ||
@@ -9641,7 +9619,6 @@
 	struct drbd_transport *transport = &connection->transport;
 	struct drbd_transport_ops *tr_ops = transport->ops;
 
-
 	rcu_flags = rcu_read_lock();
 	nc = rcu_dereference(transport->net_conf);
 	t = ping_timeout ? nc->ping_timeo : nc->ping_int;
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_receiver.c.tmp.rej
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_receiver.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_receiver.pdb
diff -r -u converted-sources/drbd/drbd_req.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c
--- converted-sources/drbd/drbd_req.c	2024-09-03 16:23:38.639020833 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c	2024-08-05 17:29:01.073551393 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -19,9 +18,6 @@
 #include "drbd_req.h"
 #include "linux/jiffies.h"
 
-
-
-
 static bool drbd_may_do_local_read(struct drbd_device *device, sector_t sector, int size);
 
 static struct drbd_request *drbd_req_new(struct drbd_device *device, struct bio *bio_src)
@@ -336,7 +332,6 @@
 	dec_ap_bio(device, rw);
 }
 
-
 /* Helper for __req_mod().
  * Set m->bio to the master bio, if it is fit to be completed,
  * or leave it alone (it is initialized to NULL in __req_mod),
@@ -627,7 +622,6 @@
 		req->net_rq_state[idx] |= set;
 	}
 
-
 	/* no change? */
 	if (req->local_rq_state == old_local &&
 	    (idx == -1 || req->net_rq_state[idx] == old_net))
@@ -1549,13 +1543,8 @@
 	/* allocate outside of all locks; */
 	req = drbd_req_new(device, bio);
 	if (!req) {
! review (some upstream fix?)
-		dec_ap_bio(device, rw);
-		/* only pass the error to the upper layers.
-		 * if user cannot handle io errors, that's not our business. */
 		drbd_err(device, "could not kmalloc() req\n");
-		bio->bi_status = BLK_STS_RESOURCE;
-		bio_endio(bio);
-		return ERR_PTR(-ENOMEM);
+		goto no_mem;
 	}
 
 	/* Update disk stats */
@@ -1563,6 +1552,11 @@
 
 	if (get_ldev(device)) {
 		req->private_bio  = bio_clone_fast(bio, GFP_NOIO, &drbd_io_bio_set);
! review (some upstream fix?)
+		if (!req->private_bio) {
+			kfree(req);
+			drbd_err(device, "could not bio_clone_fast() private_bio\n");
+			goto no_mem;
+		}
 		req->private_bio->bi_private = req;
 		req->private_bio->bi_end_io = drbd_request_endio;
 	}
@@ -1598,6 +1592,14 @@
 	ktime_aggregate_delta(device, req->start_kt, before_queue_kt);
 	drbd_queue_write(device, req);
 	return NULL;
! review (some upstream fix?)
+
+ no_mem:
+	dec_ap_bio(device, rw);
+	/* only pass the error to the upper layers.
+	 * if user cannot handle io errors, that's not our business. */
+	bio->bi_status = BLK_STS_RESOURCE;
+	bio_endio(bio);
+	return ERR_PTR(-ENOMEM);
 }
 
 /* Require at least one path to current data.
@@ -1700,7 +1702,6 @@
 		maybe_pull_ahead(device);
 	}
 
-
 	if (drbd_suspended(device)) {
 		/* push back and retry: */
 		req->local_rq_state |= RQ_POSTPONED;
@@ -2363,13 +2364,6 @@
 	ULONG_PTR next_trigger_time = now;
 	bool restart_timer = false;
 
! remove disk timeout
-	rcu_flags = rcu_read_lock();
-	if (get_ldev(device)) { /* implicit state.disk >= D_INCONSISTENT */
-		dt = rcu_dereference(device->ldev->disk_conf)->disk_timeout * HZ / 10;
-		put_ldev(device);
-	}
-	rcu_read_unlock(rcu_flags);
-
 	spin_lock_irqsave(&device->resource->req_lock, flags);
 	if (dt) {
 		ULONG_PTR write_pre_submit_jif = now, read_pre_submit_jif = now;
diff -r -u converted-sources/drbd/drbd_req.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c.tmp.bak
--- converted-sources/drbd/drbd_req.c.tmp.bak	2024-09-03 16:23:38.179016376 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c.tmp.bak	2024-08-05 17:29:00.521546341 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -19,9 +18,6 @@
 #include "drbd_req.h"
 #include "linux/jiffies.h"
 
-
-
-
 static bool drbd_may_do_local_read(struct drbd_device *device, sector_t sector, int size);
 
 static struct drbd_request *drbd_req_new(struct drbd_device *device, struct bio *bio_src)
@@ -334,7 +330,6 @@
 	dec_ap_bio(device, rw);
 }
 
-
 /* Helper for __req_mod().
  * Set m->bio to the master bio, if it is fit to be completed,
  * or leave it alone (it is initialized to NULL in __req_mod),
@@ -625,7 +620,6 @@
 		req->net_rq_state[idx] |= set;
 	}
 
-
 	/* no change? */
 	if (req->local_rq_state == old_local &&
 	    (idx == -1 || req->net_rq_state[idx] == old_net))
@@ -1536,13 +1530,8 @@
 	/* allocate outside of all locks; */
 	req = drbd_req_new(device, bio);
 	if (!req) {
-		dec_ap_bio(device, rw);
-		/* only pass the error to the upper layers.
-		 * if user cannot handle io errors, that's not our business. */
 		drbd_err(device, "could not kmalloc() req\n");
-		bio->bi_status = BLK_STS_RESOURCE;
-		bio_endio(bio);
-		return ERR_PTR(-ENOMEM);
+		goto no_mem;
 	}
 
 	/* Update disk stats */
@@ -1550,6 +1539,11 @@
 
 	if (get_ldev(device)) {
 		req->private_bio  = bio_clone_fast(bio, GFP_NOIO, &drbd_io_bio_set);
+		if (!req->private_bio) {
+			kfree(req);
+			drbd_err(device, "could not bio_clone_fast() private_bio\n");
+			goto no_mem;
+		}
 		req->private_bio->bi_private = req;
 		req->private_bio->bi_end_io = drbd_request_endio;
 	}
@@ -1585,6 +1579,14 @@
 	ktime_aggregate_delta(device, req->start_kt, before_queue_kt);
 	drbd_queue_write(device, req);
 	return NULL;
+
+ no_mem:
+	dec_ap_bio(device, rw);
+	/* only pass the error to the upper layers.
+	 * if user cannot handle io errors, that's not our business. */
+	bio->bi_status = BLK_STS_RESOURCE;
+	bio_endio(bio);
+	return ERR_PTR(-ENOMEM);
 }
 
 /* Require at least one path to current data.
@@ -1685,7 +1687,6 @@
 		maybe_pull_ahead(device);
 	}
 
-
 	if (drbd_suspended(device)) {
 		/* push back and retry: */
 		req->local_rq_state |= RQ_POSTPONED;
@@ -2341,13 +2342,6 @@
 	ULONG_PTR next_trigger_time = now;
 	bool restart_timer = false;
 
-	rcu_read_lock();
-	if (get_ldev(device)) { /* implicit state.disk >= D_INCONSISTENT */
-		dt = rcu_dereference(device->ldev->disk_conf)->disk_timeout * HZ / 10;
-		put_ldev(device);
-	}
-	rcu_read_unlock();
-
 	spin_lock_irq(&device->resource->req_lock);
 	if (dt) {
 		ULONG_PTR write_pre_submit_jif = now, read_pre_submit_jif = now;
diff -r -u converted-sources/drbd/drbd_req.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c.tmp.orig
--- converted-sources/drbd/drbd_req.c.tmp.orig	2024-09-03 16:23:38.627020717 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.c.tmp.orig	2024-08-05 17:29:01.061551283 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -19,9 +18,6 @@
 #include "drbd_req.h"
 #include "linux/jiffies.h"
 
-
-
-
 static bool drbd_may_do_local_read(struct drbd_device *device, sector_t sector, int size);
 
 static struct drbd_request *drbd_req_new(struct drbd_device *device, struct bio *bio_src)
@@ -336,7 +332,6 @@
 	dec_ap_bio(device, rw);
 }
 
-
 /* Helper for __req_mod().
  * Set m->bio to the master bio, if it is fit to be completed,
  * or leave it alone (it is initialized to NULL in __req_mod),
@@ -627,7 +622,6 @@
 		req->net_rq_state[idx] |= set;
 	}
 
-
 	/* no change? */
 	if (req->local_rq_state == old_local &&
 	    (idx == -1 || req->net_rq_state[idx] == old_net))
@@ -1549,13 +1543,8 @@
 	/* allocate outside of all locks; */
 	req = drbd_req_new(device, bio);
 	if (!req) {
-		dec_ap_bio(device, rw);
-		/* only pass the error to the upper layers.
-		 * if user cannot handle io errors, that's not our business. */
 		drbd_err(device, "could not kmalloc() req\n");
-		bio->bi_status = BLK_STS_RESOURCE;
-		bio_endio(bio);
-		return ERR_PTR(-ENOMEM);
+		goto no_mem;
 	}
 
 	/* Update disk stats */
@@ -1563,6 +1552,11 @@
 
 	if (get_ldev(device)) {
 		req->private_bio  = bio_clone_fast(bio, GFP_NOIO, &drbd_io_bio_set);
+		if (!req->private_bio) {
+			kfree(req);
+			drbd_err(device, "could not bio_clone_fast() private_bio\n");
+			goto no_mem;
+		}
 		req->private_bio->bi_private = req;
 		req->private_bio->bi_end_io = drbd_request_endio;
 	}
@@ -1598,6 +1592,14 @@
 	ktime_aggregate_delta(device, req->start_kt, before_queue_kt);
 	drbd_queue_write(device, req);
 	return NULL;
+
+ no_mem:
+	dec_ap_bio(device, rw);
+	/* only pass the error to the upper layers.
+	 * if user cannot handle io errors, that's not our business. */
+	bio->bi_status = BLK_STS_RESOURCE;
+	bio_endio(bio);
+	return ERR_PTR(-ENOMEM);
 }
 
 /* Require at least one path to current data.
@@ -1700,7 +1702,6 @@
 		maybe_pull_ahead(device);
 	}
 
-
 	if (drbd_suspended(device)) {
 		/* push back and retry: */
 		req->local_rq_state |= RQ_POSTPONED;
@@ -2363,13 +2364,6 @@
 	ULONG_PTR next_trigger_time = now;
 	bool restart_timer = false;
 
-	rcu_flags = rcu_read_lock();
-	if (get_ldev(device)) { /* implicit state.disk >= D_INCONSISTENT */
-		dt = rcu_dereference(device->ldev->disk_conf)->disk_timeout * HZ / 10;
-		put_ldev(device);
-	}
-	rcu_read_unlock(rcu_flags);
-
 	spin_lock_irqsave(&device->resource->req_lock, flags);
 	if (dt) {
 		ULONG_PTR write_pre_submit_jif = now, read_pre_submit_jif = now;
diff -r -u converted-sources/drbd/drbd_req.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.h
--- converted-sources/drbd/drbd_req.h	2024-09-03 16:23:56.919197970 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.h	2024-08-05 17:29:11.861650149 +0000
@@ -262,7 +262,6 @@
 #define RQ_POSTPONED	   (((ULONG_PTR)1) << __RQ_POSTPONED)
 #define RQ_COMPLETION_SUSP (((ULONG_PTR)1) << __RQ_COMPLETION_SUSP)
 
-
 /* these flags go into local_rq_state,
  * orhter flags go into their respective net_rq_state[idx] */
 #define RQ_STATE_0_MASK	\
diff -r -u converted-sources/drbd/drbd_req.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.h.tmp.bak
--- converted-sources/drbd/drbd_req.h.tmp.bak	2024-09-03 16:23:56.647195334 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_req.h.tmp.bak	2024-08-05 17:29:11.557647365 +0000
@@ -262,7 +262,6 @@
 #define RQ_POSTPONED	   (((ULONG_PTR)1) << __RQ_POSTPONED)
 #define RQ_COMPLETION_SUSP (((ULONG_PTR)1) << __RQ_COMPLETION_SUSP)
 
-
 /* these flags go into local_rq_state,
  * orhter flags go into their respective net_rq_state[idx] */
 #define RQ_STATE_0_MASK	\
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_req.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_req.pdb
diff -r -u converted-sources/drbd/drbd_sender.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c
--- converted-sources/drbd/drbd_sender.c	2024-09-03 16:23:04.522690176 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c	2024-08-05 17:28:57.777521229 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -24,6 +23,7 @@
 #include <linux/scatterlist.h>
 #include <linux/overflow.h>
 #include <linux/part_stat.h>
+#include <linux/drbd_limits.h>
 
 #include "drbd_int.h"
 #include "drbd_protocol.h"
@@ -88,7 +88,7 @@
  */
 static void drbd_endio_read_sec_final(struct drbd_peer_request *peer_req) __releases(local)
 {
! fixes some compiler warning?
-	KIRQL flags;
+	KIRQL flags = 0;
 	struct drbd_peer_device *peer_device = peer_req->peer_device;
 	struct drbd_device *device = peer_device->device;
 	struct drbd_connection *connection = peer_device->connection;
@@ -116,7 +116,7 @@
  * "submitted" by the receiver, final stage.  */
 void drbd_endio_write_sec_final(struct drbd_peer_request *peer_req) __releases(local)
 {
! fixes some compiler warning?
-	KIRQL flags;
+	KIRQL flags = 0;
 	struct drbd_peer_device *peer_device = peer_req->peer_device;
 	struct drbd_device *device = peer_device->device;
 	struct drbd_connection *connection = peer_device->connection;
@@ -229,7 +229,6 @@
 		/* Do not panic. */
 }
 
-
 /* read, readA or write requests on R_PRIMARY coming from drbd_submit_bio
  */
 void drbd_request_endio(struct bio *bio)
@@ -647,7 +646,10 @@
 	rcu_flags = rcu_read_lock();
 	nc = rcu_dereference(peer_device->connection->transport.net_conf);
 	mxb = nc ? nc->max_buffers : 0;
! remove (step aside)
-	if (rcu_dereference(peer_device->rs_plan_s)->size) {
+        if (windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		peer_device->c_sync_rate = DRBD_RESYNC_RATE_MAX;
+		number = RS_MAKE_REQS_INTV * peer_device->c_sync_rate  / ((BM_BLOCK_SIZE / 1024) * HZ);
+	} else if (rcu_dereference(peer_device->rs_plan_s)->size) {
 		number = drbd_rs_controller(peer_device, sect_in, ktime_to_ns(duration)) >> (BM_BLOCK_SHIFT - 9);
 		peer_device->c_sync_rate = number * HZ * (BM_BLOCK_SIZE / 1024) / RS_MAKE_REQS_INTV;
 	} else {
@@ -1164,7 +1166,6 @@
 	int verify_done = 0;
 	bool aborted = false;
 
-
 	if (repl_state[NOW] == L_SYNC_SOURCE || repl_state[NOW] == L_PAUSED_SYNC_S) {
 		/* Make sure all queued w_update_peers()/consider_sending_peers_in_sync()
 		   executed before killing the resync_lru with drbd_rs_del_all() */
@@ -3113,7 +3114,6 @@
 		if (get_t_state(thi) != RUNNING)
 			break;
 
-
 		while (!list_empty(&work_list)) {
 			w = list_first_entry(&work_list, struct drbd_work, list);
 			list_del_init(&w->list);
diff -r -u converted-sources/drbd/drbd_sender.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c.tmp.bak
--- converted-sources/drbd/drbd_sender.c.tmp.bak	2024-09-03 16:22:56.382611269 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c.tmp.bak	2024-08-05 17:28:57.121515226 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -24,6 +23,7 @@
 #include <linux/scatterlist.h>
 #include <linux/overflow.h>
 #include <linux/part_stat.h>
+#include <linux/drbd_limits.h>
 
 #include "drbd_int.h"
 #include "drbd_protocol.h"
@@ -230,7 +230,6 @@
 		device->minor, device->resource->name, device->vnr);
 }
 
-
 /* read, readA or write requests on R_PRIMARY coming from drbd_submit_bio
  */
 void drbd_request_endio(struct bio *bio)
@@ -645,7 +644,10 @@
 	rcu_read_lock();
 	nc = rcu_dereference(peer_device->connection->transport.net_conf);
 	mxb = nc ? nc->max_buffers : 0;
-	if (rcu_dereference(peer_device->rs_plan_s)->size) {
+        if (windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		peer_device->c_sync_rate = DRBD_RESYNC_RATE_MAX;
+		number = RS_MAKE_REQS_INTV * peer_device->c_sync_rate  / ((BM_BLOCK_SIZE / 1024) * HZ);
+	} else if (rcu_dereference(peer_device->rs_plan_s)->size) {
 		number = drbd_rs_controller(peer_device, sect_in, ktime_to_ns(duration)) >> (BM_BLOCK_SHIFT - 9);
 		peer_device->c_sync_rate = number * HZ * (BM_BLOCK_SIZE / 1024) / RS_MAKE_REQS_INTV;
 	} else {
@@ -1157,7 +1159,6 @@
 	int verify_done = 0;
 	bool aborted = false;
 
-
 	if (repl_state[NOW] == L_SYNC_SOURCE || repl_state[NOW] == L_PAUSED_SYNC_S) {
 		/* Make sure all queued w_update_peers()/consider_sending_peers_in_sync()
 		   executed before killing the resync_lru with drbd_rs_del_all() */
@@ -3067,7 +3068,6 @@
 		if (get_t_state(thi) != RUNNING)
 			break;
 
-
 		while (!list_empty(&work_list)) {
 			w = list_first_entry(&work_list, struct drbd_work, list);
 			list_del_init(&w->list);
diff -r -u converted-sources/drbd/drbd_sender.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c.tmp.orig
--- converted-sources/drbd/drbd_sender.c.tmp.orig	2024-09-03 16:23:04.502689983 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_sender.c.tmp.orig	2024-08-05 17:28:57.761521083 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 1999-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2002-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #include <linux/module.h>
@@ -24,6 +23,7 @@
 #include <linux/scatterlist.h>
 #include <linux/overflow.h>
 #include <linux/part_stat.h>
+#include <linux/drbd_limits.h>
 
 #include "drbd_int.h"
 #include "drbd_protocol.h"
@@ -88,7 +88,7 @@
  */
 static void drbd_endio_read_sec_final(struct drbd_peer_request *peer_req) __releases(local)
 {
-	KIRQL flags;
+	KIRQL flags = 0;
 	struct drbd_peer_device *peer_device = peer_req->peer_device;
 	struct drbd_device *device = peer_device->device;
 	struct drbd_connection *connection = peer_device->connection;
@@ -116,7 +116,7 @@
  * "submitted" by the receiver, final stage.  */
 void drbd_endio_write_sec_final(struct drbd_peer_request *peer_req) __releases(local)
 {
-	KIRQL flags;
+	KIRQL flags = 0;
 	struct drbd_peer_device *peer_device = peer_req->peer_device;
 	struct drbd_device *device = peer_device->device;
 	struct drbd_connection *connection = peer_device->connection;
@@ -230,7 +230,6 @@
 		device->minor, device->resource->name, device->vnr);
 }
 
-
 /* read, readA or write requests on R_PRIMARY coming from drbd_submit_bio
  */
 void drbd_request_endio(struct bio *bio)
@@ -645,7 +644,10 @@
 	rcu_flags = rcu_read_lock();
 	nc = rcu_dereference(peer_device->connection->transport.net_conf);
 	mxb = nc ? nc->max_buffers : 0;
-	if (rcu_dereference(peer_device->rs_plan_s)->size) {
+        if (windrbd_application_io_suspended(peer_device->device->this_bdev)) {
+		peer_device->c_sync_rate = DRBD_RESYNC_RATE_MAX;
+		number = RS_MAKE_REQS_INTV * peer_device->c_sync_rate  / ((BM_BLOCK_SIZE / 1024) * HZ);
+	} else if (rcu_dereference(peer_device->rs_plan_s)->size) {
 		number = drbd_rs_controller(peer_device, sect_in, ktime_to_ns(duration)) >> (BM_BLOCK_SHIFT - 9);
 		peer_device->c_sync_rate = number * HZ * (BM_BLOCK_SIZE / 1024) / RS_MAKE_REQS_INTV;
 	} else {
@@ -1162,7 +1164,6 @@
 	int verify_done = 0;
 	bool aborted = false;
 
-
 	if (repl_state[NOW] == L_SYNC_SOURCE || repl_state[NOW] == L_PAUSED_SYNC_S) {
 		/* Make sure all queued w_update_peers()/consider_sending_peers_in_sync()
 		   executed before killing the resync_lru with drbd_rs_del_all() */
@@ -3111,7 +3112,6 @@
 		if (get_t_state(thi) != RUNNING)
 			break;
 
-
 		while (!list_empty(&work_list)) {
 			w = list_first_entry(&work_list, struct drbd_work, list);
 			list_del_init(&w->list);
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_sender.c.tmp.rej
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_sender.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_sender.pdb
diff -r -u converted-sources/drbd/drbd_state.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c
--- converted-sources/drbd/drbd_state.c	2024-09-03 16:24:38.727603012 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c	2024-08-05 17:29:27.053789291 +0000
@@ -21,7 +21,6 @@
 #include "drbd_req.h"
 #include "drbd_state_change.h"
 
-
 struct after_state_change_work {
 	struct drbd_work w;
 	struct drbd_state_change *state_change;
@@ -1738,7 +1737,6 @@
 	return SS_SUCCESS;
 }
 
-
 /**
  * is_valid_transition() - Returns an SS_ error code if the state transition is not possible
  * This limits hard state transitions. Hard state transitions are facts there are
@@ -2311,9 +2309,13 @@
 	peer_device->rs_last_sect_ev = 0;
 	peer_device->rs_total = tw;
 	peer_device->rs_start = now;
! remove (step aside)
+	peer_device->rs_last_bm_extent = NULL;
 	peer_device->rs_last_writeout = now;
 	initialize_resync_progress_marks(peer_device);
 	drbd_rs_controller_reset(peer_device);
! remove (step aside)
+
+		/* In case resync is stalled at the beginning */
+	mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
 }
 
 /* Is there a primary with access to up to date data known */
@@ -2521,7 +2523,6 @@
 				peer_device->rs_mark_time[peer_device->rs_last_mark] = jiffies;
 			}
 
-
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(RECONCILIATION_RESYNC, &peer_device->flags);
 
@@ -2627,6 +2628,23 @@
 
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(SYNC_SRC_CRASHED_PRI, &peer_device->flags);
! remove (step aside)
+
+			if ((repl_state[OLD] == L_SYNC_SOURCE || repl_state[OLD] == L_SYNC_TARGET) &&
+			   !(repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+				del_timer(&peer_device->resync_stalled_timer);
+				peer_device->rs_last_bm_extent = NULL;
+
+				windrbd_resume_application_io(peer_device->device->this_bdev,
+					"Resync state change syncing -> not syncing, resuming application I/O\n");
+			}
+			if ((repl_state[OLD] == L_PAUSED_SYNC_S || repl_state[OLD] == L_PAUSED_SYNC_T) &&
+			    (repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+			/* In case resync is stalled when resuming */
+				printk("Resuming sync, arming application suspension timer.\n");
+				mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+				peer_device->rs_last_bm_extent = NULL;	/* We don't know where it was before. */
+			}
+
 		}
 
 		for_each_connection(connection, resource) {
@@ -3419,7 +3437,6 @@
 	drbd_md_sync_if_dirty(device);
 }
 
-
 /*
  * Perform after state change actions that may sleep.
  */
@@ -4974,7 +4991,6 @@
 
 	resource->role[NEW] = role;
 
-
 	rcu_flags = rcu_read_lock();
 	idr_for_each_entry(&resource->devices, device, vnr) {
 		if (role == R_PRIMARY && force) {
@@ -5287,7 +5303,8 @@
 {
 	struct drbd_resource *resource = connection->resource;
 
! upstream (I think this is already merged ... )
-	if ((connection->fencing_policy >= FP_RESOURCE ||
+	if (connection->cstate[NOW] == C_CONNECTED &&
+	    (connection->fencing_policy >= FP_RESOURCE ||
 	     connection->resource->res_opts.quorum != QOU_OFF) &&
 	    resource->role[NOW] != connection->peer_role[NOW]) {
 		/* primary politely disconnects from secondary,
diff -r -u converted-sources/drbd/drbd_state.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmp.bak
--- converted-sources/drbd/drbd_state.c.tmp.bak	2024-09-03 16:24:12.671350591 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmp.bak	2024-08-05 17:29:26.441783685 +0000
@@ -21,7 +21,6 @@
 #include "drbd_req.h"
 #include "drbd_state_change.h"
 
-
 struct after_state_change_work {
 	struct drbd_work w;
 	struct drbd_state_change *state_change;
@@ -1727,7 +1726,6 @@
 	return SS_SUCCESS;
 }
 
-
 /**
  * is_valid_transition() - Returns an SS_ error code if the state transition is not possible
  * This limits hard state transitions. Hard state transitions are facts there are
@@ -2298,9 +2296,13 @@
 	peer_device->rs_last_sect_ev = 0;
 	peer_device->rs_total = tw;
 	peer_device->rs_start = now;
+	peer_device->rs_last_bm_extent = NULL;
 	peer_device->rs_last_writeout = now;
 	initialize_resync_progress_marks(peer_device);
 	drbd_rs_controller_reset(peer_device);
+
+		/* In case resync is stalled at the beginning */
+	mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
 }
 
 /* Is there a primary with access to up to date data known */
@@ -2508,7 +2510,6 @@
 				peer_device->rs_mark_time[peer_device->rs_last_mark] = jiffies;
 			}
 
-
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(RECONCILIATION_RESYNC, &peer_device->flags);
 
@@ -2614,6 +2615,23 @@
 
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(SYNC_SRC_CRASHED_PRI, &peer_device->flags);
+
+			if ((repl_state[OLD] == L_SYNC_SOURCE || repl_state[OLD] == L_SYNC_TARGET) &&
+			   !(repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+				del_timer(&peer_device->resync_stalled_timer);
+				peer_device->rs_last_bm_extent = NULL;
+
+				windrbd_resume_application_io(peer_device->device->this_bdev,
+					"Resync state change syncing -> not syncing, resuming application I/O\n");
+			}
+			if ((repl_state[OLD] == L_PAUSED_SYNC_S || repl_state[OLD] == L_PAUSED_SYNC_T) &&
+			    (repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+			/* In case resync is stalled when resuming */
+				printk("Resuming sync, arming application suspension timer.\n");
+				mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+				peer_device->rs_last_bm_extent = NULL;	/* We don't know where it was before. */
+			}
+
 		}
 
 		for_each_connection(connection, resource) {
@@ -3401,7 +3419,6 @@
 	drbd_md_sync_if_dirty(device);
 }
 
-
 /*
  * Perform after state change actions that may sleep.
  */
@@ -4937,7 +4954,6 @@
 
 	resource->role[NEW] = role;
 
-
 	rcu_read_lock();
 	idr_for_each_entry(&resource->devices, device, vnr) {
 		if (role == R_PRIMARY && force) {
@@ -5249,7 +5265,8 @@
 {
 	struct drbd_resource *resource = connection->resource;
 
-	if ((connection->fencing_policy >= FP_RESOURCE ||
+	if (connection->cstate[NOW] == C_CONNECTED &&
+	    (connection->fencing_policy >= FP_RESOURCE ||
 	     connection->resource->res_opts.quorum != QOU_OFF) &&
 	    resource->role[NOW] != connection->peer_role[NOW]) {
 		/* primary politely disconnects from secondary,
diff -r -u converted-sources/drbd/drbd_state.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmp.orig
--- converted-sources/drbd/drbd_state.c.tmp.orig	2024-09-03 16:24:38.711602857 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmp.orig	2024-08-05 17:29:27.029789071 +0000
@@ -21,7 +21,6 @@
 #include "drbd_req.h"
 #include "drbd_state_change.h"
 
-
 struct after_state_change_work {
 	struct drbd_work w;
 	struct drbd_state_change *state_change;
@@ -1738,7 +1737,6 @@
 	return SS_SUCCESS;
 }
 
-
 /**
  * is_valid_transition() - Returns an SS_ error code if the state transition is not possible
  * This limits hard state transitions. Hard state transitions are facts there are
@@ -2311,9 +2309,13 @@
 	peer_device->rs_last_sect_ev = 0;
 	peer_device->rs_total = tw;
 	peer_device->rs_start = now;
+	peer_device->rs_last_bm_extent = NULL;
 	peer_device->rs_last_writeout = now;
 	initialize_resync_progress_marks(peer_device);
 	drbd_rs_controller_reset(peer_device);
+
+		/* In case resync is stalled at the beginning */
+	mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
 }
 
 /* Is there a primary with access to up to date data known */
@@ -2521,7 +2523,6 @@
 				peer_device->rs_mark_time[peer_device->rs_last_mark] = jiffies;
 			}
 
-
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(RECONCILIATION_RESYNC, &peer_device->flags);
 
@@ -2627,6 +2628,23 @@
 
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(SYNC_SRC_CRASHED_PRI, &peer_device->flags);
+
+			if ((repl_state[OLD] == L_SYNC_SOURCE || repl_state[OLD] == L_SYNC_TARGET) &&
+			   !(repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+				del_timer(&peer_device->resync_stalled_timer);
+				peer_device->rs_last_bm_extent = NULL;
+
+				windrbd_resume_application_io(peer_device->device->this_bdev,
+					"Resync state change syncing -> not syncing, resuming application I/O\n");
+			}
+			if ((repl_state[OLD] == L_PAUSED_SYNC_S || repl_state[OLD] == L_PAUSED_SYNC_T) &&
+			    (repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+			/* In case resync is stalled when resuming */
+				printk("Resuming sync, arming application suspension timer.\n");
+				mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+				peer_device->rs_last_bm_extent = NULL;	/* We don't know where it was before. */
+			}
+
 		}
 
 		for_each_connection(connection, resource) {
@@ -3419,7 +3437,6 @@
 	drbd_md_sync_if_dirty(device);
 }
 
-
 /*
  * Perform after state change actions that may sleep.
  */
@@ -4974,7 +4991,6 @@
 
 	resource->role[NEW] = role;
 
-
 	rcu_flags = rcu_read_lock();
 	idr_for_each_entry(&resource->devices, device, vnr) {
 		if (role == R_PRIMARY && force) {
@@ -5212,8 +5228,10 @@
 	if (device->disk_state[NOW] == D_ATTACHING &&
 	    context->val.disk == D_NEGOTIATING) {
 		if (device_has_peer_devices_with_disk(device)) {
+			struct drbd_connection *connection =
+				first_connection(device->resource);
 			cluster_wide_state_change =
-				supports_two_phase_commit(device->resource);
+				connection && connection->agreed_pro_version >= 110;
 		} else {
 			/* very last part of attach */
 			context->val.disk = disk_state_from_md(device);
@@ -5287,7 +5305,8 @@
 {
 	struct drbd_resource *resource = connection->resource;
 
-	if ((connection->fencing_policy >= FP_RESOURCE ||
+	if (connection->cstate[NOW] == C_CONNECTED &&
+	    (connection->fencing_policy >= FP_RESOURCE ||
 	     connection->resource->res_opts.quorum != QOU_OFF) &&
 	    resource->role[NOW] != connection->peer_role[NOW]) {
 		/* primary politely disconnects from secondary,
diff -r -u converted-sources/drbd/drbd_state.c.tmpe ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmpe
--- converted-sources/drbd/drbd_state.c.tmpe	2024-09-03 16:24:36.059577167 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.c.tmpe	2024-08-05 17:29:26.481784051 +0000
@@ -21,7 +21,6 @@
 #include "drbd_req.h"
 #include "drbd_state_change.h"
 
-
 struct after_state_change_work {
 	struct drbd_work w;
 	struct drbd_state_change *state_change;
@@ -1738,7 +1737,6 @@
 	return SS_SUCCESS;
 }
 
-
 /**
  * is_valid_transition() - Returns an SS_ error code if the state transition is not possible
  * This limits hard state transitions. Hard state transitions are facts there are
@@ -2310,9 +2308,13 @@
 	peer_device->rs_last_sect_ev = 0;
 	peer_device->rs_total = tw;
 	peer_device->rs_start = now;
+	peer_device->rs_last_bm_extent = NULL;
 	peer_device->rs_last_writeout = now;
 	initialize_resync_progress_marks(peer_device);
 	drbd_rs_controller_reset(peer_device);
+
+		/* In case resync is stalled at the beginning */
+	mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
 }
 
 /* Is there a primary with access to up to date data known */
@@ -2520,7 +2522,6 @@
 				peer_device->rs_mark_time[peer_device->rs_last_mark] = jiffies;
 			}
 
-
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(RECONCILIATION_RESYNC, &peer_device->flags);
 
@@ -2626,6 +2627,23 @@
 
 			if (repl_state[OLD] > L_ESTABLISHED && repl_state[NEW] <= L_ESTABLISHED)
 				clear_bit(SYNC_SRC_CRASHED_PRI, &peer_device->flags);
+
+			if ((repl_state[OLD] == L_SYNC_SOURCE || repl_state[OLD] == L_SYNC_TARGET) &&
+			   !(repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+				del_timer(&peer_device->resync_stalled_timer);
+				peer_device->rs_last_bm_extent = NULL;
+
+				windrbd_resume_application_io(peer_device->device->this_bdev,
+					"Resync state change syncing -> not syncing, resuming application I/O\n");
+			}
+			if ((repl_state[OLD] == L_PAUSED_SYNC_S || repl_state[OLD] == L_PAUSED_SYNC_T) &&
+			    (repl_state[NEW] == L_SYNC_SOURCE || repl_state[NEW] == L_SYNC_TARGET)) {
+			/* In case resync is stalled when resuming */
+				printk("Resuming sync, arming application suspension timer.\n");
+				mod_timer(&peer_device->resync_stalled_timer, jiffies + 5*HZ);
+				peer_device->rs_last_bm_extent = NULL;	/* We don't know where it was before. */
+			}
+
 		}
 
 		for_each_connection(connection, resource) {
@@ -3418,7 +3436,6 @@
 	drbd_md_sync_if_dirty(device);
 }
 
-
 /*
  * Perform after state change actions that may sleep.
  */
@@ -4971,7 +4988,6 @@
 
 	resource->role[NEW] = role;
 
-
 	rcu_flags = rcu_read_lock();
 	idr_for_each_entry(&resource->devices, device, vnr) {
 		if (role == R_PRIMARY && force) {
@@ -5285,7 +5301,8 @@
 {
 	struct drbd_resource *resource = connection->resource;
 
-	if ((connection->fencing_policy >= FP_RESOURCE ||
+	if (connection->cstate[NOW] == C_CONNECTED &&
+	    (connection->fencing_policy >= FP_RESOURCE ||
 	     connection->resource->res_opts.quorum != QOU_OFF) &&
 	    resource->role[NOW] != connection->peer_role[NOW]) {
 		/* primary politely disconnects from secondary,
diff -r -u converted-sources/drbd/drbd_state.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h
--- converted-sources/drbd/drbd_state.h	2024-09-03 16:23:52.227152506 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h	2024-08-05 17:29:07.357608913 +0000
@@ -62,7 +62,6 @@
 extern void clear_remote_state_change(struct drbd_resource *resource);
 extern void __clear_remote_state_change(struct drbd_resource *resource);
 
-
 enum which_state;
 extern union drbd_state drbd_get_device_state(struct drbd_device *, enum which_state);
 extern union drbd_state drbd_get_peer_device_state(struct drbd_peer_device *, enum which_state);
diff -r -u converted-sources/drbd/drbd_state.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h.tmp.bak
--- converted-sources/drbd/drbd_state.h.tmp.bak	2024-09-03 16:23:51.719147584 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h.tmp.bak	2024-08-05 17:29:06.817603970 +0000
@@ -62,7 +62,6 @@
 extern void clear_remote_state_change(struct drbd_resource *resource);
 extern void __clear_remote_state_change(struct drbd_resource *resource);
 
-
 enum which_state;
 extern union drbd_state drbd_get_device_state(struct drbd_device *, enum which_state);
 extern union drbd_state drbd_get_peer_device_state(struct drbd_peer_device *, enum which_state);
diff -r -u converted-sources/drbd/drbd_state.h.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h.tmp.orig
--- converted-sources/drbd/drbd_state.h.tmp.orig	2024-09-03 16:23:52.019150490 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_state.h.tmp.orig	2024-08-05 17:29:07.045606057 +0000
@@ -47,12 +47,12 @@
 enum drbd_disk_state conn_highest_disk(struct drbd_connection *connection);
 enum drbd_disk_state conn_highest_pdsk(struct drbd_connection *connection);
 
-extern void state_change_lock(struct drbd_resource *, KIRQL *, enum chg_state_flags);
-extern void state_change_unlock(struct drbd_resource *, KIRQL *);
+extern void state_change_lock(struct drbd_resource *, ULONG_PTR *, enum chg_state_flags);
+extern void state_change_unlock(struct drbd_resource *, ULONG_PTR *);
 
-extern void begin_state_change(struct drbd_resource *, KIRQL *, enum chg_state_flags);
-extern enum drbd_state_rv end_state_change(struct drbd_resource *, KIRQL *);
-extern void abort_state_change(struct drbd_resource *, KIRQL *);
+extern void begin_state_change(struct drbd_resource *, ULONG_PTR *, enum chg_state_flags);
+extern enum drbd_state_rv end_state_change(struct drbd_resource *, ULONG_PTR *);
+extern void abort_state_change(struct drbd_resource *, ULONG_PTR *);
 extern void abort_state_change_locked(struct drbd_resource *resource);
 
 extern void begin_state_change_locked(struct drbd_resource *, enum chg_state_flags);
@@ -62,7 +62,6 @@
 extern void clear_remote_state_change(struct drbd_resource *resource);
 extern void __clear_remote_state_change(struct drbd_resource *resource);
 
-
 enum which_state;
 extern union drbd_state drbd_get_device_state(struct drbd_device *, enum which_state);
 extern union drbd_state drbd_get_peer_device_state(struct drbd_peer_device *, enum which_state);
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_state.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_state.pdb
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_strings.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_strings.pdb
diff -r -u converted-sources/drbd/drbd_transport.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c
--- converted-sources/drbd/drbd_transport.c	2024-09-03 16:24:12.219346213 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c	2024-08-05 17:29:25.933779031 +0000
@@ -120,7 +120,6 @@
     return (addr->u.Byte[0] == 0xff) && ((addr->u.Byte[1] & 0xc0) == 0x80);
 }
 
-
 static bool addr_equal(const struct sockaddr_storage *addr1, const struct sockaddr_storage *addr2)
 {
 	if (addr1->ss_family != addr2->ss_family)
diff -r -u converted-sources/drbd/drbd_transport.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c.tmp.bak
--- converted-sources/drbd/drbd_transport.c.tmp.bak	2024-09-03 16:24:11.947343577 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c.tmp.bak	2024-08-05 17:29:25.597775953 +0000
@@ -120,7 +120,6 @@
     return (addr->u.Byte[0] == 0xff) && ((addr->u.Byte[1] & 0xc0) == 0x80);
 }
 
-
 static bool addr_equal(const struct sockaddr_storage *addr1, const struct sockaddr_storage *addr2)
 {
 	if (addr1->ss_family != addr2->ss_family)
diff -r -u converted-sources/drbd/drbd_transport.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c.tmp.orig
--- converted-sources/drbd/drbd_transport.c.tmp.orig	2024-09-03 16:24:12.203346058 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport.c.tmp.orig	2024-08-05 17:29:25.601775989 +0000
@@ -120,7 +120,6 @@
     return (addr->u.Byte[0] == 0xff) && ((addr->u.Byte[1] & 0xc0) == 0x80);
 }
 
-
 static bool addr_equal(const struct sockaddr_storage *addr1, const struct sockaddr_storage *addr2)
 {
 	if (addr1->ss_family != addr2->ss_family)
@@ -332,11 +331,6 @@
 	notify_path(connection, path, destroyed ? NOTIFY_DESTROY : NOTIFY_CHANGE);
 }
 
-void init_transport(void)
-{
-	init_rwsem(&transport_classes_lock);
-}
-
 /* Network transport abstractions */
 EXPORT_SYMBOL_GPL(drbd_register_transport_class);
 EXPORT_SYMBOL_GPL(drbd_unregister_transport_class);
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_transport.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_transport.pdb
diff -r -u converted-sources/drbd/drbd_transport_tcp.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c
--- converted-sources/drbd/drbd_transport_tcp.c	2024-09-03 16:23:55.603185219 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c	2024-08-05 17:29:10.501637697 +0000
@@ -6,7 +6,6 @@
 
    Copyright (C) 2014-2017, LINBIT HA-Solutions GmbH.
 
-
 */
 
 #include <linux/module.h>
@@ -24,7 +23,6 @@
 #include "drbd_wrappers.h"
 #include <drbd_int.h>
 
-
 MODULE_AUTHOR("Philipp Reisner <philipp.reisner@linbit.com>");
 MODULE_AUTHOR("Lars Ellenberg <lars.ellenberg@linbit.com>");
 MODULE_AUTHOR("Roland Kammerer <roland.kammerer@linbit.com>");
@@ -496,8 +494,6 @@
 		switch (err) {
 		case -ETIMEDOUT:
 		case -EINPROGRESS:
! review ??
-		case -EINTR:
-		case -ERESTARTSYS:
 		case -ECONNREFUSED:
 		case -ECONNRESET:
 		case -ENETUNREACH:
@@ -955,7 +951,6 @@
 	dsocket = NULL;
 	csocket = NULL;
 
-
 	for_each_path_ref(drbd_path, transport) {
 		struct dtt_path *path = container_of(drbd_path, struct dtt_path, path);
 
diff -r -u converted-sources/drbd/drbd_transport_tcp.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c.tmp.bak
--- converted-sources/drbd/drbd_transport_tcp.c.tmp.bak	2024-09-03 16:23:55.083180180 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c.tmp.bak	2024-08-05 17:29:09.965632789 +0000
@@ -6,7 +6,6 @@
 
    Copyright (C) 2014-2017, LINBIT HA-Solutions GmbH.
 
-
 */
 
 #include <linux/module.h>
@@ -23,7 +22,6 @@
 #include <drbd_transport.h>
 #include "drbd_wrappers.h"
 
-
 MODULE_AUTHOR("Philipp Reisner <philipp.reisner@linbit.com>");
 MODULE_AUTHOR("Lars Ellenberg <lars.ellenberg@linbit.com>");
 MODULE_AUTHOR("Roland Kammerer <roland.kammerer@linbit.com>");
@@ -467,8 +465,6 @@
 		switch (err) {
 		case -ETIMEDOUT:
 		case -EINPROGRESS:
-		case -EINTR:
-		case -ERESTARTSYS:
 		case -ECONNREFUSED:
 		case -ECONNRESET:
 		case -ENETUNREACH:
@@ -901,7 +897,6 @@
 	dsocket = NULL;
 	csocket = NULL;
 
-
 	for_each_path_ref(drbd_path, transport) {
 		struct dtt_path *path = container_of(drbd_path, struct dtt_path, path);
 
diff -r -u converted-sources/drbd/drbd_transport_tcp.c.tmp.orig ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c.tmp.orig
--- converted-sources/drbd/drbd_transport_tcp.c.tmp.orig	2024-09-03 16:23:55.559184793 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/drbd_transport_tcp.c.tmp.orig	2024-08-05 17:29:10.409636854 +0000
@@ -6,7 +6,6 @@
 
    Copyright (C) 2014-2017, LINBIT HA-Solutions GmbH.
 
-
 */
 
 #include <linux/module.h>
@@ -24,7 +23,6 @@
 #include "drbd_wrappers.h"
 #include <drbd_int.h>
 
-
 MODULE_AUTHOR("Philipp Reisner <philipp.reisner@linbit.com>");
 MODULE_AUTHOR("Lars Ellenberg <lars.ellenberg@linbit.com>");
 MODULE_AUTHOR("Roland Kammerer <roland.kammerer@linbit.com>");
@@ -78,7 +76,7 @@
 static void dtt_stats(struct drbd_transport *transport, struct drbd_transport_stats *stats);
 static void dtt_net_conf_change(struct drbd_transport *transport, struct net_conf *new_net_conf);
 static void dtt_set_rcvtimeo(struct drbd_transport *transport, enum drbd_stream stream, LONG_PTR timeout);
-static LONG_PTR dtt_get_rcvtimeo(struct drbd_transport *transport, enum drbd_stream stream);
+static long dtt_get_rcvtimeo(struct drbd_transport *transport, enum drbd_stream stream);
 static int dtt_send_page(struct drbd_transport *transport, enum drbd_stream, struct page *page,
 		int offset, size_t size, unsigned msg_flags);
 static int dtt_send_zc_bio(struct drbd_transport *, struct bio *bio);
@@ -495,8 +493,6 @@
 		switch (err) {
 		case -ETIMEDOUT:
 		case -EINPROGRESS:
-		case -EINTR:
-		case -ERESTARTSYS:
 		case -ECONNREFUSED:
 		case -ECONNRESET:
 		case -ENETUNREACH:
@@ -954,7 +950,6 @@
 	dsocket = NULL;
 	csocket = NULL;
 
-
 	for_each_path_ref(drbd_path, transport) {
 		struct dtt_path *path = container_of(drbd_path, struct dtt_path, path);
 
@@ -1196,7 +1191,7 @@
 	socket->sk->sk_rcvtimeo = timeout;
 }
 
-static LONG_PTR dtt_get_rcvtimeo(struct drbd_transport *transport, enum drbd_stream stream)
+static long dtt_get_rcvtimeo(struct drbd_transport *transport, enum drbd_stream stream)
 {
 	struct drbd_tcp_transport *tcp_transport =
 		container_of(transport, struct drbd_tcp_transport, transport);
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_transport_tcp.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: drbd_transport_tcp.pdb
diff -r -u converted-sources/drbd/kref_debug.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/kref_debug.c
--- converted-sources/drbd/kref_debug.c	2024-09-03 16:23:57.643204986 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/kref_debug.c	2024-08-05 17:29:12.661657474 +0000
@@ -1,4 +1,3 @@
-
 #define pr_fmt(fmt) ":" fmt
 
 #include <linux/spinlock.h>
diff -r -u converted-sources/drbd/kref_debug.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/kref_debug.c.tmp.bak
--- converted-sources/drbd/kref_debug.c.tmp.bak	2024-09-03 16:23:57.335202001 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/kref_debug.c.tmp.bak	2024-08-05 17:29:12.357654691 +0000
@@ -1,4 +1,3 @@
-
 #define pr_fmt(fmt) ":" fmt
 
 #include <linux/spinlock.h>
Only in converted-sources/drbd: kref_debug.c.tmp.orig
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: kref_debug.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: kref_debug.pdb
diff -r -u converted-sources/drbd/linux/lru_cache.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/linux/lru_cache.h
--- converted-sources/drbd/linux/lru_cache.h	2024-09-03 16:24:42.483639394 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/linux/lru_cache.h	2024-08-05 17:29:31.193827224 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 2003-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2003-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #ifndef LRU_CACHE_H
@@ -198,7 +197,6 @@
 	/* see below: flag-bits for lru_cache */
 	ULONG_PTR flags;
 
-
 	void  *lc_private;
 	const char *name;
 
@@ -207,7 +205,6 @@
 	struct lc_element **lc_element;
 };
 
-
 /* flag-bits for lru_cache */
 enum {
 	/* debugging aid, to catch concurrent access early.
diff -r -u converted-sources/drbd/linux/lru_cache.h.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/linux/lru_cache.h.tmp.bak
--- converted-sources/drbd/linux/lru_cache.h.tmp.bak	2024-09-03 16:24:42.483639394 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/linux/lru_cache.h.tmp.bak	2024-08-05 17:29:31.189827187 +0000
@@ -8,7 +8,6 @@
    Copyright (C) 2003-2008, Philipp Reisner <philipp.reisner@linbit.com>.
    Copyright (C) 2003-2008, Lars Ellenberg <lars.ellenberg@linbit.com>.
 
-
  */
 
 #ifndef LRU_CACHE_H
@@ -198,7 +197,6 @@
 	/* see below: flag-bits for lru_cache */
 	ULONG_PTR flags;
 
-
 	void  *lc_private;
 	const char *name;
 
@@ -207,7 +205,6 @@
 	struct lc_element **lc_element;
 };
 
-
 /* flag-bits for lru_cache */
 enum {
 	/* debugging aid, to catch concurrent access early.
diff -r -u converted-sources/drbd/lru_cache.c ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c
--- converted-sources/drbd/lru_cache.c	2024-09-03 16:23:50.091131808 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c	2024-08-05 17:29:05.105588298 +0000
@@ -4,7 +4,6 @@
 
    This file is part of DRBD by Philipp Reisner and Lars Ellenberg.
 
-
  */
 
  /* Enable all warnings throws lots of those warnings: */
@@ -26,9 +25,9 @@
 	BUG_ON(test_and_set_bit(__LC_PARANOIA, &lc->flags)); \
 } while (0)
 
-#define RETURN(x...)     do { \
+#define RETURN(...)     do { \
 	clear_bit_unlock(__LC_PARANOIA, &lc->flags); \
-	return x ; } while (0)
+	return __VA_ARGS__ ; } while (0)
 
 /* BUG() if e is not one of the elements tracked by lc */
 #define PARANOIA_LC_ELEMENT(lc, e) do {	\
@@ -38,7 +37,6 @@
 	BUG_ON(i >= lc_->nr_elements);	\
 	BUG_ON(lc_->lc_element[i] != e_); } while (0)
 
-
 /* We need to atomically
  *  - try_ to grab the lock (set LC_LOCKED)
  *  - only if there is no pending transaction
@@ -224,7 +222,6 @@
 	return  lc->lc_slot + (enr % lc->nr_elements);
 }
 
-
 static struct lc_element *__lc_find(struct lru_cache *lc, unsigned int enr,
 		bool include_changing)
 {
@@ -525,7 +522,6 @@
 	RETURN();
 }
 
-
 /**
  * lc_put - give up refcnt of @e
  * @lc: the lru cache to operate on
diff -r -u converted-sources/drbd/lru_cache.c.tmp.bak ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c.tmp.bak
--- converted-sources/drbd/lru_cache.c.tmp.bak	2024-09-03 16:23:49.623127273 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c.tmp.bak	2024-08-05 17:29:04.609583758 +0000
@@ -4,7 +4,6 @@
 
    This file is part of DRBD by Philipp Reisner and Lars Ellenberg.
 
-
  */
 
 #include <linux/module.h>
@@ -23,9 +22,9 @@
 	BUG_ON(test_and_set_bit(__LC_PARANOIA, &lc->flags)); \
 } while (0)
 
-#define RETURN(x...)     do { \
+#define RETURN(...)     do { \
 	clear_bit_unlock(__LC_PARANOIA, &lc->flags); \
-	return x ; } while (0)
+	return __VA_ARGS__ ; } while (0)
 
 /* BUG() if e is not one of the elements tracked by lc */
 #define PARANOIA_LC_ELEMENT(lc, e) do {	\
@@ -35,7 +34,6 @@
 	BUG_ON(i >= lc_->nr_elements);	\
 	BUG_ON(lc_->lc_element[i] != e_); } while (0)
 
-
 /* We need to atomically
  *  - try_ to grab the lock (set LC_LOCKED)
  *  - only if there is no pending transaction
@@ -221,7 +219,6 @@
 	return  lc->lc_slot + (enr % lc->nr_elements);
 }
 
-
 static struct lc_element *__lc_find(struct lru_cache *lc, unsigned int enr,
 		bool include_changing)
 {
@@ -522,7 +519,6 @@
 	RETURN();
 }
 
-
 /**
  * lc_put - give up refcnt of @e
  * @lc: the lru cache to operate on
Only in converted-sources/drbd: lru_cache.c.tmp.orig
diff -r -u converted-sources/drbd/lru_cache.c.tmpe ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c.tmpe
--- converted-sources/drbd/lru_cache.c.tmpe	2024-09-03 16:23:49.603127080 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/lru_cache.c.tmpe	2024-08-05 17:29:04.577583465 +0000
@@ -4,7 +4,6 @@
 
    This file is part of DRBD by Philipp Reisner and Lars Ellenberg.
 
-
  */
 
 #include <linux/module.h>
@@ -23,9 +22,9 @@
 	BUG_ON(test_and_set_bit(__LC_PARANOIA, &lc->flags)); \
 } while (0)
 
-#define RETURN(x...)     do { \
+#define RETURN(...)     do { \
 	clear_bit_unlock(__LC_PARANOIA, &lc->flags); \
-	return x ; } while (0)
+	return __VA_ARGS__ ; } while (0)
 
 /* BUG() if e is not one of the elements tracked by lc */
 #define PARANOIA_LC_ELEMENT(lc, e) do {	\
@@ -35,7 +34,6 @@
 	BUG_ON(i >= lc_->nr_elements);	\
 	BUG_ON(lc_->lc_element[i] != e_); } while (0)
 
-
 /* We need to atomically
  *  - try_ to grab the lock (set LC_LOCKED)
  *  - only if there is no pending transaction
@@ -222,7 +220,6 @@
 	return  lc->lc_slot + (enr % lc->nr_elements);
 }
 
-
 static struct lc_element *__lc_find(struct lru_cache *lc, unsigned int enr,
 		bool include_changing)
 {
@@ -523,7 +520,6 @@
 	RETURN();
 }
 
-
 /**
  * lc_put - give up refcnt of @e
  * @lc: the lru cache to operate on
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: lru_cache.obj
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: lru_cache.pdb
Only in converted-sources/drbd: ms-cl-jt-win7-32bit-server2003.cmd
Only in converted-sources/drbd: ms-cl-jt-win7-32bit.cmd
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: msg00002.bin
diff -r -u converted-sources/drbd/resource.rc ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/resource.rc
--- converted-sources/drbd/resource.rc	2024-09-03 16:24:43.983653924 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/resource.rc	2024-08-06 15:00:09.871540593 +0000
@@ -1,5 +1,5 @@
-#define VER_FILEVERSION		1,1,8,81
-#define VER_FILEVERSION_STR	"windrbd-1.1.8-81-g8882d292\0"
+#define VER_FILEVERSION		1,1,19,0
+#define VER_FILEVERSION_STR	"windrbd-1.1.19\0"
 #define VER_PRODUCTVERSION	VER_FILEVERSION
 #define VER_PRODUCTVERSION_STR	VER_FILEVERSION_STR
 
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: resource.res
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: stalled-resync-fix.patch
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd-event-log.h
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd-event-log.rc
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd-event-log.res
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd.cat
diff -r -u converted-sources/drbd/windrbd.inf ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/windrbd.inf
--- converted-sources/drbd/windrbd.inf	2024-09-03 16:24:43.987653963 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/windrbd.inf	2024-08-06 15:00:10.074799853 +0000
@@ -12,7 +12,7 @@
 ; Class       = "System"
 ; ClassGUID   = {4D36E97D-E325-11CE-BFC1-08002BE10318}
 Provider    =   "Linbit"
-DriverVer = 09/03/2024,1.1.8.81  ;Replaced by build magic
+DriverVer = 08/06/2024,1.1.19.0  ;Replaced by build magic
 CatalogFile =   windrbd.cat
 
 [Manufacturer]
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd.pdb
Only in ../../windrbd-1.1-branch/windrbd/converted-sources/drbd: windrbd.sys
diff -r -u converted-sources/drbd/windrbd_version.h ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/windrbd_version.h
--- converted-sources/drbd/windrbd_version.h	2024-09-03 16:24:43.983653924 +0000
+++ ../../windrbd-1.1-branch/windrbd/converted-sources/drbd/windrbd_version.h	2024-08-06 15:00:09.905930485 +0000
@@ -1,4 +1,4 @@
 #ifndef __WINDRBD_VERSION_H
 #define __WINDRBD_VERSION_H
-#define WINDRBD_VERSION "windrbd-1.1.8-81-g8882d292"
+#define WINDRBD_VERSION "windrbd-1.1.19"
 #endif
