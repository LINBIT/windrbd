diff --git a/drbd/drbd-headers b/drbd/drbd-headers
index 690752cf8..dbd2e0af0 160000
--- a/drbd/drbd-headers
+++ b/drbd/drbd-headers
@@ -1 +1 @@
-Subproject commit 690752cf834db544ae0d3d6f945a20f907d3839a
+Subproject commit dbd2e0af08823774f44243839a9c2617d3ce6079
diff --git a/drbd/drbd_bitmap.c b/drbd/drbd_bitmap.c
index 6df175eb4..e16444a21 100644
--- a/drbd/drbd_bitmap.c
+++ b/drbd/drbd_bitmap.c
! upstream: put_page is better here
@@ -335,7 +335,7 @@ static void bm_free_pages(struct page **pages, unsigned long number)
 				 i, number);
 			continue;
 		}
-		__free_page(pages[i]);
+		put_page(pages[i]);
 		pages[i] = NULL;
 	}
 }
diff --git a/drbd/drbd_int.h b/drbd/drbd_int.h
index cd46f2974..dfa6af370 100644
--- a/drbd/drbd_int.h
+++ b/drbd/drbd_int.h
! upstream: ULL is 32-bit save
@@ -1975,7 +1975,7 @@ sector_t drbd_partition_data_capacity(struct drbd_device *device);
 #else
 /* We allow up to 1 PiB on 64 bit architectures as long as our meta data
  * is large enough. */
-#define DRBD_MAX_SECTORS (1UL << (50 - SECTOR_SHIFT))
+#define DRBD_MAX_SECTORS (1ULL << (50 - SECTOR_SHIFT))
 #endif
 
 #define DRBD_MAX_SIZE_H80_PACKET (1U << 15) /* Header 80 only allows packets up to 32KiB data */
@@ -2382,7 +2382,7 @@ void drbd_init_connect_state(struct drbd_connection *connection);
 
! compat: get_capacity->bdev_nr_sectors
 static inline sector_t drbd_get_capacity(struct block_device *bdev)
 {
-	return bdev ? bdev_nr_sectors(bdev) : 0;
+	return bdev ? get_capacity(bdev->bd_disk) : 0;
 }
 
 /* sets the number of 512 byte sectors of our virtual device */
diff --git a/drbd/drbd_main.c b/drbd/drbd_main.c
index 5fb74b159..26be1231d 100644
--- a/drbd/drbd_main.c
+++ b/drbd/drbd_main.c
@@ -48,6 +48,7 @@
! remove: we're not Primary yet ...
 #include "drbd_debugfs.h"
 #include "drbd_meta_data.h"
 #include "drbd_dax_pmem.h"
+#include "windrbd.h"
 
 static int drbd_open(struct gendisk *gd, blk_mode_t mode);
 static void drbd_release(struct gendisk *gd);
@@ -98,7 +99,7 @@ module_param_named(allow_oos, drbd_allow_oos, bool, 0);
 unsigned int drbd_minor_count = DRBD_MINOR_COUNT_DEF;
 /* Module parameter for setting the user mode helper program
  * to run. Default is /sbin/drbdadm */
! compat: need to have an ioctl for setting the um helper binary and user mode helper should call that um helper (it might be something different from c:\windrbd) after all is this used by WinDRBD?
-char drbd_usermode_helper[80] = "/sbin/drbdadm";
+char drbd_usermode_helper[80] = "/cygdrive/c/windrbd/usr/sbin/drbdadm";
 module_param_named(minor_count, drbd_minor_count, uint, 0444);
 module_param_string(usermode_helper, drbd_usermode_helper, sizeof(drbd_usermode_helper), 0644);
 
@@ -150,8 +151,8 @@ module_param_named(strict_names, drbd_strict_names, drbd_strict_names, 0644);
  */
! compat: have is_initialized flag in mutex, and initialize on first usage (but not spinlocks spinlocks are 0).
 struct idr drbd_devices;
 struct list_head drbd_resources;
-static DEFINE_SPINLOCK(drbd_devices_lock);
-DEFINE_MUTEX(resources_mutex);
+spinlock_t drbd_devices_lock;
+struct mutex resources_mutex;
 
 struct workqueue_struct *ping_ack_sender;
 struct kmem_cache *drbd_request_cache;
@@ -2616,7 +2617,7 @@ static void snprintf_current_comm_pid_tag(union comm_pid_tag_buf *s, const char
 	snprintf(s->buf + len, sizeof(s->buf)-len, ":%d %s", task_pid_nr(current), tag);
 }
 
! compat: this should go away once we throw out booting from WinDRBD
-static int try_to_promote(struct drbd_device *device, long timeout, bool ndelay)
+int try_to_promote(struct drbd_device *device, long timeout, bool ndelay)
 {
 	struct drbd_resource *resource = device->resource;
 	int rv;
@@ -3328,7 +3329,7 @@ void drbd_destroy_device(struct kref *kref)
 		free_peer_device(peer_device);
 	}
 
! upstream: put_page is better here
-	__free_page(device->md_io.page);
+	put_page(device->md_io.page);
 	kref_debug_destroy(&device->kref_debug);
 
 	INIT_WORK(&device->finalize_work, drbd_device_finalize_work_fn);
@@ -4077,6 +4078,7 @@ static int init_submitter(struct drbd_device *device)
 enum drbd_ret_code drbd_create_device(struct drbd_config_context *adm_ctx, unsigned int minor,
 				      struct device_conf *device_conf, struct drbd_device **p_device)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_flags;
 	struct drbd_resource *resource = adm_ctx->resource;
 	struct drbd_connection *connection;
 	struct drbd_device *device;
@@ -4207,7 +4209,7 @@ enum drbd_ret_code drbd_create_device(struct drbd_config_context *adm_ctx, unsig
 	INIT_LIST_HEAD(&device->pending_bitmap_io);
 
 	locked = true;
! remove: unneccessary now as we fixed the RCU locks.
-	write_lock_irq(&resource->state_rwlock);
+	write_lock_irqsave(&resource->state_rwlock, spin_lock_flags);
 	spin_lock(&drbd_devices_lock);
 	id = idr_alloc(&drbd_devices, device, minor, minor + 1, GFP_NOWAIT);
 	spin_unlock(&drbd_devices_lock);
@@ -4241,7 +4243,7 @@ enum drbd_ret_code drbd_create_device(struct drbd_config_context *adm_ctx, unsig
 		kref_get(&device->kref);
 		kref_debug_get(&device->kref_debug, 1);
 	}
! remove: unneccessary now as we fixed the RCU locks.
-	write_unlock_irq(&resource->state_rwlock);
+	write_unlock_irqrestore(&resource->state_rwlock, spin_lock_flags);
 	locked = false;
 
 	if (init_conflict_submitter(device)) {
@@ -4271,6 +4273,15 @@ enum drbd_ret_code drbd_create_device(struct drbd_config_context *adm_ctx, unsig
 
 	drbd_debugfs_device_add(device);
 	*p_device = device;
! compat: the DRBD device is in bdev->bd_disk->private
+
+		/* From here we consider the DRBD device as valid.
+		 * drbd_open and I/O will be called.
+		 */
+	device->vdisk->part0->drbd_device = device;
+
! remove: we're not Primary yet ...
+		/* Tell the PnP manager that we are there ... */
+	windrbd_rescan_bus();
+
 	return NO_ERROR;
 
 out_destroy_submitter:
@@ -4298,7 +4309,7 @@ out_idr_remove_minor:
 	kref_debug_put(&device->kref_debug, 1);
 out_no_minor_idr:
 	if (locked)
! remove: unneccessary now as we fixed the RCU locks.
-		write_unlock_irq(&resource->state_rwlock);
+		write_unlock_irqrestore(&resource->state_rwlock, spin_lock_flags);
 	synchronize_rcu();
 
 out_no_peer_device:
@@ -4309,7 +4320,7 @@ out_no_peer_device:
 
 	drbd_bm_free(device->bitmap);
 out_no_bitmap:
! upstream: put_page is better here
-	__free_page(device->md_io.page);
+	put_page(device->md_io.page);
 out_no_io_page:
 	put_disk(disk);
 out_no_disk:
@@ -4522,6 +4533,13 @@ static int __init drbd_init(void)
 	spin_lock_init(&retry.lock);
 	INIT_LIST_HEAD(&retry.writes);
 
! compat: have is_initialized flag in mutex, and initialize on first usage (but not spinlocks spinlocks are 0).
+	/* WinDRBD: initialize these at runtime since a Windows
+	 * function (KeInitializeSpinLock) has to be called.
+	 */
+	spin_lock_init(&drbd_devices_lock);
+	mutex_init(&resources_mutex);
+	mutex_init(&notification_mutex);        /* defined in drbd_nl.c */
+
 	drbd_debugfs_init();
 
 	pr_info("initialized. "
@@ -5312,7 +5330,7 @@ static const char *name_of_node_id(struct drbd_resource *resource, int node_id)
 	return connection ? rcu_dereference(connection->transport.net_conf)->name : "";
 }
 
! remove: unneccessary now as we fixed the RCU locks.
-static void forget_bitmap(struct drbd_device *device, int node_id) __must_hold(local)
+static void forget_bitmap(struct drbd_device *device, int node_id, unsigned long *spin_lock_flags_p) __must_hold(local)
 {
 	int bitmap_index = device->ldev->md.peers[node_id].bitmap_index;
 	const char *name;
@@ -5320,7 +5338,7 @@ static void forget_bitmap(struct drbd_device *device, int node_id) __must_hold(l
 	if (_drbd_bm_total_weight(device, bitmap_index) == 0)
 		return;
 
! remove: unneccessary now as we fixed the RCU locks.
-	spin_unlock_irq(&device->ldev->md.uuid_lock);
+	spin_unlock_irqrestore(&device->ldev->md.uuid_lock, *spin_lock_flags_p);
 	rcu_read_lock();
 	name = name_of_node_id(device->resource, node_id);
 	drbd_info(device, "clearing bitmap UUID and content (%lu bits) for node %d (%s)(slot %d)\n",
@@ -5332,10 +5350,10 @@ static void forget_bitmap(struct drbd_device *device, int node_id) __must_hold(l
 	drbd_bm_unlock(device);
 	drbd_resume_io(device);
 	drbd_md_mark_dirty(device);
! remove: unneccessary now as we fixed the RCU locks.
-	spin_lock_irq(&device->ldev->md.uuid_lock);
+	spin_lock_irqsave(&device->ldev->md.uuid_lock, *spin_lock_flags_p);
 }
 
! remove: unneccessary now as we fixed the RCU locks.
-static void copy_bitmap(struct drbd_device *device, int from_id, int to_id) __must_hold(local)
+static void copy_bitmap(struct drbd_device *device, int from_id, int to_id, unsigned long *spin_lock_flags_p) __must_hold(local)
 {
 	struct drbd_peer_device *peer_device = peer_device_by_node_id(device, to_id);
 	struct drbd_peer_md *peer_md = device->ldev->md.peers;
@@ -5353,7 +5371,7 @@ static void copy_bitmap(struct drbd_device *device, int from_id, int to_id) __mu
 	if (peer_device && peer_device->comm_bitmap_uuid == previous_bitmap_uuid)
 		peer_device->comm_bitmap_uuid = peer_md[from_id].bitmap_uuid;
 
! remove: unneccessary now as we fixed the RCU locks.
-	spin_unlock_irq(&device->ldev->md.uuid_lock);
+	spin_unlock_irqrestore(&device->ldev->md.uuid_lock, *spin_lock_flags_p);
 	rcu_read_lock();
 	from_name = name_of_node_id(device->resource, from_id);
 	to_name = name_of_node_id(device->resource, to_id);
@@ -5366,7 +5384,7 @@ static void copy_bitmap(struct drbd_device *device, int from_id, int to_id) __mu
 	drbd_bm_unlock(device);
 	drbd_resume_io(device);
 	drbd_md_mark_dirty(device);
! remove: unneccessary now as we fixed the RCU locks.
-	spin_lock_irq(&device->ldev->md.uuid_lock);
+	spin_lock_irqsave(&device->ldev->md.uuid_lock, *spin_lock_flags_p);
 }
 
 static int find_node_id_by_bitmap_uuid(struct drbd_device *device, u64 bm_uuid) __must_hold(local)
@@ -5404,7 +5422,7 @@ static bool node_connected(struct drbd_resource *resource, int node_id)
 	return r;
 }
 
! remove: unneccessary now as we fixed the RCU locks.
-static bool detect_copy_ops_on_peer(struct drbd_peer_device *peer_device) __must_hold(local)
+static bool detect_copy_ops_on_peer(struct drbd_peer_device *peer_device, unsigned long *spin_lock_irq_flags_p) __must_hold(local)
 {
 	struct drbd_device *device = peer_device->device;
 	struct drbd_peer_md *peer_md = device->ldev->md.peers;
@@ -5458,13 +5476,13 @@ found:
 
 	if (from_id != node_id1 &&
 	    peer_md[node_id1].bitmap_uuid != peer_bm_uuid) {
! remove: unneccessary now as we fixed the RCU locks.
-		copy_bitmap(device, from_id, node_id1);
+		copy_bitmap(device, from_id, node_id1, spin_lock_irq_flags_p);
 		modified = true;
 
 	}
 	if (from_id != node_id2 &&
 	    peer_md[node_id2].bitmap_uuid != peer_bm_uuid) {
! remove: unneccessary now as we fixed the RCU locks.
-		copy_bitmap(device, from_id, node_id2);
+		copy_bitmap(device, from_id, node_id2, spin_lock_irq_flags_p);
 		modified = true;
 	}
 
@@ -5473,6 +5491,7 @@ found:
 
 void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __must_hold(local)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_irq_flags;
 	u64 peer_current_uuid = peer_device->current_uuid & ~UUID_PRIMARY;
 	struct drbd_device *device = peer_device->device;
 	struct drbd_peer_md *peer_md = device->ldev->md.peers;
@@ -5485,7 +5504,7 @@ void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __m
 	current_equal = peer_current_uuid == (drbd_resolved_uuid(peer_device, NULL) & ~UUID_PRIMARY) &&
 		!(peer_device->uuid_flags & UUID_FLAG_SYNC_TARGET);
 
! remove: unneccessary now as we fixed the RCU locks.
-	spin_lock_irq(&device->ldev->md.uuid_lock);
+	spin_lock_irqsave(&device->ldev->md.uuid_lock, spin_lock_irq_flags);
 
 	if (peer_device->repl_state[NOW] == L_OFF && current_equal) {
 		u64 bm_to_peer = peer_device->comm_bitmap_uuid & ~UUID_PRIMARY;
@@ -5501,7 +5520,7 @@ void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __m
 			drbd_info(peer_device, "Missed end of resync as sync-source\n");
 			set_bit(RS_SOURCE_MISSED_END, &peer_device->flags);
 		}
! remove: unneccessary now as we fixed the RCU locks.
-		spin_unlock_irq(&device->ldev->md.uuid_lock);
+		spin_unlock_irqrestore(&device->ldev->md.uuid_lock, spin_lock_irq_flags);
 		return;
 	}
 
@@ -5528,7 +5547,7 @@ void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __m
 				if (node_id == peer_device->node_id)
 					drbd_print_uuids(peer_device, "updated UUIDs");
 				else if (peer_md[node_id].flags & MDF_HAVE_BITMAP)
! remove: unneccessary now as we fixed the RCU locks.
-					forget_bitmap(device, node_id);
+					forget_bitmap(device, node_id, &spin_lock_irq_flags);
 				else
 					drbd_info(device, "Clearing bitmap UUID for node %d\n",
 						  node_id);
@@ -5542,7 +5561,7 @@ void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __m
 					 peer_md[node_id].bitmap_dagtag)) {
 				if (peer_md[node_id].flags & MDF_HAVE_BITMAP &&
 				    peer_md[from_node_id].flags & MDF_HAVE_BITMAP)
! remove: unneccessary now as we fixed the RCU locks.
-					copy_bitmap(device, from_node_id, node_id);
+					copy_bitmap(device, from_node_id, node_id, &spin_lock_irq_flags);
 				else
 					drbd_info(device, "Node %d synced up to node %d.\n",
 						  node_id, from_node_id);
@@ -5552,8 +5571,8 @@ void drbd_uuid_detect_finished_resyncs(struct drbd_peer_device *peer_device) __m
 		}
 	}
 
! remove: unneccessary now as we fixed the RCU locks.
-	write_bm |= detect_copy_ops_on_peer(peer_device);
-	spin_unlock_irq(&device->ldev->md.uuid_lock);
+	write_bm |= detect_copy_ops_on_peer(peer_device, &spin_lock_irq_flags);
+	spin_unlock_irqrestore(&device->ldev->md.uuid_lock, spin_lock_irq_flags);
 
 	if (write_bm || filled) {
 		u64 to_nodes = filled ? -1 : ~NODE_MASK(peer_device->node_id);
@@ -5857,7 +5876,6 @@ static void md_sync_timer_fn(struct timer_list *t)
 	drbd_device_post_work(device, MD_SYNC);
 }
 
-
 void lock_all_resources(void)
 {
 	struct drbd_resource *resource;
diff --git a/drbd/drbd_nl.c b/drbd/drbd_nl.c
index 3a7991e74..4c7be2b03 100644
--- a/drbd/drbd_nl.c
+++ b/drbd/drbd_nl.c
@@ -33,6 +33,7 @@
 #include <net/genetlink.h>
 #include <net/sock.h>
 
! upstream and compat: have 'primary' and 'secondary' Linux events.
+#include "windrbd.h"
 #include "drbd_meta_data.h"
 
 /* .doit */
@@ -93,7 +94,7 @@ void drbd_enable_netns(void)
 
 atomic_t drbd_genl_seq = ATOMIC_INIT(2); /* two. */
 
! compat: have is_initialized flag in mutex, and initialize on first usage (but not spinlocks spinlocks are 0).
-DEFINE_MUTEX(notification_mutex);
+struct mutex notification_mutex;
 
 /* used bdev_open_by_path, to claim our meta data device(s) */
 static char *drbd_m_holder = "Hands off! this is DRBD's meta data device.";
@@ -1033,6 +1034,13 @@ drbd_set_role(struct drbd_resource *resource, enum drbd_role role, bool force, c
 	const char *err_str = NULL;
 	enum chg_state_flags flags = CS_ALREADY_SERIALIZED | CS_DONT_RETRY | CS_WAIT_COMPLETE;
 	bool fenced_peers = false;
! upstream and compat: have 'primary' and 'secondary' Linux events.
+	enum drbd_role old_role = resource->role[NOW];
+
+	if (old_role == R_PRIMARY && role == R_SECONDARY) {
+		idr_for_each_entry(&resource->devices, device, vnr) {
+			windrbd_become_secondary(device, &err_str);
+		}
+	}
 
 retry:
 
@@ -1249,6 +1257,12 @@ retry:
 			kobject_uevent(&disk_to_dev(device->vdisk)->kobj, KOBJ_CHANGE);
 	}
 
! upstream and compat: have 'primary' and 'secondary' Linux events.
+	if (old_role == R_SECONDARY && role == R_PRIMARY) {
+		idr_for_each_entry(&resource->devices, device, vnr) {
+			windrbd_become_primary(device, &err_str);
+		}
+	}
+
 out:
 	up(&resource->state_sem);
 	if (err_str) {
@@ -1284,7 +1298,7 @@ void youngest_and_oldest_opener_to_str(struct drbd_device *device, char *buf, si
 		first = list_first_entry(&device->openers, struct opener, list);
 		ts = ktime_to_timespec64(first->opened);
 		time64_to_tm(ts.tv_sec, -sys_tz.tz_minuteswest * 60, &tm);
! compat: use Linux vsnprintf() implementation (with %l as 'ULONG_PTR')
-		cnt = snprintf(buf, len, " [%s:%d:%04ld-%02d-%02d_%02d:%02d:%02d.%03ld]",
+		cnt = snprintf(buf, len, " [%s:%d:%04" PRI_LONG_PTR "-%02d-%02d_%02d:%02d:%02d.%03" PRI_LONG_PTR "]",
 			      first->comm, first->pid,
 			      tm.tm_year + 1900, tm.tm_mon + 1, tm.tm_mday,
 			      tm.tm_hour, tm.tm_min, tm.tm_sec, ts.tv_nsec / NSEC_PER_MSEC);
@@ -1295,7 +1309,7 @@ void youngest_and_oldest_opener_to_str(struct drbd_device *device, char *buf, si
 			len -= cnt-1;
 			ts = ktime_to_timespec64(last->opened);
 			time64_to_tm(ts.tv_sec, -sys_tz.tz_minuteswest * 60, &tm);
! compat: use Linux vsnprintf() implementation (with %l as 'ULONG_PTR')
-			snprintf(buf, len, "%s%s:%d:%04ld-%02d-%02d_%02d:%02d:%02d.%03ld]",
+			snprintf(buf, len, "%s%s:%d:%04" PRI_LONG_PTR "-%02d-%02d_%02d:%02d:%02d.%03" PRI_LONG_PTR "]",
 			      device->open_cnt > 2 ? ", ..., " : ", ",
 			      last->comm, last->pid,
 			      tm.tm_year + 1900, tm.tm_mon + 1, tm.tm_mday,
@@ -1330,7 +1344,7 @@ static int put_device_opener_info(struct drbd_device *device, struct sk_buff *re
 			dotdotdot = "[...]\n";
 		}
 		drbd_msg_sprintf_info(reply_skb,
! compat: use Linux vsnprintf() implementation (with %l as 'ULONG_PTR')
-			"%sdrbd%d opened by %s (pid %d) at %04ld-%02d-%02d %02d:%02d:%02d.%03ld",
+			"%sdrbd%d opened by %s (pid %d) at %04" PRI_LONG_PTR "-%02d-%02d %02d:%02d:%02d.%03" PRI_LONG_PTR,
 			dotdotdot,
 			device->minor, o->comm, o->pid,
 			tm.tm_year + 1900, tm.tm_mon + 1, tm.tm_mday,
@@ -2632,7 +2646,7 @@ static int clear_peer_slot(struct drbd_device *device, int peer_node_id, u32 md_
 {
 	struct drbd_peer_md *peer_md, *day0_md;
 	struct meta_data_on_disk_9 *buffer;
! upstream: was that an uninitialized variable? If not then remove.
-	int from_index, freed_index;
+	int from_index, freed_index = -1;
 	bool free_bitmap_slot;
 
 	if (!get_ldev(device))
@@ -7840,3 +7854,47 @@ out:
 	drbd_adm_finish(&adm_ctx, info, retcode);
 	return 0;
 }
! compat: this should work somehow with implementing register_netlink_family
+
+/* TODO: WinDRBD specific: we have several static variables which we
+ * can only access in here:
+ */
+
! compat: this should work somehow with implementing register_netlink_family
+/* For WINDRBD_NETLINK_FAMILY_ID: */
+#include "windrbd/windrbd_ioctl.h"
+
+/* Those two functions taken from netlink.c_inc, originally they
+ * are probably generated.
+ */
+
! compat: this should work somehow with implementing register_netlink_family
+int drbd_tla_parse(struct nlmsghdr *nlh, struct nlattr **attr)
+{
+		/* Since drbd_nl.c does not have an initialization
+		 * function and drbd_genl_family is static (we don't
+		 * want to change the header), we initialize that here.
+		 * This is 'our' function.
+		 */
+	drbd_genl_family.id = WINDRBD_NETLINK_FAMILY_ID;
+
+       return nla_parse(attr, ARRAY_SIZE(drbd_tla_nl_policy) - 1,
+               nlmsg_attrdata(nlh, GENL_HDRLEN + drbd_genl_family.hdrsize),
+               nlmsg_attrlen(nlh, GENL_HDRLEN + drbd_genl_family.hdrsize),
+               drbd_tla_nl_policy);
+}
+
! compat: this should work somehow with implementing register_netlink_family
+struct genl_ops *get_drbd_genl_ops(u8 cmd)
+{
+       int i;
+
+       for (i=0; i<sizeof(drbd_genl_ops)/sizeof((drbd_genl_ops)[0]); i++) {
+               if (drbd_genl_ops[i].cmd == cmd)
+                       return &drbd_genl_ops[i];
+       }
+       return NULL;
+}
+
! compat: this should work somehow with implementing register_netlink_family
+/* Don't want to touch magic func header, which declares this static */
+
+const char *windrbd_genl_cmd_to_str(u8 cmd)
+{
+       return drbd_genl_cmd_to_str(cmd);
+}
diff --git a/drbd/drbd_receiver.c b/drbd/drbd_receiver.c
index 5a5592504..c2aac6c55 100644
--- a/drbd/drbd_receiver.c
+++ b/drbd/drbd_receiver.c
@@ -3447,7 +3447,6 @@ void drbd_cleanup_after_failed_submit_peer_write(struct drbd_peer_request *peer_
 		drbd_al_complete_io(device, &peer_req->i);
 
 	drbd_remove_peer_req_interval(peer_req);
! remove: empty line
-
 	drbd_may_finish_epoch(connection, peer_req->epoch, EV_PUT + EV_CLEANUP);
 	put_ldev(device);
 	drbd_free_peer_req(peer_req);
@@ -6308,6 +6307,7 @@ static int receive_uuids(struct drbd_connection *connection, struct packet_info
 
 static int receive_uuids110(struct drbd_connection *connection, struct packet_info *pi)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_flags = 0;	/* silence warning */
 	struct drbd_peer_device *peer_device;
 	struct p_uuids110 *p = pi->data;
 	int bitmap_uuids, history_uuids, rest, i, pos, err;
@@ -6348,7 +6348,7 @@ static int receive_uuids110(struct drbd_connection *connection, struct packet_in
 
 	if (get_ldev(device)) {
 		peer_md = device->ldev->md.peers;
! remove: unneccessary now as we fixed the RCU locks.
-		spin_lock_irq(&device->ldev->md.uuid_lock);
+		spin_lock_irqsave(&device->ldev->md.uuid_lock, spin_lock_flags);
 	}
 
 	if (device->resource->role[NOW] != R_PRIMARY ||
@@ -6389,7 +6389,7 @@ static int receive_uuids110(struct drbd_connection *connection, struct packet_in
 		peer_device->history_uuids[i++] = 0;
 	set_bit(UUIDS_RECEIVED, &peer_device->flags);
 	if (peer_md) {
! remove: unneccessary now as we fixed the RCU locks.
-		spin_unlock_irq(&device->ldev->md.uuid_lock);
+		spin_unlock_irqrestore(&device->ldev->md.uuid_lock, spin_lock_flags);
 		put_ldev(device);
 	}
 
@@ -6922,7 +6922,6 @@ far_away_change(struct drbd_connection *connection,
 	unsigned long irq_flags;
 	int iterate_vnr;
 
-
 	if (flags & CS_PREPARE && mask.role == role_MASK && val.role == R_PRIMARY &&
 	    resource->role[NOW] == R_PRIMARY) {
 		struct net_conf *nc;
@@ -7092,7 +7091,7 @@ enum alt_rv {
 	ALT_TIMEOUT,
 };
 
! remove: unneccessary now as we fixed the RCU locks.
-static enum alt_rv when_done_lock(struct drbd_resource *resource, unsigned int for_tid)
+static enum alt_rv when_done_lock(struct drbd_resource *resource, unsigned int for_tid, unsigned long *spin_lock_irq_flags_p)
 {
 	write_lock_irq(&resource->state_rwlock);
 	if (!resource->remote_state_change)
@@ -7103,7 +7102,8 @@ static enum alt_rv when_done_lock(struct drbd_resource *resource, unsigned int f
 
 	return ALT_TIMEOUT;
 }
! remove: unneccessary now as we fixed the RCU locks.
-static enum alt_rv abort_local_transaction(struct drbd_connection *connection, unsigned int for_tid)
+
+static enum alt_rv abort_local_transaction(struct drbd_connection *connection, unsigned int for_tid, unsigned long *spin_lock_irq_flags_p)
 {
 	struct drbd_resource *resource = connection->resource;
 	struct net_conf *nc;
@@ -7116,10 +7116,10 @@ static enum alt_rv abort_local_transaction(struct drbd_connection *connection, u
 	rcu_read_unlock();
 
 	set_bit(TWOPC_ABORT_LOCAL, &resource->flags);
! remove: unneccessary now as we fixed the RCU locks.
-	write_unlock_irq(&resource->state_rwlock);
+	write_unlock_irqrestore(&resource->state_rwlock, *spin_lock_irq_flags_p);
 	wake_up_all(&resource->state_wait);
 	wait_event_timeout(resource->twopc_wait,
! remove: unneccessary now as we fixed the RCU locks.
-			   (rv = when_done_lock(resource, for_tid)) != ALT_TIMEOUT, t);
+			   (rv = when_done_lock(resource, for_tid, spin_lock_irq_flags_p)) != ALT_TIMEOUT, t);
 	clear_bit(TWOPC_ABORT_LOCAL, &resource->flags);
 	return rv;
 }
@@ -7317,6 +7317,7 @@ static void process_twopc(struct drbd_connection *connection,
 			 struct packet_info *pi,
 			 unsigned long receive_jif)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_irq_flags;
 	struct drbd_connection *affected_connection = connection;
 	struct drbd_resource *resource = connection->resource;
 	struct drbd_peer_device *peer_device = NULL;
@@ -7328,6 +7329,25 @@ static void process_twopc(struct drbd_connection *connection,
 	bool waiting_allowed = true;
 	enum csc_rv csc_rv;
 
! compat: Windows OS should inform us about device size change ...
+		/* Under Windows, queriing a block device's size must
+		 * happen at IRQL < DISPATCH_LEVEL. Since holding a
+		 * spinlock raises the IRQL to DISPATCH_LEVEL we have
+		 * to query the backing device size before we take
+		 * the spinlock. A later query of the get_capacity()
+		 * function inside the spinlock will then return the
+		 * value we cached here.
+		 */
+
+	if (pi->cmd == P_TWOPC_PREP_RSZ) {
+		struct drbd_device *device;
+
+		device = conn_peer_device(connection, pi->vnr)->device;
+		if (get_ldev(device)) {
+			(void) drbd_get_capacity(device->ldev->backing_bdev);
+			put_ldev(device);
+		}
+	}
+
 	request.tid = be32_to_cpu(p->tid);
 	if (connection->agreed_features & DRBD_FF_2PC_V2) {
 		request.flags = be32_to_cpu(p->flags);
@@ -7344,7 +7364,7 @@ static void process_twopc(struct drbd_connection *connection,
 
 	/* Check for concurrent transactions and duplicate packets. */
 retry:
! remove: unneccessary now as we fixed the RCU locks.
-	write_lock_irq(&resource->state_rwlock);
+	write_lock_irqsave(&resource->state_rwlock, spin_lock_irq_flags);
 
 	csc_rv = check_concurrent_transactions(resource, reply);
 
@@ -7354,14 +7374,14 @@ retry:
 
 		if (!is_prepare(pi->cmd)) {
 			/* We have committed or aborted this transaction already. */
! remove: unneccessary now as we fixed the RCU locks.
-			write_unlock_irq(&resource->state_rwlock);
+			write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 			dynamic_drbd_dbg(connection, "Ignoring %s packet %u\n",
 				   drbd_packet_name(pi->cmd),
 				   reply->tid);
 			return;
 		}
 		if (reply->is_aborted) {
! remove: unneccessary now as we fixed the RCU locks.
-			write_unlock_irq(&resource->state_rwlock);
+			write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 			return;
 		}
 		resource->remote_state_change = true;
@@ -7376,7 +7396,8 @@ retry:
 		flags |= CS_PREPARED;
 
 		if (test_and_set_bit(TWOPC_EXECUTED, &resource->flags)) {
! remove: unneccessary now as we fixed the RCU locks.
-			write_unlock_irq(&resource->state_rwlock);
+			write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
+
 			drbd_info(connection, "Ignoring redundant %s packet %u.\n",
 				  drbd_packet_name(pi->cmd),
 				  reply->tid);
@@ -7389,7 +7410,7 @@ retry:
 			  "state change %u.\n",
 			  resource->twopc_reply.tid,
 			  reply->tid);
! remove: unneccessary now as we fixed the RCU locks.
-		alt_rv = abort_local_transaction(connection, reply->tid);
+		alt_rv = abort_local_transaction(connection, reply->tid, &spin_lock_irq_flags);
 		if (alt_rv == ALT_MATCH) {
 			/* abort_local_transaction() comes back unlocked in this case... */
 			goto match;
@@ -7404,7 +7425,7 @@ retry:
 		}
 		/* abort_local_transaction() returned with the state_rwlock write lock */
 		if (reply->is_aborted) {
! remove: unneccessary now as we fixed the RCU locks.
-			write_unlock_irq(&resource->state_rwlock);
+			write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 			return;
 		}
 		resource->remote_state_change = true;
@@ -7415,11 +7436,11 @@ retry:
 		clear_bit(TWOPC_EXECUTED, &resource->flags);
 	} else if (pi->cmd == P_TWOPC_ABORT) {
 		/* crc_rc != CRC_MATCH */
! remove: unneccessary now as we fixed the RCU locks.
-		write_unlock_irq(&resource->state_rwlock);
+		write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 		nested_twopc_abort(resource, &request);
 		return;
 	} else {
! remove: unneccessary now as we fixed the RCU locks.
-		write_unlock_irq(&resource->state_rwlock);
+		write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 
 		if (csc_rv == CSC_TID_MISS && is_prepare(pi->cmd) && waiting_allowed) {
 			/* CSC_TID_MISS implies the two transactions are from the same initiator */
@@ -7435,7 +7456,6 @@ retry:
 				goto retry;
 			}
 		}
! remove: empty line
-
 		if (csc_rv == CSC_REJECT ||
 		    (csc_rv == CSC_TID_MISS && is_prepare(pi->cmd))) {
 			drbd_info(connection, "Rejecting concurrent "
@@ -7456,10 +7476,10 @@ retry:
 				drbd_info(connection,
 						"Duplicate prepare for remote state change %u\n",
 						reply->tid);
! remove: unneccessary now as we fixed the RCU locks.
-				write_lock_irq(&resource->state_rwlock);
+				write_lock_irqsave(&resource->state_rwlock, spin_lock_irq_flags);
 				resource->twopc_parent_nodes |= NODE_MASK(connection->peer_node_id);
 				reply_cmd = resource->twopc_prepare_reply_cmd;
! remove: unneccessary now as we fixed the RCU locks.
-				write_unlock_irq(&resource->state_rwlock);
+				write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 
 				if (reply_cmd) {
 					drbd_send_twopc_reply(connection, reply_cmd,
@@ -7467,10 +7487,10 @@ retry:
 				} else {
 					/* if a node sends us a prepare, that means he has
 					   prepared this himsilf successfully. */
! remove: unneccessary now as we fixed the RCU locks.
-					write_lock_irq(&resource->state_rwlock);
+					write_lock_irqsave(&resource->state_rwlock, spin_lock_irq_flags);
 					set_bit(TWOPC_YES, &connection->flags);
 					drbd_maybe_cluster_wide_reply(resource);
! remove: unneccessary now as we fixed the RCU locks.
-					write_unlock_irq(&resource->state_rwlock);
+					write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 				}
 			}
 		} else {
@@ -7574,7 +7594,7 @@ retry:
 	}
 
 	resource->twopc_reply = *reply;
! remove: unneccessary now as we fixed the RCU locks.
-	write_unlock_irq(&resource->state_rwlock);
+	write_unlock_irqrestore(&resource->state_rwlock, spin_lock_irq_flags);
 
 	if (affected_connection && affected_connection != connection &&
 	    affected_connection->cstate[NOW] == C_CONNECTED) {
@@ -8196,7 +8216,7 @@ static int receive_state(struct drbd_connection *connection, struct packet_info
 	if (old_peer_state.i != drbd_get_peer_device_state(peer_device, NOW).i) {
 		old_peer_state = drbd_get_peer_device_state(peer_device, NOW);
 		abort_state_change_locked(resource);
! remove: unneccessary now as we fixed the RCU locks.
-		write_unlock_irq(&resource->state_rwlock);
+		write_unlock_irqrestore(&resource->state_rwlock, irq_flags);
 		goto retry;
 	}
 	clear_bit(CONSIDER_RESYNC, &peer_device->flags);
@@ -9565,6 +9585,7 @@ static bool initiator_can_commit_or_abort(struct drbd_connection *connection)
 
 static void cleanup_remote_state_change(struct drbd_connection *connection)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_irq_flags;
 	struct drbd_resource *resource = connection->resource;
 	struct twopc_reply *reply = &resource->twopc_reply;
 	struct twopc_request request;
@@ -9590,7 +9611,7 @@ static void cleanup_remote_state_change(struct drbd_connection *connection)
 			timer_delete(&resource->twopc_timer);
 			__clear_remote_state_change(resource);
 		} else {
! remove: unneccessary now as we fixed the RCU locks.
-			enum alt_rv alt_rv = abort_local_transaction(connection, 0);
+			enum alt_rv alt_rv = abort_local_transaction(connection, 0, &spin_lock_irq_flags);
 			if (alt_rv != ALT_LOCKED)
 				return;
 		}
diff --git a/drbd/drbd_req.c b/drbd/drbd_req.c
index e941ad6c6..57ecd4abe 100644
--- a/drbd/drbd_req.c
+++ b/drbd/drbd_req.c
@@ -1942,8 +1942,9 @@ static void drbd_send_and_submit(struct drbd_request *req)
 	struct bio_and_error m = { NULL, };
 	bool no_remote = false;
 	bool submit_private_bio = false;
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long flags;
 
! remove: unneccessary now as we fixed the RCU locks.
-	read_lock_irq(&resource->state_rwlock);
+	read_lock_irqsave(&resource->state_rwlock, flags);
 
 	if (rw == WRITE) {
 		/* check for congestion, and potentially stop sending
@@ -2081,7 +2082,7 @@ nodata:
 
 out:
 	drbd_req_put_completion_ref(req, &m, 1);
! remove: unneccessary now as we fixed the RCU locks.
-	read_unlock_irq(&resource->state_rwlock);
+	read_unlock_irqrestore(&resource->state_rwlock, flags);
 
 	/* Even though above is a kref_put(), this is safe.
 	 * As long as we still need to submit our private bio,
@@ -2760,7 +2761,8 @@ void request_timer_fn(struct timer_list *t)
 	struct drbd_resource *resource = device->resource;
 	struct drbd_connection *connection;
 	struct drbd_request *req_read, *req_write;
! remove: change to old version
-	unsigned long oldest_submit_jif, irq_flags;
+	unsigned long oldest_submit_jif;
+	unsigned long irq_flags;
 	unsigned long disk_timeout = 0, effective_timeout = 0, now = jiffies, next_trigger_time = now;
 	bool restart_timer = false, io_error = false;
 	unsigned long timeout_peers = 0;
diff --git a/drbd/drbd_sender.c b/drbd/drbd_sender.c
index 30f2cbc00..3769b478f 100644
--- a/drbd/drbd_sender.c
+++ b/drbd/drbd_sender.c
@@ -1837,7 +1837,7 @@ void drbd_resync_finished(struct drbd_peer_device *peer_device,
 	{
 	char tmp[sizeof(" but 01234567890123456789 4k blocks skipped")] = "";
 	if (verify_done && peer_device->ov_skipped)
! compat: use Linux vsnprintf() implementation (with %l as 'ULONG_PTR')
-		snprintf(tmp, sizeof(tmp), " but %lu %dk blocks skipped",
+		snprintf(tmp, sizeof(tmp), " but %" PRI_ULONG_PTR " %dk blocks skipped",
 			peer_device->ov_skipped, Bit2KB(1));
 	drbd_info(peer_device, "%s %s%s (total %lu sec; paused %lu sec; %lu K/sec)\n",
 		  verify_done ? "Online verify" : "Resync",
@@ -2501,7 +2501,7 @@ static void maybe_send_unplug_remote(struct drbd_connection *connection, bool se
 		/* Paranoia: to avoid a continuous stream of unplug-hints,
 		 * in case we never get any unplug events */
 		connection->todo.unplug_dagtag_sector[connection->todo.unplug_slot] =
! upstream: this is a DRBD bug
-			connection->send.current_dagtag_sector + (1ULL << 63);
+			connection->send.current_dagtag_sector + (1ULL << 62);
 		/* advance the current unplug slot */
 		connection->todo.unplug_slot ^= 1;
 	} else if (!send_anyways)
@@ -3260,7 +3260,7 @@ static struct drbd_request *__next_request_for_connection(
 	return NULL;
 }
 
! remove: unneccessary now as we fixed the RCU locks.
-static struct drbd_request *tl_next_request_for_connection(struct drbd_connection *connection)
+static struct drbd_request *tl_next_request_for_connection(struct drbd_connection *connection, unsigned long *flags_p)
 {
 	if (connection->todo.req_next == NULL)
 		connection->todo.req_next = __next_request_for_connection(connection);
@@ -3312,10 +3312,10 @@ static void maybe_send_state_afer_ahead(struct drbd_connection *connection)
  * It also moves all currently queued connection->sender_work
  * to connection->todo.work_list.
  */
! remove: unneccessary now as we fixed the RCU locks.
-static bool check_sender_todo(struct drbd_connection *connection)
+static bool check_sender_todo(struct drbd_connection *connection, unsigned long *flags_p)
 {
 	rcu_read_lock();
! remove: unneccessary now as we fixed the RCU locks.
-	tl_next_request_for_connection(connection);
+	tl_next_request_for_connection(connection, flags_p);
 
 	/* FIXME can we get rid of this additional lock? */
 	spin_lock_irq(&connection->sender_work.q_lock);
@@ -3331,12 +3331,14 @@ static bool check_sender_todo(struct drbd_connection *connection)
 static void wait_for_sender_todo(struct drbd_connection *connection)
 {
 	struct drbd_resource *resource = connection->resource;
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_irq_flags;
+
 	DEFINE_WAIT(wait);
 	struct net_conf *nc;
 	int uncork, cork;
 	bool got_something = 0;
 
! remove: unneccessary now as we fixed the RCU locks.
-	got_something = check_sender_todo(connection);
+	got_something = check_sender_todo(connection, &spin_lock_irq_flags);
 	if (got_something)
 		return;
 
@@ -3357,7 +3359,7 @@ static void wait_for_sender_todo(struct drbd_connection *connection)
 		int send_barrier;
 		prepare_to_wait(&connection->sender_work.q_wait, &wait,
 				TASK_INTERRUPTIBLE);
! remove: unneccessary now as we fixed the RCU locks.
-		if (check_sender_todo(connection) || signal_pending(current)) {
+		if (check_sender_todo(connection, &spin_lock_irq_flags) || signal_pending(current)) {
 			break;
 		}
 
@@ -3486,6 +3488,7 @@ static bool is_write_in_flight(struct drbd_peer_device *peer_device, struct drbd
 
 static int process_one_request(struct drbd_connection *connection)
 {
! remove: unneccessary now as we fixed the RCU locks.
+	unsigned long spin_lock_irq_flags;
 	struct bio_and_error m;
 	struct drbd_request *req = connection->todo.req;
 	struct drbd_device *device = req->device;
@@ -3582,7 +3585,7 @@ static int process_one_request(struct drbd_connection *connection)
 	__req_mod(req, what, peer_device, &m);
 	read_unlock_irq(&connection->resource->state_rwlock);
 
! remove: unneccessary now as we fixed the RCU locks.
-	check_sender_todo(connection);
+	check_sender_todo(connection, &spin_lock_irq_flags);
 
 	if (m.bio)
 		complete_master_bio(device, &m);
diff --git a/drbd/drbd_transport.c b/drbd/drbd_transport.c
index 00e7f9269..aa945a6c8 100644
--- a/drbd/drbd_transport.c
+++ b/drbd/drbd_transport.c
@@ -8,7 +8,12 @@
 #include "drbd_int.h"
 
 static LIST_HEAD(transport_classes);
! compat: have is_initialized flag in rw_semaphore, and initialize on first usage (but not spinlocks spinlocks are 0).
-static DECLARE_RWSEM(transport_classes_lock);
+static struct rw_semaphore transport_classes_lock;
+
+void init_transport(void)
+{
+	init_rwsem(&transport_classes_lock);
+}
 
 static struct drbd_transport_class *__find_transport_class(const char *transport_name)
 {
diff --git a/drbd/drbd_transport_tcp.c b/drbd/drbd_transport_tcp.c
index 6ec26a091..c4ab3aa3d 100644
--- a/drbd/drbd_transport_tcp.c
+++ b/drbd/drbd_transport_tcp.c
@@ -301,6 +301,7 @@ static int dtt_recv_short(struct socket *socket, void *buf, size_t size, int fla
 
 	ret = kernel_recvmsg(socket, &msg, &iov, 1, size, flags);
 
! compat: have TLS dummy
+#if 0
 	if (msg.msg_controllen != sizeof(u)) {
 		u8 level, description;
 
@@ -319,6 +320,7 @@ static int dtt_recv_short(struct socket *socket, void *buf, size_t size, int fla
 			break;
 		}
 	}
! compat: have TLS dummy
+#endif
 
 	return ret;
 }
@@ -434,6 +436,7 @@ static void dtt_setbufsize(struct socket *socket, unsigned int snd,
 	} else {
 		sk->sk_userlocks &= ~SOCK_RCVBUF_LOCK;
 	}
! compat: this should be done by the sk_write_space() function
+	windrbd_update_socket_buffer_sizes(socket);
 }
 
 static bool dtt_path_cmp_addr(struct dtt_path *path)
@@ -536,6 +539,8 @@ out:
 	return err;
 }
 
! compat: have TLS dummy
+#if 0
+
 typedef int (*tls_hello_func)(const struct tls_handshake_args *, gfp_t);
 
 struct tls_handshake_wait {
@@ -601,6 +606,8 @@ static int tls_wait_hello(struct tls_handshake_wait *csocket_tls_wait,
 	return dsocket_tls_wait->status;
 }
 
! compat: have TLS dummy
+#endif
+
 
 static int dtt_send_first_packet(struct drbd_tcp_transport *tcp_transport, struct socket *socket,
 				 enum drbd_packet cmd)
@@ -623,7 +630,9 @@ static void dtt_socket_free(struct socket **socket)
 	if (!*socket)
 		return;
 
! compat: have TLS dummy
+#if 0
 	tls_handshake_cancel((*socket)->sk);
! compat: have TLS dummy
+#endif
 	kernel_sock_shutdown(*socket, SHUT_RDWR);
 
 	if ((*socket)->file)
@@ -855,6 +864,7 @@ static int dtt_receive_first_packet(struct drbd_tcp_transport *tcp_transport, st
 	return be16_to_cpu(h->command);
 }
 
! compat: implement skb_prepare_seq_read, ...
+#if 0
 
 static int dtt_control_tcp_input(read_descriptor_t *rd_desc, struct sk_buff *skb,
 				 unsigned int offset, size_t len)
@@ -884,6 +894,8 @@ static int dtt_control_tcp_input(read_descriptor_t *rd_desc, struct sk_buff *skb
 	return consumed;
 }
 
! compat: implement skb_prepare_seq_read, ...
+#endif
+
 static void dtt_control_data_ready_work(struct work_struct *item)
 {
 	struct drbd_tcp_transport *tcp_transport =
@@ -910,10 +922,12 @@ static void dtt_control_data_ready(struct sock *sock)
 	struct drbd_tcp_transport *tcp_transport =
 		container_of(transport, struct drbd_tcp_transport, transport);
 
! compat: implement skb_prepare_seq_read, ...
+#if 0
 	read_descriptor_t rd_desc = {
 		.count = 1,
 		.arg = { .data = transport },
 	};
! compat: implement skb_prepare_seq_read, ...
+#endif
 
 	if (!test_bit(DTT_DATA_READY_ARMED, &tcp_transport->flags)
 	    && tcp_transport->original_control_sk_data_ready)
@@ -932,9 +946,12 @@ static void dtt_control_data_ready(struct sock *sock)
 	if (tcp_transport->control_data_ready_work.func) {
 		queue_work(dtt_csocket_recv, &tcp_transport->control_data_ready_work);
 	} else {
! compat: implement skb_prepare_seq_read, ...
+		printk("Warning: tcp_read_sock not implemented.\n");
+#if 0
 		spin_lock_bh(&tcp_transport->control_recv_lock);
 		tcp_read_sock(sock, &rd_desc, dtt_control_tcp_input);
 		spin_unlock_bh(&tcp_transport->control_recv_lock);
! compat: implement skb_prepare_seq_read, ...
+#endif
 	}
 }
 
@@ -1003,6 +1020,7 @@ static int dtt_init_listener(struct drbd_transport *transport,
 	struct socket *s_listen;
 	struct net_conf *nc;
 	const char *what = "";
! upstream: we really should use set_socket_opt() ...
+	char val;
 
 	rcu_read_lock();
 	nc = rcu_dereference(transport->net_conf);
@@ -1016,14 +1034,19 @@ static int dtt_init_listener(struct drbd_transport *transport,
 
 	my_addr = *(struct sockaddr_storage *)addr;
 
! compat: not sure what to do about SOCK_LISTEN maybe handle it as soon as listen() is called ...
-	err = sock_create_kern(net, my_addr.ss_family, SOCK_STREAM, IPPROTO_TCP, &s_listen);
-	if (err < 0) {
+	err = sock_create_kern(net, my_addr.ss_family, SOCK_LISTEN, IPPROTO_TCP, &s_listen);
+	if (err) {
 		s_listen = NULL;
 		what = "sock_create_kern";
 		goto out;
 	}
 
! upstream: we really should use set_socket_opt() ...
-	s_listen->sk->sk_reuse = SK_CAN_REUSE; /* SO_REUSEADDR */
+	val = 1;
+	err = kernel_setsockopt(s_listen, SOL_SOCKET, SO_REUSEADDR, &val, sizeof(val));
+	if (err < 0) {
+		what = "kernel_setsockopt SO_REUSEADDR";
+		goto out;
+	}
 	dtt_setbufsize(s_listen, sndbuf_size, rcvbuf_size);
 
 	addr_len = addr->sa_family == AF_INET6 ? sizeof(struct sockaddr_in6)
@@ -1134,10 +1157,17 @@ static int dtt_connect(struct drbd_transport *transport)
 	struct dtt_path *connect_to_path, *first_path = NULL;
 	struct socket *dsocket, *csocket;
 	struct net_conf *nc;
! compat: have TLS dummy
+#if 0
 	bool tls, dsocket_is_server = false, csocket_is_server = false;
! compat: have TLS dummy
+#else
+	bool tls;
+#endif
 	char peername[64];
! compat: have TLS dummy
+#if 0
 	key_serial_t tls_keyring, tls_privkey, tls_certificate;
! compat: have TLS dummy
+#endif
 	int timeout, err;
! upstream: we really should use set_socket_opt() ...
+	char one = 1;
 	bool ok;
 
 	dsocket = NULL;
@@ -1197,10 +1227,14 @@ static int dtt_connect(struct drbd_transport *transport)
 				dtt_socket_free(&s);
 			} else if (use_for_data) {
 				dsocket = s;
! compat: have TLS dummy
+#if 0
 				dsocket_is_server = false;
! compat: have TLS dummy
+#endif
 			} else {
 				csocket = s;
! compat: have TLS dummy
+#if 0
 				csocket_is_server = false;
! compat: have TLS dummy
+#endif
 			}
 		} else if (!first_path) {
 			connect_to_path = dtt_next_path(connect_to_path, transport);
@@ -1248,11 +1282,15 @@ retry:
 					tr_warn(transport, "initial packet S crossed\n");
 					dtt_socket_free(&dsocket);
 					dsocket = s;
! compat: have TLS dummy
+#if 0
 					dsocket_is_server = true;
! compat: have TLS dummy
+#endif
 					goto randomize;
 				}
 				dsocket = s;
! compat: have TLS dummy
+#if 0
 				dsocket_is_server = true;
! compat: have TLS dummy
+#endif
 				break;
 			case P_INITIAL_META:
 				set_bit(RESOLVE_CONFLICTS, &transport->flags);
@@ -1260,11 +1298,15 @@ retry:
 					tr_warn(transport, "initial packet M crossed\n");
 					dtt_socket_free(&csocket);
 					csocket = s;
! compat: have TLS dummy
+#if 0
 					csocket_is_server = true;
! compat: have TLS dummy
+#endif
 					goto randomize;
 				}
 				csocket = s;
! compat: have TLS dummy
+#if 0
 				csocket_is_server = true;
! compat: have TLS dummy
+#endif
 				break;
 			default:
 				tr_warn(transport, "Error receiving initial packet: %d\n", fp);
@@ -1286,9 +1328,11 @@ randomize:
 	timeout = nc->timeout * HZ / 10;
 	tls = nc->tls;
 	memcpy(peername, nc->name, 64);
! compat: have TLS dummy
+#if 0
 	tls_keyring = nc->tls_keyring;
 	tls_privkey = nc->tls_privkey;
 	tls_certificate = nc->tls_certificate;
! compat: have TLS dummy
+#endif
 	rcu_read_unlock();
 
 	write_lock_bh(&csocket->sk->sk_callback_lock);
@@ -1299,6 +1343,8 @@ randomize:
 	write_unlock_bh(&csocket->sk->sk_callback_lock);
 
 	if (tls) {
! compat: have TLS dummy
+		printk("Warning: TLS not supported on this platform.\n");
+#if 0
 		struct tls_handshake_wait csocket_tls_wait = {
 			.done = COMPLETION_INITIALIZER_ONSTACK(csocket_tls_wait.done),
 		};
@@ -1331,15 +1377,24 @@ randomize:
 		}
 
 		INIT_WORK(&tcp_transport->control_data_ready_work, dtt_control_data_ready_work);
! compat: have TLS dummy
+#endif
 	}
+	INIT_WORK(&tcp_transport->control_data_ready_work, dtt_control_data_ready_work);
 
 	TR_ASSERT(transport, first_path == connect_to_path);
 	set_bit(TR_ESTABLISHED, &connect_to_path->path.flags);
 	drbd_path_event(transport, &connect_to_path->path);
 
! upstream: we really should use set_socket_opt() ...
-	dsocket->sk->sk_reuse = SK_CAN_REUSE; /* SO_REUSEADDR */
-	csocket->sk->sk_reuse = SK_CAN_REUSE; /* SO_REUSEADDR */
-
+	err = kernel_setsockopt(dsocket, SOL_SOCKET, SO_REUSEADDR, &one, sizeof(one));
+	if (err < 0) {
+		printk("kernel_setsockopt SO_REUSEADDR failed\n");
+		goto out;
+	}
+	err = kernel_setsockopt(csocket, SOL_SOCKET, SO_REUSEADDR, &one, sizeof(one));
+	if (err < 0) {
+		printk("kernel_setsockopt SO_REUSEADDR failed\n");
+		goto out;
+	}
 	/* We are a block device, we are in the write-out path,
 	 * we may need memory to facilitate memory reclaim
 	 */
